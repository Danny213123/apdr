---
phase: 23-policy-truth-and-failure-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - benchmark_ui/service.py
  - benchmark_ui/runner.py
  - benchmark_ui/test_run_contract.py
  - benchmark_ui/test_runner_events.py
autonomous: true
requirements:
  - DFV-02
user_setup: []
must_haves:
  truths:
    - Every `llm` case row and live `case_complete` event must preserve requested policy and actual path as separate truths.
    - Docker bypass state must be machine-readable via explicit fields, not inferred from raw logs.
    - Historical rows and live rows must expose the same policy-truth keys so reviewers can compare them directly.
  artifacts:
    - path: "benchmark_ui/service.py"
      provides: "Case-row normalization for requested LLM policy, route label, Docker bypass reason, bypass note, and debug path"
      contains: "_result_requested_llm_validation_policy"
    - path: "benchmark_ui/runner.py"
      provides: "Live result and SSE event pass-through for policy-truth fields"
      contains: "requestedLlmValidationPolicy"
    - path: "benchmark_ui/test_runner_events.py"
      provides: "Regression coverage for live event truth fields"
      contains: "dockerBypassReason"
    - path: "benchmark_ui/test_run_contract.py"
      provides: "Regression coverage for saved-run case-row truth fields"
      contains: "llmValidationRoute"
  key_links:
    - from: "benchmark_ui/service.py"
      to: ".planning/REQUIREMENTS.md"
      via: "DFV-02 requires case inspection to show requested policy, actual path, and bypass reason together."
      pattern: "DFV-02"
    - from: "benchmark_ui/runner.py"
      to: ".planning/phases/23-policy-truth-and-failure-semantics/23-RESEARCH.md"
      via: "Phase 23 must align live SSE truth with saved-row truth rather than leaving them on different schemas."
      pattern: "Live SSE events are similarly incomplete"
---

<objective>
Thread the Phase 22 policy-truth metadata through benchmark rows and live events so operators can inspect `llm` cases without opening raw metadata files.

Purpose: APDR already writes `requested_llm_validation_policy`, `llm_validation_route`, `docker_bypass_reason`, `docker_bypass_note`, and `debug_dir`, but benchmark readers and live SSE events still hide most of them. This plan turns those saved fields into stable case-row and event fields without widening the backend model again.
Output: saved-run case rows and live `case_complete` events that expose requested policy, actual path, route label, Docker bypass reason, bypass note, and debug directory pointers.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-policy-truth-and-failure-semantics/23-RESEARCH.md
@.planning/phases/23-policy-truth-and-failure-semantics/23-VALIDATION.md
@tools/apdr/src/lib.rs
@tools/apdr/src/docker/builder/agent_backend.rs
@benchmark_ui/service.py
@benchmark_ui/runner.py
@benchmark_ui/test_run_contract.py
@benchmark_ui/test_runner_events.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add saved-run case-row readers for policy-truth and Docker debug pointers</name>
  <files>benchmark_ui/service.py, benchmark_ui/test_run_contract.py</files>
  <read_first>benchmark_ui/service.py, benchmark_ui/test_run_contract.py, tools/apdr/src/lib.rs, tools/apdr/src/docker/builder/agent_backend.rs, .planning/phases/23-policy-truth-and-failure-semantics/23-RESEARCH.md</read_first>
  <action>In `benchmark_ui/service.py`, add exact helper readers for `requested_llm_validation_policy`, `llm_validation_route`, `docker_bypass_reason`, `docker_bypass_note`, and `debug_dir`, and expose them in `_build_case_row(...)` using exact camelCase keys `requestedLlmValidationPolicy`, `llmValidationRoute`, `dockerBypassReason`, `dockerBypassNote`, and `debugDir`. Keep the existing `validationBackend`, `validationPath`, `escalatedBackend`, and `failureFamily` fields untouched so requested policy remains separate from actual route. In `benchmark_ui/test_run_contract.py`, add or update assertions that a saved `llm` case row can carry all of those exact keys from `output_metadata` without collapsing them into `validationBackend` or raw `output_metadata` access.</action>
  <verify>python3 -m unittest benchmark_ui.test_run_contract</verify>
  <acceptance_criteria>
    - `benchmark_ui/service.py` contains `_result_requested_llm_validation_policy`
    - `benchmark_ui/service.py` contains `_result_llm_validation_route`
    - `benchmark_ui/service.py` contains `_result_docker_bypass_reason`
    - `benchmark_ui/service.py` contains `_result_docker_bypass_note`
    - `benchmark_ui/service.py` contains `_result_debug_dir`
    - `benchmark_ui/test_run_contract.py` asserts `requestedLlmValidationPolicy`, `llmValidationRoute`, `dockerBypassReason`, `dockerBypassNote`, and `debugDir`
    - `python3 -m unittest benchmark_ui.test_run_contract` exits 0
  </acceptance_criteria>
  <done>Saved-run case rows now expose the exact Phase 22 policy-truth metadata needed for inspection.</done>
</task>

<task type="auto">
  <name>Task 2: Extend live result payloads and SSE events with the same truth fields</name>
  <files>benchmark_ui/runner.py, benchmark_ui/test_runner_events.py</files>
  <read_first>benchmark_ui/runner.py, benchmark_ui/test_runner_events.py, benchmark_ui/service.py, tools/apdr/src/lib.rs, .planning/phases/23-policy-truth-and-failure-semantics/23-RESEARCH.md</read_first>
  <action>In `benchmark_ui/runner.py`, read `requested_llm_validation_policy`, `llm_validation_route`, `docker_bypass_reason`, `docker_bypass_note`, and `debug_dir` from the APDR output metadata and copy them into the live `result` dict using the same exact camelCase keys introduced in the service layer. Extend the emitted `case_complete` event payload to include `requestedLlmValidationPolicy`, `llmValidationRoute`, `dockerBypassReason`, `dockerBypassNote`, and `debugDir` whenever they are present, while preserving the existing `validationPath`, `failureFamily`, and fallback fields. In `benchmark_ui/test_runner_events.py`, lock those exact event keys and ensure live rows preserve the same truth surface as saved rows.</action>
  <verify>python3 -m unittest benchmark_ui.test_runner_events</verify>
  <acceptance_criteria>
    - `benchmark_ui/runner.py` contains `requestedLlmValidationPolicy`
    - `benchmark_ui/runner.py` contains `llmValidationRoute`
    - `benchmark_ui/runner.py` contains `dockerBypassReason`
    - `benchmark_ui/runner.py` contains `dockerBypassNote`
    - `benchmark_ui/runner.py` contains `debugDir`
    - `benchmark_ui/test_runner_events.py` asserts those exact keys on `case_complete` payloads or live rows
    - `python3 -m unittest benchmark_ui.test_runner_events` exits 0
  </acceptance_criteria>
  <done>Live `llm` case inspection now uses the same policy-truth contract as saved-run inspection.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_runner_events`
- [ ] `rg -n 'requestedLlmValidationPolicy|llmValidationRoute|dockerBypassReason|dockerBypassNote|debugDir' benchmark_ui/service.py benchmark_ui/runner.py benchmark_ui/test_run_contract.py benchmark_ui/test_runner_events.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Saved and live `llm` case inspection share the same policy-truth field set
- Requested policy, route label, and bypass reason stay separate from `validationBackend` and `validationPath`

</success_criteria>

<output>
After completion, create `.planning/phases/23-policy-truth-and-failure-semantics/23-01-SUMMARY.md`
</output>
