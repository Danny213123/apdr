---
phase: 23-policy-truth-and-failure-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - benchmark_ui/service.py
  - web/src/main.js
  - tools/apdr/src/resolver/recovery_diagnostics.rs
  - tools/apdr/src/docker/builder/mod.rs
  - benchmark_ui/test_run_contract.py
  - benchmark_ui/test_runner_events.py
autonomous: true
requirements:
  - DFV-02
  - GDR-02
user_setup: []
must_haves:
  truths:
    - Expanded LLM case inspection must show requested policy, actual path, route label, bypass reason, and failure family together without raw-log scraping.
    - Docker-first host-runtime and framework/runtime blockers must remain `environment-specific`.
    - The UI change must stay additive and detail-oriented instead of redesigning benchmark tables or relabeling `validation_backend`.
  artifacts:
    - path: "web/src/main.js"
      provides: "Expanded case-detail truth panel for LLM cases"
      contains: "Validation truth"
    - path: "benchmark_ui/service.py"
      provides: "Derived display labels for Docker participation and debug pointers"
      contains: "dockerStatus"
    - path: "tools/apdr/src/resolver/recovery_diagnostics.rs"
      provides: "Classification guard for docker-first host-runtime and framework/runtime blockers"
      contains: "phase23_truth_"
    - path: "tools/apdr/src/docker/builder/mod.rs"
      provides: "Route-level regression coverage for docker-first policy archetypes"
      contains: "phase23_truth_"
  key_links:
    - from: "web/src/main.js"
      to: ".planning/REQUIREMENTS.md"
      via: "DFV-02 is satisfied only if reviewers can inspect a case in the UI without opening raw metadata files."
      pattern: "DFV-02"
    - from: "tools/apdr/src/resolver/recovery_diagnostics.rs"
      to: ".planning/phases/23-policy-truth-and-failure-semantics/23-RESEARCH.md"
      via: "Phase 23 must freeze environment-specific semantics under docker-first rather than assuming Phase 19 coverage is sufficient."
      pattern: "Failure-family classification exists, but docker-first regression coverage does not"
---

<objective>
Expose the new policy-truth fields in the UI and lock failure-family semantics so docker-first runtime blockers do not look like dependency misses.

Purpose: After Plan 01, the benchmark layer will finally carry the right fields, but reviewers still need a human-readable surface and a classifier guard. This plan adds an additive LLM case truth section in the UI and freezes the environment-specific family for host-runtime and framework/runtime blockers under docker-first.
Output: a UI inspection surface for policy/path/bypass truth, a lightweight Docker participation label, and Rust regression coverage for environment-specific semantics.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-policy-truth-and-failure-semantics/23-RESEARCH.md
@.planning/phases/23-policy-truth-and-failure-semantics/23-VALIDATION.md
@benchmark_ui/service.py
@benchmark_ui/test_run_contract.py
@benchmark_ui/test_runner_events.py
@web/src/main.js
@tools/apdr/src/docker/builder/agent_backend.rs
@tools/apdr/src/docker/builder/mod.rs
@tools/apdr/src/resolver/recovery_diagnostics.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an expanded LLM case truth section and Docker participation label in the UI</name>
  <files>benchmark_ui/service.py, web/src/main.js, benchmark_ui/test_run_contract.py, benchmark_ui/test_runner_events.py</files>
  <read_first>benchmark_ui/service.py, web/src/main.js, benchmark_ui/test_run_contract.py, benchmark_ui/test_runner_events.py, .planning/phases/23-policy-truth-and-failure-semantics/23-RESEARCH.md</read_first>
  <action>In `benchmark_ui/service.py`, derive an exact display field named `dockerStatus` from the existing route and path metadata, using exact labels `attempted`, `env-first control`, `host-runtime pre-skip`, and `bypassed`, and include it in case rows alongside the policy-truth fields from Plan 01. In `web/src/main.js`, extend the expanded LLM case detail view with a `Validation truth` section that renders `Requested policy`, `Validation path`, `LLM route`, `Docker status`, `Docker bypass`, `Failure family`, `Result origin`, `Debug dir`, and `Docker bypass note` when present. Keep the existing table columns intact; this truth surface belongs in the expanded detail panel and LLM-specific detail rendering, not in a broader UI redesign. Update Python tests if the service shape changes.</action>
  <verify>/bin/zsh -lc "python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_runner_events && npm run build --prefix web"</verify>
  <acceptance_criteria>
    - `benchmark_ui/service.py` contains `dockerStatus`
    - `web/src/main.js` contains `Validation truth`
    - `web/src/main.js` contains `Requested policy`
    - `web/src/main.js` contains `Docker status`
    - `web/src/main.js` contains `Docker bypass`
    - `web/src/main.js` contains `Failure family`
    - `web/src/main.js` contains `Debug dir`
    - `npm run build --prefix web` exits 0
  </acceptance_criteria>
  <done>Operators can now expand an LLM case and inspect Docker participation and failure-family truth without leaving the UI.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze environment-specific classification for docker-first runtime blockers</name>
  <files>tools/apdr/src/resolver/recovery_diagnostics.rs, tools/apdr/src/docker/builder/mod.rs</files>
  <read_first>tools/apdr/src/resolver/recovery_diagnostics.rs, tools/apdr/src/docker/builder/mod.rs, tools/apdr/src/docker/builder/agent_backend.rs, .planning/phases/23-policy-truth-and-failure-semantics/23-RESEARCH.md</read_first>
  <action>In `tools/apdr/src/resolver/recovery_diagnostics.rs`, tighten `classify_failure_family(...)` if needed so docker-first host-runtime pre-skips, framework-runtime markers, and Docker-unusable runtime failures remain `environment-specific` instead of falling back to `dependency-resolution`. Add Rust regression tests prefixed `phase23_truth_` across `recovery_diagnostics.rs` and `tools/apdr/src/docker/builder/mod.rs` that cover exact archetypes for `env-first-host-runtime`, a framework-runtime failure marker, and Docker daemon or CLI bypass under docker-first. Keep dependency-resolution classification for true package-resolution misses unchanged.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase23_truth_</verify>
  <acceptance_criteria>
    - `tools/apdr/src/resolver/recovery_diagnostics.rs` contains `phase23_truth_`
    - `tools/apdr/src/docker/builder/mod.rs` contains `phase23_truth_`
    - at least one `phase23_truth_` test asserts `environment-specific`
    - at least one `phase23_truth_` test asserts docker-first host-runtime or Docker-bypass behavior
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase23_truth_` exits 0
  </acceptance_criteria>
  <done>Docker-first runtime blockers now have an explicit regression contract keeping them out of generic dependency-resolution buckets.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml phase23_truth_`
- [ ] `python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_runner_events`
- [ ] `npm run build --prefix web`
- [ ] `rg -n 'Validation truth|Requested policy|Docker status|Docker bypass|Failure family|debugDir|phase23_truth_' benchmark_ui/service.py web/src/main.js tools/apdr/src/resolver/recovery_diagnostics.rs tools/apdr/src/docker/builder/mod.rs`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The UI can show per-case policy/path/bypass/family truth for LLM cases
- Docker-first host-runtime and framework/runtime blockers stay `environment-specific`

</success_criteria>

<output>
After completion, create `.planning/phases/23-policy-truth-and-failure-semantics/23-02-SUMMARY.md`
</output>
