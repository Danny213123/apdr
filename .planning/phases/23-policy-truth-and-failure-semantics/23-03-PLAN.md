---
phase: 23-policy-truth-and-failure-semantics
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - scripts/check_phase23_policy_truth.py
  - .planning/phases/23-policy-truth-and-failure-semantics/23-policy-truth-slice.json
  - .planning/phases/23-policy-truth-and-failure-semantics/23-POLICY-TRUTH-PROOF.md
autonomous: true
requirements:
  - DFV-02
  - GDR-02
user_setup: []
must_haves:
  truths:
    - Phase 23 must close on a deterministic proof contract, not on ad hoc screenshot review or raw log inspection.
    - The proof slice must cover both policy-truth visibility and failure-family truth together.
    - The locked archetypes must include real Docker attempt, env-first control, Docker bypass, host-runtime pre-skip, and framework/runtime environment-specific cases.
  artifacts:
    - path: "scripts/check_phase23_policy_truth.py"
      provides: "Deterministic checker for policy-truth and failure-family contract drift"
      contains: "--slice-json"
    - path: ".planning/phases/23-policy-truth-and-failure-semantics/23-policy-truth-slice.json"
      provides: "Frozen Phase 23 archetype slice"
      contains: "requested_policy"
    - path: ".planning/phases/23-policy-truth-and-failure-semantics/23-POLICY-TRUTH-PROOF.md"
      provides: "Reviewer-readable proof pack for Phase 23"
      contains: "environment-specific"
  key_links:
    - from: "scripts/check_phase23_policy_truth.py"
      to: ".planning/ROADMAP.md"
      via: "Phase 23 closes on inspectable policy truth and preserved failure semantics, not on the later env-first versus docker-first comparison harness."
      pattern: "actual validation path, bypass reasons, and failure-family truth inspectable end to end"
    - from: ".planning/phases/23-policy-truth-and-failure-semantics/23-policy-truth-slice.json"
      to: ".planning/phases/23-policy-truth-and-failure-semantics/23-RESEARCH.md"
      via: "The proof slice must freeze the exact archetypes called out in the research."
      pattern: "one Docker CLI bypass case, one Docker daemon-unavailable bypass case, one host-runtime pre-skip case, and one framework-runtime environment-specific case"
---

<objective>
Freeze a deterministic proof package that locks Phase 23's case-inspection and failure-family promises into a reusable contract.

Purpose: Phase 23 should not rely on humans re-reading raw metadata or re-running a benchmark to prove case truth. This plan creates a fixed archetype slice, a checker that fails on drift, and a reviewer-readable proof note so later phases can compare policies on top of a stable truth contract.
Output: a Phase 23 slice JSON, a deterministic checker, and a reviewer-facing proof document.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-policy-truth-and-failure-semantics/23-RESEARCH.md
@.planning/phases/23-policy-truth-and-failure-semantics/23-VALIDATION.md
@scripts/check_phase22_docker_policy.py
@.planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-slice.json
@.planning/phases/22-docker-first-policy-and-safe-degradation/22-DOCKER-POLICY-PROOF.md
@tools/apdr/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a fixed Phase 23 archetype slice and deterministic checker</name>
  <files>scripts/check_phase23_policy_truth.py, .planning/phases/23-policy-truth-and-failure-semantics/23-policy-truth-slice.json</files>
  <read_first>scripts/check_phase22_docker_policy.py, .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-slice.json, .planning/phases/23-policy-truth-and-failure-semantics/23-RESEARCH.md, .planning/phases/23-policy-truth-and-failure-semantics/23-VALIDATION.md</read_first>
  <action>Create `scripts/check_phase23_policy_truth.py` with exact flags `--slice-json`, `--status-json`, and `--probe-only`. The checker must validate a fixed JSON slice saved as `.planning/phases/23-policy-truth-and-failure-semantics/23-policy-truth-slice.json`, and that slice must include at least six archetypes with exact coverage for `docker-first` attempted, `env-first-control`, Docker CLI unavailable bypass, Docker daemon unavailable bypass, `env-first-host-runtime`, and a framework-runtime `environment-specific` case. The checker must fail if any case drifts on requested policy, validation path, route label, Docker bypass reason, failure family, or expected UI-facing truth keys such as `requestedLlmValidationPolicy`, `llmValidationRoute`, `dockerBypassReason`, and `debugDir`.</action>
  <verify>python3 scripts/check_phase23_policy_truth.py --slice-json .planning/phases/23-policy-truth-and-failure-semantics/23-policy-truth-slice.json --status-json .planning/phases/23-policy-truth-and-failure-semantics/23-policy-truth-proof-status.json --probe-only</verify>
  <acceptance_criteria>
    - `scripts/check_phase23_policy_truth.py` contains `--slice-json`
    - `scripts/check_phase23_policy_truth.py` contains `--status-json`
    - `.planning/phases/23-policy-truth-and-failure-semantics/23-policy-truth-slice.json` contains `requested_policy`
    - `.planning/phases/23-policy-truth-and-failure-semantics/23-policy-truth-slice.json` contains `failure_family`
    - `.planning/phases/23-policy-truth-and-failure-semantics/23-policy-truth-slice.json` contains `env-first-host-runtime`
    - `python3 scripts/check_phase23_policy_truth.py --slice-json .planning/phases/23-policy-truth-and-failure-semantics/23-policy-truth-slice.json --status-json .planning/phases/23-policy-truth-and-failure-semantics/23-policy-truth-proof-status.json --probe-only` exits 0
  </acceptance_criteria>
  <done>Phase 23 now has a deterministic artifact contract for case-level policy truth and failure-family truth.</done>
</task>

<task type="auto">
  <name>Task 2: Write the reviewer-facing proof note for the Phase 23 truth contract</name>
  <files>.planning/phases/23-policy-truth-and-failure-semantics/23-POLICY-TRUTH-PROOF.md</files>
  <read_first>.planning/phases/23-policy-truth-and-failure-semantics/23-policy-truth-slice.json, .planning/phases/23-policy-truth-and-failure-semantics/23-RESEARCH.md, .planning/phases/22-docker-first-policy-and-safe-degradation/22-DOCKER-POLICY-PROOF.md</read_first>
  <action>Create `.planning/phases/23-policy-truth-and-failure-semantics/23-POLICY-TRUTH-PROOF.md` that summarizes the locked archetypes, the exact truth keys reviewers should expect to see in saved or live LLM case inspection, and the `environment-specific` expectation for host-runtime and framework-runtime blockers. The proof note must clearly say that Phase 23 proves inspectability and failure-family truth only; it must not present itself as the later env-first versus docker-first comparison result.</action>
  <verify>python3 scripts/check_phase23_policy_truth.py --slice-json .planning/phases/23-policy-truth-and-failure-semantics/23-policy-truth-slice.json --status-json .planning/phases/23-policy-truth-and-failure-semantics/23-policy-truth-proof-status.json --probe-only</verify>
  <acceptance_criteria>
    - `.planning/phases/23-policy-truth-and-failure-semantics/23-POLICY-TRUTH-PROOF.md` contains `requested policy`
    - `.planning/phases/23-policy-truth-and-failure-semantics/23-POLICY-TRUTH-PROOF.md` contains `failure family`
    - `.planning/phases/23-policy-truth-and-failure-semantics/23-POLICY-TRUTH-PROOF.md` contains `environment-specific`
    - `.planning/phases/23-policy-truth-and-failure-semantics/23-POLICY-TRUTH-PROOF.md` contains `not the Phase 24 comparison harness`
  </acceptance_criteria>
  <done>Reviewers now have a stable proof note for the policy-truth contract that later phases can build on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 scripts/check_phase23_policy_truth.py --slice-json .planning/phases/23-policy-truth-and-failure-semantics/23-policy-truth-slice.json --status-json .planning/phases/23-policy-truth-and-failure-semantics/23-policy-truth-proof-status.json --probe-only`
- [ ] `rg -n 'slice-json|status-json|requested_policy|failure_family|environment-specific' scripts/check_phase23_policy_truth.py .planning/phases/23-policy-truth-and-failure-semantics/23-policy-truth-slice.json .planning/phases/23-policy-truth-and-failure-semantics/23-POLICY-TRUTH-PROOF.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Phase 23 closes on a deterministic proof contract instead of ad hoc inspection
- The proof package locks both policy-truth visibility and environment-specific failure semantics

</success_criteria>

<output>
After completion, create `.planning/phases/23-policy-truth-and-failure-semantics/23-03-SUMMARY.md`
</output>
