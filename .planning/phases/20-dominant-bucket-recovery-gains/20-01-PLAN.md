---
phase: 20-dominant-bucket-recovery-gains
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/apdr/data/recovery/module_rules.json
  - tools/apdr/src/resolver/retry_loop.rs
  - tools/apdr/src/resolver/recovery_diagnostics.rs
  - tools/apdr/tests/test_resolver.rs
autonomous: true
requirements:
  - VAL-03
user_setup: []
must_haves:
  truths:
    - Recoverable alias families in the dominant `module-not-found` bucket must resolve to concrete installable packages before the retry loop gives up.
    - Clearly non-PyPI or runtime-specific imports must stop with explicit non-generic outcomes instead of remaining in the dominant `module-not-found` bucket.
    - Phase 20 must reduce the dominant-bucket surface with bounded, data-backed rules rather than more opaque retry churn.
  artifacts:
    - path: "tools/apdr/data/recovery/module_rules.json"
      provides: "Expanded dominant-bucket module provider and stop-reason policy"
      contains: "mod-request-requests"
    - path: "tools/apdr/src/resolver/recovery_diagnostics.rs"
      provides: "Non-generic terminal status mapping for targeted module stop reasons"
      contains: "skipped-unsolvable"
    - path: "tools/apdr/tests/test_resolver.rs"
      provides: "Phase 20 module recovery regressions"
      contains: "phase20_module_"
  key_links:
    - from: "tools/apdr/data/recovery/module_rules.json"
      to: ".planning/phases/20-dominant-bucket-recovery-gains/20-RESEARCH.md"
      via: "Phase 20 research identified request, eyeD3, and Cython.Distutils as recoverable alias families and simplegui/canvas/mosquitto as non-PyPI/runtime-specific dominant-bucket imports."
      pattern: "request -> requests"
    - from: "tools/apdr/src/resolver/recovery_diagnostics.rs"
      to: ".planning/REQUIREMENTS.md"
      via: "VAL-03 requires reducing the dominant module-not-found bucket, not just rewording it."
      pattern: "APDR reduces failures in the `module-not-found`"
---

<objective>
Shrink the dominant `module-not-found` bucket with bounded provider recovery and explicit bucket exits.

Purpose: The March 30 baseline still leaves recoverable alias families and obviously non-PyPI/runtime-specific imports inside the same dominant bucket. This plan expands the data-backed module policy and makes targeted stop reasons leave the bucket instead of ending as generic mapping failures.
Output: expanded module policy, stop-reason-to-status mapping, and focused Rust regressions for Phase 20 module recovery.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-dominant-bucket-recovery-gains/20-RESEARCH.md
@.planning/phases/20-dominant-bucket-recovery-gains/20-VALIDATION.md
@runs/20260329-165524-apdr/cases/04ef258fa29e4e685287a30cf60462d0/resolution-report.txt
@tools/apdr/data/recovery/module_rules.json
@tools/apdr/src/resolver/retry_loop.rs
@tools/apdr/src/resolver/recovery_diagnostics.rs
@tools/apdr/tests/test_resolver.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand dominant-bucket provider aliases and targeted stop reasons</name>
  <files>tools/apdr/data/recovery/module_rules.json</files>
  <read_first>tools/apdr/data/recovery/module_rules.json, .planning/phases/20-dominant-bucket-recovery-gains/20-RESEARCH.md, runs/20260329-165524-apdr/cases/04ef258fa29e4e685287a30cf60462d0/resolution-report.txt</read_first>
  <action>In `tools/apdr/data/recovery/module_rules.json`, add provider rules with these exact ids and mappings: `mod-request-requests` for `request -> requests`, `mod-eyed3-eyed3` for `eyeD3 -> eyed3`, and `mod-cython-distutils` for `Cython.Distutils -> Cython`. Also add stop-reason rules with these exact ids and reason prefixes: `stop-simplegui-runtime` for `simplegui` with reason `host-runtime: CodeSkulptor simplegui runtime is not pip-installable`, `stop-canvas-runtime` for `canvas` with reason `host-runtime: canvas imports require a browser or GUI runtime rather than a PyPI package`, and `stop-mosquitto-runtime` for `mosquitto` with reason `system-runtime: mosquitto broker/runtime is not a pip-installable module`.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver phase20_module_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/data/recovery/module_rules.json` contains `mod-request-requests`
    - `tools/apdr/data/recovery/module_rules.json` contains `mod-eyed3-eyed3`
    - `tools/apdr/data/recovery/module_rules.json` contains `mod-cython-distutils`
    - `tools/apdr/data/recovery/module_rules.json` contains `stop-simplegui-runtime`
    - `tools/apdr/data/recovery/module_rules.json` contains `stop-canvas-runtime`
    - `tools/apdr/data/recovery/module_rules.json` contains `stop-mosquitto-runtime`
  </acceptance_criteria>
  <done>Phase 20 has bounded deterministic data for both recoverable alias families and non-generic dominant-bucket exits.</done>
</task>

<task type="auto">
  <name>Task 2: Map targeted stop reasons out of the dominant bucket and lock module recovery tests</name>
  <files>tools/apdr/src/resolver/retry_loop.rs, tools/apdr/src/resolver/recovery_diagnostics.rs, tools/apdr/tests/test_resolver.rs</files>
  <read_first>tools/apdr/src/resolver/retry_loop.rs, tools/apdr/src/resolver/recovery_diagnostics.rs, tools/apdr/tests/test_resolver.rs, tools/apdr/data/recovery/module_rules.json, .planning/phases/20-dominant-bucket-recovery-gains/20-RESEARCH.md</read_first>
  <action>Update the retry-loop and recovery-diagnostics path so targeted stop reasons beginning with `host-runtime:` end as `validation_status: skipped-host-runtime`, while targeted stop reasons beginning with `project-local:` or `removed-runtime:` end as `validation_status: skipped-unsolvable` and no longer keep `failure_bucket: module-not-found`. Then add Phase 20 Rust tests in `tools/apdr/tests/test_resolver.rs` named `phase20_module_request_alias_recovers_requests`, `phase20_module_simplegui_leaves_module_not_found_bucket`, and `phase20_module_removed_runtime_becomes_skipped_unsolvable`.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver phase20_module_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/resolver/recovery_diagnostics.rs` contains `skipped-unsolvable`
    - `tools/apdr/src/resolver/recovery_diagnostics.rs` contains `skipped-host-runtime`
    - `tools/apdr/tests/test_resolver.rs` contains `phase20_module_request_alias_recovers_requests`
    - `tools/apdr/tests/test_resolver.rs` contains `phase20_module_simplegui_leaves_module_not_found_bucket`
    - `tools/apdr/tests/test_resolver.rs` contains `phase20_module_removed_runtime_becomes_skipped_unsolvable`
    - `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver phase20_module_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>Recoverable module aliases can pass, and non-recoverable targeted imports stop outside the dominant `module-not-found` bucket.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver phase20_module_ -- --nocapture`
- [ ] `rg -n 'mod-request-requests|mod-eyed3-eyed3|mod-cython-distutils|stop-simplegui-runtime|stop-canvas-runtime|stop-mosquitto-runtime|phase20_module_' tools/apdr/data/recovery/module_rules.json tools/apdr/src/resolver/recovery_diagnostics.rs tools/apdr/tests/test_resolver.rs`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Dominant-bucket module aliases recover deterministically
- Non-PyPI/runtime-specific imports leave the generic `module-not-found` bucket

</success_criteria>

<output>
After completion, create `.planning/phases/20-dominant-bucket-recovery-gains/20-01-SUMMARY.md`
</output>
