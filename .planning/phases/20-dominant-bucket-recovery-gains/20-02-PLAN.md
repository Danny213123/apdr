---
phase: 20-dominant-bucket-recovery-gains
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - tools/apdr/data/recovery/compatibility_rules.json
  - tools/apdr/src/resolver/targeted_recovery.rs
  - tools/apdr/src/resolver/retry_loop.rs
  - tools/apdr/tests/test_resolver.rs
autonomous: true
requirements:
  - AGT-09
  - VAL-03
user_setup: []
must_haves:
  truths:
    - Legacy version/build failures on the selected slice must have deterministic replacement and interpreter-floor guidance rather than oscillating across impossible pins.
    - Compatibility policy must be expressive enough to represent replacement packages and Python floors for recurring dominant-bucket families.
    - The retry loop must apply those rules before repeating the same `version-not-found` or `environment-build-failed` churn.
  artifacts:
    - path: "tools/apdr/data/recovery/compatibility_rules.json"
      provides: "Phase 20 compatibility policy for replacements, floors, and dominant build/version families"
      contains: "compat-beautifulsoup-rename"
    - path: "tools/apdr/src/resolver/targeted_recovery.rs"
      provides: "Compatibility schema that can express replacement packages and Python floors"
      contains: "replacement_packages"
    - path: "tools/apdr/tests/test_resolver.rs"
      provides: "Phase 20 compatibility regressions"
      contains: "phase20_compat_"
  key_links:
    - from: "tools/apdr/data/recovery/compatibility_rules.json"
      to: "runs/20260329-165524-apdr/cases/10295174/resolution-report.txt"
      via: "Phase 20 research found that a PyMC3-family case dropped to Python 2.7 and then failed on numpy==1.21.6, which requires interpreter-floor protection."
      pattern: "Package `numpy==1.21.6` is unavailable for the selected Python version."
    - from: "tools/apdr/src/resolver/retry_loop.rs"
      to: "runs/20260329-165524-apdr/cases/056626de3fbdc7cf7b59de1d9f6279d1/resolution-report.txt"
      via: "The dominant build-failure path currently oscillates through opencv/numpy wheel choices instead of converging."
      pattern: "Repeated failure signature `BuildFailure|TPL-OS||numpy`"
---

<objective>
Stabilize legacy package replacement and interpreter selection for dominant version/build failures.

Purpose: Several high-yield Phase 20 cases are not missing a package name so much as using legacy names, incompatible wheel pins, or the wrong Python floor after metadata gaps. This plan extends the compatibility policy and the retry loop so those families converge on recoverable requirements instead of oscillating or falling back to impossible versions.
Output: replacement-aware compatibility rules, interpreter-floor support, and Rust regressions for the dominant version/build families.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-dominant-bucket-recovery-gains/20-RESEARCH.md
@.planning/phases/20-dominant-bucket-recovery-gains/20-VALIDATION.md
@runs/20260329-165524-apdr/cases/10295174/resolution-report.txt
@runs/20260329-165524-apdr/cases/056626de3fbdc7cf7b59de1d9f6279d1/resolution-report.txt
@tools/apdr/data/recovery/compatibility_rules.json
@tools/apdr/src/resolver/targeted_recovery.rs
@tools/apdr/src/resolver/retry_loop.rs
@tools/apdr/tests/test_resolver.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend compatibility policy with replacements and interpreter floors</name>
  <files>tools/apdr/data/recovery/compatibility_rules.json, tools/apdr/src/resolver/targeted_recovery.rs</files>
  <read_first>tools/apdr/data/recovery/compatibility_rules.json, tools/apdr/src/resolver/targeted_recovery.rs, .planning/phases/20-dominant-bucket-recovery-gains/20-RESEARCH.md, runs/20260329-165524-apdr/cases/10295174/resolution-report.txt</read_first>
  <action>Extend the compatibility schema in `tools/apdr/src/resolver/targeted_recovery.rs` so clusters can express `replacement_packages` and `python_floor` in addition to the existing preferred versions and ceilings. Then update `tools/apdr/data/recovery/compatibility_rules.json` with these exact rule ids and target values: `compat-beautifulsoup-rename` replacing `BeautifulSoup` with `beautifulsoup4==4.12.3`, `compat-mysql-python-rename` replacing `MySQL-python` with `mysqlclient==2.2.4`, `compat-opencv-headless-legacy` pinning `opencv-python-headless==3.4.11.43` with `numpy==1.24.4`, and `compat-pymc3-floor` keeping the legacy PyMC3 cluster on `python_floor: 3.9` with `python_ceiling: 3.11`.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver phase20_compat_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/resolver/targeted_recovery.rs` contains `replacement_packages`
    - `tools/apdr/src/resolver/targeted_recovery.rs` contains `python_floor`
    - `tools/apdr/data/recovery/compatibility_rules.json` contains `compat-beautifulsoup-rename`
    - `tools/apdr/data/recovery/compatibility_rules.json` contains `compat-mysql-python-rename`
    - `tools/apdr/data/recovery/compatibility_rules.json` contains `compat-opencv-headless-legacy`
    - `tools/apdr/data/recovery/compatibility_rules.json` contains `compat-pymc3-floor`
  </acceptance_criteria>
  <done>Phase 20 compatibility data can now express the legacy replacement and interpreter-floor logic needed by the dominant version/build families.</done>
</task>

<task type="auto">
  <name>Task 2: Apply replacement and floor rules before dominant version/build loops repeat</name>
  <files>tools/apdr/src/resolver/retry_loop.rs, tools/apdr/tests/test_resolver.rs</files>
  <read_first>tools/apdr/src/resolver/retry_loop.rs, tools/apdr/tests/test_resolver.rs, tools/apdr/data/recovery/compatibility_rules.json, tools/apdr/src/resolver/targeted_recovery.rs, .planning/phases/20-dominant-bucket-recovery-gains/20-RESEARCH.md</read_first>
  <action>Update `tools/apdr/src/resolver/retry_loop.rs` so `VersionNotFound`, contradictory-pin, and repeated `BuildFailure|TPL-OS` paths consult the new `replacement_packages` and `python_floor` fields before stripping all pins or falling back to older interpreters. The concrete target behaviors are: `BeautifulSoup` rewrites to `beautifulsoup4`, `MySQL-python` rewrites to `mysqlclient`, the PyMC3-family path cannot fall below Python 3.9 when family knowledge has already selected a modern Python, and the opencv/dlib build family converges on `opencv-python-headless==3.4.11.43` plus `numpy==1.24.4` instead of retrying `numpy==1.21.6`, `numpy==1.25.2`, and other conflicting wheel pins indefinitely. Add Rust tests named `phase20_compat_beautifulsoup_rewrites_to_bs4`, `phase20_compat_pymc3_keeps_python_floor`, and `phase20_compat_opencv_cluster_converges`.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver phase20_compat_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/resolver/retry_loop.rs` contains `replacement_packages`
    - `tools/apdr/src/resolver/retry_loop.rs` contains `python_floor`
    - `tools/apdr/tests/test_resolver.rs` contains `phase20_compat_beautifulsoup_rewrites_to_bs4`
    - `tools/apdr/tests/test_resolver.rs` contains `phase20_compat_pymc3_keeps_python_floor`
    - `tools/apdr/tests/test_resolver.rs` contains `phase20_compat_opencv_cluster_converges`
    - `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver phase20_compat_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>Dominant version/build families now converge on bounded replacement and compatibility choices instead of repeating the same unrecoverable loops.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver phase20_compat_ -- --nocapture`
- [ ] `rg -n 'replacement_packages|python_floor|compat-beautifulsoup-rename|compat-mysql-python-rename|compat-opencv-headless-legacy|compat-pymc3-floor|phase20_compat_' tools/apdr/data/recovery/compatibility_rules.json tools/apdr/src/resolver/targeted_recovery.rs tools/apdr/src/resolver/retry_loop.rs tools/apdr/tests/test_resolver.rs`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Legacy replacement and interpreter-floor logic reduce dominant version/build churn
- Recoverable version/build families converge on bounded requirements instead of repeating impossible pins

</success_criteria>

<output>
After completion, create `.planning/phases/20-dominant-bucket-recovery-gains/20-02-SUMMARY.md`
</output>
