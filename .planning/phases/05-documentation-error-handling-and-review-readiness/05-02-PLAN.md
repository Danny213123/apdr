---
phase: 05-documentation-error-handling-and-review-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - tools/apdr/src/resolver/tier3_llm/process.rs
  - tools/apdr/src/docker/builder/env_backend.rs
  - tools/apdr/src/resolver/family_knowledge/learned.rs
  - tools/apdr/src/resolver/pre_solve.rs
  - tools/apdr/src/resolver/kgraph_db.rs
autonomous: true
requirements:
  - QUAL-02
  - QUAL-05
user_setup: []
must_haves:
  truths:
    - Runtime-facing LLM subprocess setup no longer panics when Python, pipes, or child process startup fail.
    - Env-validation backend escalation logic no longer depends on `summary.attempts.last().unwrap()` and preserves the existing fallback ordering.
    - Any remaining panic sites in touched production code are narrow, documented invariants rather than host- or environment-facing failures.
  artifacts:
    - path: "tools/apdr/src/resolver/tier3_llm/process.rs"
      provides: "Graceful Tier 3 LLM unavailability path instead of panic-on-spawn"
      contains: "call_python"
    - path: "tools/apdr/src/docker/builder/env_backend.rs"
      provides: "Guarded backend-escalation checks without `summary.attempts.last().unwrap()`"
      contains: "env_attempt_requires_backend_escalation"
    - path: "tools/apdr/src/resolver/family_knowledge/learned.rs"
      provides: "Small runtime-safety cleanup inside the family-knowledge surface"
      contains: "get_learned_alternatives"
  key_links:
    - from: "tools/apdr/src/resolver/tier3_llm/process.rs"
      to: "tools/apdr/src/resolver/tier3_llm/mod.rs"
      via: "Tier 3 subprocess failure should degrade to LLM-unavailable behavior without changing the public facade entrypoints"
      pattern: "call_python"
    - from: "tools/apdr/src/docker/builder/env_backend.rs"
      to: "tools/apdr/src/docker/builder/mod.rs"
      via: "env backend escalation should stay explicit and preserve env-to-Docker retry ordering"
      pattern: "validate_requirements"
---

<objective>
Remove the runtime-facing panic paths inside the touched modernization surfaces while preserving the existing fallback behavior reviewers now need to understand.

Purpose: Phase 5 is supposed to harden review-ready Rust behavior, but `tier3_llm/process.rs` and `env_backend.rs` still contain avoidable runtime-facing panics and assumption-heavy fallback checks.
Output: Graceful LLM-process failure handling, guarded env-backend escalation, and documented or tightened narrow invariants in the touched production surfaces.
</objective>

<execution_context>
@C:\Users\danny\.codex\get-shit-done\workflows\execute-plan.md
@C:\Users\danny\.codex\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\apdr\.planning\PROJECT.md
@D:\apdr\.planning\ROADMAP.md
@D:\apdr\.planning\STATE.md
@D:\apdr\.planning\phases\05-documentation-error-handling-and-review-readiness\05-CONTEXT.md
@D:\apdr\.planning\phases\05-documentation-error-handling-and-review-readiness\05-RESEARCH.md
@D:\apdr\.planning\phases\05-documentation-error-handling-and-review-readiness\05-VALIDATION.md
@D:\apdr\tools\apdr\src\resolver\tier3_llm\process.rs
@D:\apdr\tools\apdr\src\docker\builder\env_backend.rs
@D:\apdr\tools\apdr\src\resolver\family_knowledge\learned.rs
@D:\apdr\tools\apdr\src\resolver\pre_solve.rs
@D:\apdr\tools\apdr\src\resolver\kgraph_db.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Tier 3 subprocess startup panics with graceful LLM-unavailable behavior</name>
  <files>tools/apdr/src/resolver/tier3_llm/process.rs</files>
  <read_first>tools/apdr/src/resolver/tier3_llm/process.rs, tools/apdr/src/resolver/tier3_llm/mod.rs, .planning/phases/05-documentation-error-handling-and-review-readiness/05-CONTEXT.md</read_first>
  <action>In `tools/apdr/src/resolver/tier3_llm/process.rs`, remove the runtime panics from `spawn_python_process()`. Change the subprocess bootstrap so child spawn failure, missing stdin, or missing stdout no longer call `panic!` or `expect(...)`. The target behavior is: log a warning with `eprintln!`, treat the Tier 3 service as unavailable, and let `call_python(...)` return `None` so the existing fallback behavior in the resolver facade can continue. If needed, change the process cache shape from `OnceLock<Mutex<LlmProcess>>` to a fallible cached form such as `OnceLock<Option<Mutex<LlmProcess>>>`, but keep the public helper name `call_python(...)` unchanged.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/resolver/tier3_llm/process.rs` does not contain `panic!(\"Failed to spawn Python LLM service:`
    - `tools/apdr/src/resolver/tier3_llm/process.rs` does not contain `expect(\"Failed to open stdin\")`
    - `tools/apdr/src/resolver/tier3_llm/process.rs` does not contain `expect(\"Failed to open stdout\")`
    - `tools/apdr/src/resolver/tier3_llm/process.rs` contains `call_python`
    - `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>Tier 3 subprocess initialization no longer aborts the process on host-state failures.</done>
</task>

<task type="auto">
  <name>Task 2: Remove assumption-heavy env-backend escalation unwraps without changing fallback ordering</name>
  <files>tools/apdr/src/docker/builder/env_backend.rs</files>
  <read_first>tools/apdr/src/docker/builder/env_backend.rs, tools/apdr/src/docker/builder/mod.rs, tools/apdr/src/docker/builder/docker_backend.rs, .planning/phases/03-validation-pipeline-throughput/03-01-SUMMARY.md</read_first>
  <action>In `tools/apdr/src/docker/builder/env_backend.rs`, replace all four `summary.attempts.last().unwrap()` escalation checks with explicit guarded logic. Introduce one local helper or repeated `if let Some(last_attempt) = summary.attempts.last()` checks so backend escalation only runs when an attempt exists. Preserve the current break and continue behavior, preserve env-to-Docker retry ordering, and do not change the public `validate_requirements(...)` or `validate_requirements_env(...)` signatures.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml validation_pipeline_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/docker/builder/env_backend.rs` does not contain `summary.attempts.last().unwrap()`
    - `tools/apdr/src/docker/builder/env_backend.rs` contains `env_attempt_requires_backend_escalation`
    - `cargo test --manifest-path tools/apdr/Cargo.toml validation_pipeline_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>The env backend no longer assumes attempt history exists before checking escalation behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Tighten or document narrow internal invariants in the touched production surfaces</name>
  <files>tools/apdr/src/resolver/family_knowledge/learned.rs, tools/apdr/src/resolver/pre_solve.rs, tools/apdr/src/resolver/kgraph_db.rs</files>
  <read_first>tools/apdr/src/resolver/family_knowledge/learned.rs, tools/apdr/src/resolver/pre_solve.rs, tools/apdr/src/resolver/kgraph_db.rs, .planning/phases/05-documentation-error-handling-and-review-readiness/05-RESEARCH.md</read_first>
  <action>Audit the narrow invariant-style panic sites in `tools/apdr/src/resolver/family_knowledge/learned.rs`, `tools/apdr/src/resolver/pre_solve.rs`, and `tools/apdr/src/resolver/kgraph_db.rs`. Replace the trivial runtime-safe unwrap in `get_learned_alternatives(...)` with the explicit `let Some(learned) = learned else { return Vec::new(); };` form. For `pre_solve.rs` and `kgraph_db.rs`, either make the local refactor if it stays small and does not widen signatures, or add a precise comment that explains the invariant in reviewer terms right above the remaining site. Do not widen this task into a repo-wide panic cleanup.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture && cargo test --manifest-path tools/apdr/Cargo.toml validation_pipeline_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/resolver/family_knowledge/learned.rs` does not contain `let learned = learned.unwrap();`
    - `tools/apdr/src/resolver/pre_solve.rs` contains either `match self.undo_stack.pop()` or a reviewer-facing comment explaining the checkpoint invariant
    - `tools/apdr/src/resolver/kgraph_db.rs` contains either `expect(` with an invariant message or a reviewer-facing comment explaining the pooled-connection invariant
    - `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture` exits 0
    - `cargo test --manifest-path tools/apdr/Cargo.toml validation_pipeline_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>The remaining touched panic sites are either removed or clearly narrowed to documented internal invariants.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n 'unwrap\\(|expect\\(' tools/apdr/src/resolver tools/apdr/src/docker/builder`
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture`
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml validation_pipeline_ -- --nocapture`
- [ ] `cargo clippy --manifest-path tools/apdr/Cargo.toml --all-targets -- -D warnings`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Runtime-facing panic paths in the touched modernization surfaces are removed
- Existing fallback behavior stays intact and reviewable
- Any remaining panic site in touched production code is a documented narrow invariant
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-error-handling-and-review-readiness/05-02-SUMMARY.md`
</output>
