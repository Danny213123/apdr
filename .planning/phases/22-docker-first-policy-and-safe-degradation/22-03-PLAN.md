---
phase: 22-docker-first-policy-and-safe-degradation
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - tools/apdr/src/lib.rs
  - tools/apdr/src/docker/builder/agent_backend.rs
  - tools/apdr/src/docker/builder/docker_backend.rs
  - scripts/check_phase22_docker_policy.py
  - .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-slice.json
  - .planning/phases/22-docker-first-policy-and-safe-degradation/22-DOCKER-POLICY-PROOF.md
autonomous: true
requirements:
  - DFV-01
  - GDR-01
user_setup: []
must_haves:
  truths:
    - Every `llm` case must leave Docker-oriented debug materials or an explicit Docker-bypass note in its debug folder.
    - Docker-first degradation to env must be machine-readable so reviewers can tell Docker was requested but not honored.
    - Phase 22 must close with a deterministic proof contract covering docker-first, env-first control, Docker-bypass, and host-runtime pre-skip behavior.
  artifacts:
    - path: "tools/apdr/src/docker/builder/docker_backend.rs"
      provides: "Deterministic Docker attempt artifact layout reused by docker-first `llm` runs"
      contains: "docker-build.command.txt"
    - path: "tools/apdr/src/docker/builder/agent_backend.rs"
      provides: "Docker-bypass note or metadata writing for non-Docker `llm` paths"
      contains: "bypass"
    - path: "scripts/check_phase22_docker_policy.py"
      provides: "Deterministic checker for Phase 22 routing, degradation, and debug-artifact contract"
      contains: "--slice-json"
    - path: ".planning/phases/22-docker-first-policy-and-safe-degradation/22-DOCKER-POLICY-PROOF.md"
      provides: "Reviewer-readable proof package for the Phase 22 policy contract"
      contains: "docker-first"
  key_links:
    - from: "tools/apdr/src/docker/builder/agent_backend.rs"
      to: ".planning/phases/22-docker-first-policy-and-safe-degradation/22-CONTEXT.md"
      via: "Phase 22 requires every `llm` case to leave Docker materials or a bypass note in the debug folder."
      pattern: "Every `llm` case should leave Docker-oriented materials in its debug folder"
    - from: "scripts/check_phase22_docker_policy.py"
      to: ".planning/ROADMAP.md"
      via: "Phase 22 closes on policy and degradation proof, not on the later env-first versus docker-first comparison harness."
      pattern: "safe fallback when Docker is unavailable or unsupported"
---

<objective>
Guarantee per-case Docker debug context for `llm` runs and lock a deterministic proof around the Phase 22 policy contract.

Purpose: Phase 22 is not complete unless reviewers can see Docker attempt or bypass context in each `llm` case without scraping raw logs. This plan turns that requirement into a stable artifact contract and freezes a deterministic checker so later phases can compare policies without re-arguing what Phase 22 promised.
Output: `llm` case debug folders with Docker attempt or bypass artifacts, a fixed policy proof slice, and a deterministic Phase 22 checker.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-docker-first-policy-and-safe-degradation/22-CONTEXT.md
@.planning/phases/22-docker-first-policy-and-safe-degradation/22-RESEARCH.md
@.planning/phases/22-docker-first-policy-and-safe-degradation/22-VALIDATION.md
@tools/apdr/src/lib.rs
@tools/apdr/src/docker/builder/agent_backend.rs
@tools/apdr/src/docker/builder/docker_backend.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guarantee Docker attempt artifacts or explicit bypass notes for every `llm` case</name>
  <files>tools/apdr/src/lib.rs, tools/apdr/src/docker/builder/agent_backend.rs, tools/apdr/src/docker/builder/docker_backend.rs</files>
  <read_first>tools/apdr/src/lib.rs, tools/apdr/src/docker/builder/agent_backend.rs, tools/apdr/src/docker/builder/docker_backend.rs, .planning/phases/22-docker-first-policy-and-safe-degradation/22-CONTEXT.md</read_first>
  <action>Reuse the existing deterministic Docker artifact layout in `tools/apdr/src/docker/builder/docker_backend.rs` for actual docker-first `llm` attempts so the case debug folder contains the generated Dockerfile, Docker build command, Docker run command, and Docker logs. In `tools/apdr/src/docker/builder/agent_backend.rs`, add an explicit bypass artifact or metadata note for `llm` cases where Docker is skipped, unavailable, unsupported, or pre-skipped, and ensure the top-level validation/debug metadata records the bypass reason in a machine-readable way. Keep env-first control cases explicit rather than letting them look like accidental Docker failures.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase22_policy_</verify>
  <acceptance_criteria>
    - actual Docker-attempt `llm` cases leave `Dockerfile`, `docker-build.command.txt`, `docker-run.command.txt`, and Docker logs under the case debug area
    - Docker-bypassed `llm` cases leave an explicit bypass artifact or note instead of a silent gap
    - the bypass reason is machine-readable in top-level metadata or the bypass artifact
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase22_policy_` exits 0
  </acceptance_criteria>
  <done>`llm` case inspection no longer depends on reconstructing Docker participation from raw logs alone.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze a deterministic Phase 22 policy proof contract</name>
  <files>scripts/check_phase22_docker_policy.py, .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-slice.json, .planning/phases/22-docker-first-policy-and-safe-degradation/22-DOCKER-POLICY-PROOF.md</files>
  <read_first>scripts/check_phase20_recovery_delta.py, scripts/check_phase21_1_footprint.py, .planning/phases/22-docker-first-policy-and-safe-degradation/22-RESEARCH.md, .planning/phases/22-docker-first-policy-and-safe-degradation/22-VALIDATION.md</read_first>
  <action>Create `scripts/check_phase22_docker_policy.py` with exact flags `--slice-json`, `--status-json`, and `--probe-only`. The checker must validate a fixed JSON slice covering at least one docker-first case, one env-first control case, one Docker-bypass fallback case, and one host-runtime pre-skip case, and it must fail if requested policy, first hop, bypass reason, or expected debug-artifact presence drifts from the saved contract. Capture that contract in `22-docker-policy-slice.json`, then summarize the slice and expectations in `22-DOCKER-POLICY-PROOF.md`.</action>
  <verify>python3 scripts/check_phase22_docker_policy.py --slice-json .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-slice.json --status-json .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-proof-status.json --probe-only</verify>
  <acceptance_criteria>
    - `scripts/check_phase22_docker_policy.py` contains `--slice-json`
    - `scripts/check_phase22_docker_policy.py` contains `--status-json`
    - `.planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-slice.json` covers docker-first, env-first, bypass, and pre-skip cases
    - `python3 scripts/check_phase22_docker_policy.py --slice-json .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-slice.json --status-json .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-proof-status.json --probe-only` exits 0
  </acceptance_criteria>
  <done>Phase 22 now has a deterministic proof contract for its routing, degradation, and debug-artifact promises.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml phase22_policy_`
- [ ] `python3 scripts/check_phase22_docker_policy.py --slice-json .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-slice.json --status-json .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-proof-status.json --probe-only`
- [ ] `rg -n 'Dockerfile|docker-build.command.txt|docker-run.command.txt|bypass|slice-json|status-json' tools/apdr/src/docker/builder/agent_backend.rs tools/apdr/src/docker/builder/docker_backend.rs scripts/check_phase22_docker_policy.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Every `llm` case leaves Docker attempt artifacts or an explicit bypass note
- Phase 22 closes with a deterministic proof for docker-first policy behavior and safe degradation

</success_criteria>

<output>
After completion, create `.planning/phases/22-docker-first-policy-and-safe-degradation/22-03-SUMMARY.md`
</output>
