---
phase: 22-docker-first-policy-and-safe-degradation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/apdr/src/lib.rs
  - tools/apdr/src/main.rs
  - tools/apdr/src/docker/builder/agent_backend.rs
  - tools/apdr/src/docker/builder/mod.rs
  - tools/apdr/test_executor.py
autonomous: true
requirements:
  - DFV-01
  - DFV-03
user_setup: []
must_haves:
  truths:
    - Docker-first must become the default first-hop policy inside `validation_backend=llm`, not a new backend name.
    - Env-first must remain explicitly selectable as a comparison control while docker-first is being evaluated.
    - Host-runtime and clearly unsuitable cases must stay ahead of Docker instead of being forced through the docker-first route.
  artifacts:
    - path: "tools/apdr/src/lib.rs"
      provides: "Normalized `llm` validation policy config that stays separate from `validation_backend`"
      contains: "llm_validation_policy"
    - path: "tools/apdr/src/main.rs"
      provides: "CLI surface for selecting docker-first versus env-first within `llm` mode"
      contains: "--llm-validation-policy"
    - path: "tools/apdr/src/docker/builder/agent_backend.rs"
      provides: "Docker-first `llm` route with explicit env-first control branch"
      contains: "docker-first"
    - path: "tools/apdr/src/docker/builder/mod.rs"
      provides: "Phase 22 Rust routing and policy regression tests"
      contains: "phase22_policy_"
  key_links:
    - from: "tools/apdr/src/docker/builder/agent_backend.rs"
      to: ".planning/phases/22-docker-first-policy-and-safe-degradation/22-CONTEXT.md"
      via: "Phase 22 locks docker-first as the standard `llm` route while preserving env-first control."
      pattern: "docker-first becomes the standard `llm` route now"
    - from: "tools/apdr/src/main.rs"
      to: ".planning/REQUIREMENTS.md"
      via: "DFV-03 requires env-first to remain explicitly selectable rather than hidden behind code changes."
      pattern: "DFV-03"
---

<objective>
Create the core docker-first `llm` routing contract and explicit policy surface without widening the public backend model.

Purpose: Phase 18 already inserted a targeted Docker middle hop, but the benchmark operator still cannot choose docker-first versus env-first within `llm`, and the route still defaults to env-first. This plan adds the narrow policy seam, flips the default first hop, and locks the route order with Rust tests so the rest of Phase 22 can build on a stable core.
Output: a normalized `llm` policy field, CLI and wrapper pass-through, docker-first default routing, explicit env-first control, and Phase 22 Rust policy tests.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-docker-first-policy-and-safe-degradation/22-CONTEXT.md
@.planning/phases/22-docker-first-policy-and-safe-degradation/22-RESEARCH.md
@.planning/phases/22-docker-first-policy-and-safe-degradation/22-VALIDATION.md
@tools/apdr/src/lib.rs
@tools/apdr/src/main.rs
@tools/apdr/src/docker/builder/mod.rs
@tools/apdr/src/docker/builder/agent_backend.rs
@tools/apdr/src/docker/builder/docker_backend.rs
@tools/apdr/test_executor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit `llm` policy config and wrapper pass-through</name>
  <files>tools/apdr/src/lib.rs, tools/apdr/src/main.rs, tools/apdr/test_executor.py</files>
  <read_first>tools/apdr/src/lib.rs, tools/apdr/src/main.rs, tools/apdr/test_executor.py, .planning/phases/22-docker-first-policy-and-safe-degradation/22-CONTEXT.md</read_first>
  <action>In `tools/apdr/src/lib.rs`, add a narrow normalized field named `llm_validation_policy` or equivalent on `ResolveConfig`, with exact normalized values `docker-first` and `env-first`, and default it to `docker-first` when `validation_backend=llm` and no override is provided. In `tools/apdr/src/main.rs`, expose an exact CLI flag `--llm-validation-policy` that accepts those values without changing the existing `--validation-backend` contract. In `tools/apdr/test_executor.py`, pass the selected policy through to the Rust CLI and update the help text so `llm` no longer claims to be env-first by definition.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase22_policy_</verify>
  <acceptance_criteria>
    - `tools/apdr/src/lib.rs` contains `llm_validation_policy`
    - `tools/apdr/src/main.rs` contains `--llm-validation-policy`
    - `tools/apdr/test_executor.py` passes `--llm-validation-policy`
    - `validation_backend` remains `llm` for `llm` runs instead of turning into a new backend value
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase22_policy_` exits 0
  </acceptance_criteria>
  <done>The repo can now express docker-first versus env-first inside `llm` mode without changing the public backend name.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the `llm` first hop to docker-first and lock the policy boundaries</name>
  <files>tools/apdr/src/docker/builder/agent_backend.rs, tools/apdr/src/docker/builder/mod.rs</files>
  <read_first>tools/apdr/src/docker/builder/agent_backend.rs, tools/apdr/src/docker/builder/mod.rs, tools/apdr/src/docker/builder/docker_backend.rs, .planning/phases/22-docker-first-policy-and-safe-degradation/22-RESEARCH.md</read_first>
  <action>In `tools/apdr/src/docker/builder/agent_backend.rs`, branch the `llm` route on the selected policy so docker-first is the default first hop for eligible cases, env-first remains an explicit control path, and host-runtime or clearly unsuitable pre-skips still happen before Docker is attempted. Keep the final agent fallback after the selected first hop and any safe degradation. In `tools/apdr/src/docker/builder/mod.rs`, add Rust tests prefixed `phase22_policy_`, including exact coverage for docker-first default, env-first control preservation, host-runtime pre-skip before Docker, and Docker-bypass fallback to env.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase22_policy_</verify>
  <acceptance_criteria>
    - `tools/apdr/src/docker/builder/mod.rs` contains `phase22_policy_llm_defaults_to_docker_first`
    - `tools/apdr/src/docker/builder/mod.rs` contains `phase22_policy_llm_env_first_control_preserved`
    - `tools/apdr/src/docker/builder/mod.rs` contains `phase22_policy_host_runtime_skips_before_docker`
    - `tools/apdr/src/docker/builder/mod.rs` contains `phase22_policy_docker_bypass_falls_back_to_env`
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase22_policy_` exits 0
  </acceptance_criteria>
  <done>`llm` routing now defaults to Docker first, keeps env-first as a real control path, and preserves the locked pre-skip boundaries.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml phase22_policy_`
- [ ] `rg -n 'llm_validation_policy|--llm-validation-policy|phase22_policy_' tools/apdr/src/lib.rs tools/apdr/src/main.rs tools/apdr/src/docker/builder/agent_backend.rs tools/apdr/src/docker/builder/mod.rs tools/apdr/test_executor.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Docker-first is the default `llm` policy while env-first remains explicitly selectable
- The `llm` route changes do not invent a new backend name or force Docker onto pre-skipped host-runtime cases

</success_criteria>

<output>
After completion, create `.planning/phases/22-docker-first-policy-and-safe-degradation/22-01-SUMMARY.md`
</output>
