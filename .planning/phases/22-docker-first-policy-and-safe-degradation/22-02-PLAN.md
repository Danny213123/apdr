---
phase: 22-docker-first-policy-and-safe-degradation
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - benchmark_ui/run_contract.py
  - benchmark_ui/runner.py
  - benchmark_ui/service.py
  - benchmark_ui/state.py
  - benchmark_ui/test_run_contract.py
  - benchmark_ui/test_state_backend_doctor.py
  - web/index.html
  - web/src/main.js
autonomous: true
requirements:
  - DFV-01
  - DFV-03
  - GDR-01
user_setup: []
must_haves:
  truths:
    - Benchmark operators must be able to choose docker-first versus env-first control for APDR `llm` runs without treating policy as a new backend.
    - Docker-first must be the default policy presented for APDR `llm`, while env-first remains an explicit comparison option.
    - Phase 22 operator surfaces must explain docker-first safe degradation clearly, but detailed per-case Docker-build truth stays deferred to Phase 23.
  artifacts:
    - path: "benchmark_ui/run_contract.py"
      provides: "Run-contract support for the requested `llm` validation policy"
      contains: "llm_validation_policy"
    - path: "benchmark_ui/runner.py"
      provides: "Benchmark wrapper pass-through for the selected `llm` policy"
      contains: "--llm-validation-policy"
    - path: "web/src/main.js"
      provides: "Operator-facing control for docker-first versus env-first `llm` policy"
      contains: "llm-validation-policy"
    - path: "benchmark_ui/state.py"
      provides: "Doctor and runtime wording for docker-first `llm` with safe env fallback"
      contains: "docker-first"
  key_links:
    - from: "web/src/main.js"
      to: ".planning/phases/22-docker-first-policy-and-safe-degradation/22-CONTEXT.md"
      via: "The user wants docker-first to become the normal `llm` route now while env-first remains available as a control."
      pattern: "env-first must remain available as an explicit control path"
    - from: "benchmark_ui/state.py"
      to: ".planning/REQUIREMENTS.md"
      via: "GDR-01 requires clear degradation rather than silent env-only behavior when Docker cannot be used."
      pattern: "GDR-01"
---

<objective>
Expose the Phase 22 policy choice through benchmark operator surfaces and update runtime messaging without widening into full case-row truth work.

Purpose: The Rust route change alone does not satisfy the milestone. Benchmark operators need a visible way to request docker-first versus env-first `llm`, and Doctor/runtime copy must explain how docker-first degrades when Docker is unavailable. This plan adds that operator surface while explicitly deferring per-case Docker-build visibility to Phase 23.
Output: benchmark config and loadout support for `llm` policy, UI control and pass-through, updated Doctor/runtime descriptions, and Python/web regression coverage.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-docker-first-policy-and-safe-degradation/22-CONTEXT.md
@.planning/phases/22-docker-first-policy-and-safe-degradation/22-RESEARCH.md
@.planning/phases/22-docker-first-policy-and-safe-degradation/22-VALIDATION.md
@benchmark_ui/run_contract.py
@benchmark_ui/runner.py
@benchmark_ui/service.py
@benchmark_ui/state.py
@benchmark_ui/test_run_contract.py
@benchmark_ui/test_state_backend_doctor.py
@web/index.html
@web/src/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add benchmark run-config and UI support for `llm` policy selection</name>
  <files>benchmark_ui/run_contract.py, benchmark_ui/runner.py, benchmark_ui/service.py, web/index.html, web/src/main.js</files>
  <read_first>benchmark_ui/run_contract.py, benchmark_ui/runner.py, benchmark_ui/service.py, web/index.html, web/src/main.js, .planning/phases/22-docker-first-policy-and-safe-degradation/22-CONTEXT.md</read_first>
  <action>Extend the benchmark run-config flow so APDR `llm` runs carry a separate `llm_validation_policy` field with `docker-first` as the default and `env-first` as the explicit control. Update the web form and payload assembly to expose that control only when APDR `llm` is selected, keep loadouts and previews round-tripping the field, and make `benchmark_ui/runner.py` pass the chosen policy through to `test_executor.py`. Keep `validation_backend` equal to `llm`, and do not add per-case Docker-build visibility in result rows yet.</action>
  <verify>python3 -m unittest benchmark_ui.test_run_contract</verify>
  <acceptance_criteria>
    - `benchmark_ui/run_contract.py` contains `llm_validation_policy`
    - `benchmark_ui/runner.py` passes `--llm-validation-policy`
    - `web/index.html` and `web/src/main.js` contain a dedicated `llm` policy control
    - docker-first is the default policy presented when APDR `llm` is selected
    - `python3 -m unittest benchmark_ui.test_run_contract` exits 0
  </acceptance_criteria>
  <done>Benchmark operators can now explicitly choose docker-first or env-first control for APDR `llm` runs without changing the backend name.</done>
</task>

<task type="auto">
  <name>Task 2: Update Doctor and runtime messaging for docker-first safe degradation</name>
  <files>benchmark_ui/state.py, benchmark_ui/service.py, benchmark_ui/test_state_backend_doctor.py, benchmark_ui/test_run_contract.py</files>
  <read_first>benchmark_ui/state.py, benchmark_ui/service.py, benchmark_ui/test_state_backend_doctor.py, benchmark_ui/test_run_contract.py, .planning/phases/22-docker-first-policy-and-safe-degradation/22-RESEARCH.md</read_first>
  <action>Update APDR backend descriptions, Doctor rows, and run-summary labels so `llm` now reads as docker-first with env fallback and agent fallback rather than env-first targeted escalation. When Docker is unavailable, unhealthy, unsupported, or bypassed, the wording must say that docker-first was requested but APDR will degrade to env validation instead of silently acting as env-only. Add Python tests that lock the new operator wording, the persisted policy field, and the stable `validation_backend=llm` contract. Use the web build as the regression gate for the new policy control markup and JS wiring.</action>
  <verify>/bin/zsh -lc "python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_state_backend_doctor && npm run build --prefix web"</verify>
  <acceptance_criteria>
    - `benchmark_ui/state.py` no longer describes APDR `llm` as env-first by default
    - Docker-unavailable Doctor output explicitly mentions docker-first degradation to env
    - `benchmark_ui/test_state_backend_doctor.py` and `benchmark_ui/test_run_contract.py` cover the new policy field and wording
    - `npm run build --prefix web` exits 0
  </acceptance_criteria>
  <done>Operator-facing benchmark surfaces now describe docker-first truthfully and keep env-first available as a clear control path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_state_backend_doctor`
- [ ] `npm run build --prefix web`
- [ ] `rg -n 'llm_validation_policy|docker-first|env-first|llm-validation-policy' benchmark_ui/run_contract.py benchmark_ui/runner.py benchmark_ui/service.py benchmark_ui/state.py web/index.html web/src/main.js`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Benchmark operators can request docker-first or env-first control for APDR `llm`
- Doctor and runtime messaging explain docker-first degradation clearly without pretending Phase 23 case-row truth already exists

</success_criteria>

<output>
After completion, create `.planning/phases/22-docker-first-policy-and-safe-degradation/22-02-SUMMARY.md`
</output>
