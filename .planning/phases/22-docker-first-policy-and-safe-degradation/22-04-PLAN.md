---
phase: 22-docker-first-policy-and-safe-degradation
plan: 04
type: execute
wave: 4
depends_on:
  - 03
gap_closure: true
files_modified:
  - tools/apdr/src/docker/builder/process.rs
  - tools/apdr/src/docker/builder/agent_backend.rs
  - tools/apdr/src/docker/builder/mod.rs
  - benchmark_ui/state.py
  - benchmark_ui/test_state_backend_doctor.py
  - scripts/check_phase22_docker_policy.py
  - .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-slice.json
  - .planning/phases/22-docker-first-policy-and-safe-degradation/22-DOCKER-POLICY-PROOF.md
autonomous: true
requirements:
  - GDR-01
user_setup: []
must_haves:
  truths:
    - Docker-first `llm` routing must degrade to env validation when Docker is installed but unusable, not only when the CLI is missing.
    - The machine-readable bypass reason for unusable Docker must be persisted in the same route metadata and bypass note surfaces Phase 22 already introduced.
    - Phase 22 proof and Doctor messaging must freeze the installed-but-unusable Docker case so the routing contract and operator copy stop disagreeing.
  artifacts:
    - path: "tools/apdr/src/docker/builder/process.rs"
      provides: "Concrete Docker usability probe for docker-first `llm` route selection"
      contains: "docker info --format"
    - path: "tools/apdr/src/docker/builder/agent_backend.rs"
      provides: "Docker-first `llm` bypass path for daemon-unavailable or otherwise unusable Docker"
      contains: "docker daemon unavailable"
    - path: "tools/apdr/src/docker/builder/mod.rs"
      provides: "Rust regression coverage for unusable-Docker env fallback"
      contains: "phase22_policy_daemon_unavailable_falls_back_to_env"
    - path: "benchmark_ui/state.py"
      provides: "Doctor and runtime wording that explicitly degrades docker-first `llm` to env when the Docker daemon is unavailable"
      contains: "docker daemon unavailable"
    - path: "benchmark_ui/test_state_backend_doctor.py"
      provides: "Regression coverage for daemon-unavailable Doctor messaging"
      contains: "docker daemon unavailable"
    - path: "scripts/check_phase22_docker_policy.py"
      provides: "Phase 22 checker contract extended with an installed-but-unusable Docker case"
      contains: "docker-daemon-unavailable"
    - path: ".planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-slice.json"
      provides: "Fixed proof slice entry for the installed-but-unusable Docker contract case"
      contains: "docker-daemon-unavailable"
    - path: ".planning/phases/22-docker-first-policy-and-safe-degradation/22-DOCKER-POLICY-PROOF.md"
      provides: "Reviewer-facing proof note that Phase 22 covers installed-but-unusable Docker"
      contains: "docker daemon unavailable"
  key_links:
    - from: "tools/apdr/src/docker/builder/agent_backend.rs"
      to: ".planning/phases/22-docker-first-policy-and-safe-degradation/22-VERIFICATION.md"
      via: "The verifier found that `llm` routing only bypasses Docker when the CLI is missing or the case is pre-classified as host-runtime."
      pattern: "runtime route only checks Docker PATH presence"
    - from: "benchmark_ui/state.py"
      to: ".planning/phases/22-docker-first-policy-and-safe-degradation/22-VERIFICATION.md"
      via: "Doctor wording already promises unusable-Docker degradation, so the gap closure must keep operator messaging aligned with the repaired runtime route."
      pattern: "UI/Doctor copy says APDR will fall back to env validation when Docker is unavailable"
    - from: "scripts/check_phase22_docker_policy.py"
      to: ".planning/phases/22-docker-first-policy-and-safe-degradation/22-VERIFICATION.md"
      via: "The verifier requires the proof contract to cover installed-but-unusable Docker, not only missing Docker CLI."
      pattern: "proof contract covers env-first control, missing-Docker bypass, and host-runtime pre-skip, but not installed-but-unusable Docker"
    - from: ".planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-slice.json"
      to: ".planning/phases/22-docker-first-policy-and-safe-degradation/22-DOCKER-POLICY-PROOF.md"
      via: "The fixed slice and reviewer proof must describe the same installed-but-unusable Docker case so the deterministic checker and prose contract stay in lockstep."
      pattern: "docker-daemon-unavailable"
---

<objective>
Close the Phase 22 verification gap by making docker-first `llm` degrade correctly when Docker is present but unusable, and by freezing that behavior in tests and proof artifacts.

Purpose: Phase 22 implemented docker-first policy control, bypass notes, and a proof contract, but verification showed the runtime route still treats `docker on PATH` as sufficient even when the daemon is unavailable or the host cannot use Docker. This plan aligns runtime gating, operator messaging, and the proof contract around the same unusable-Docker fallback behavior so `GDR-01` can actually pass.
Output: real Docker-usability gating for docker-first `llm`, regression coverage for daemon-unavailable fallback, and an expanded Phase 22 proof contract that includes the unusable-Docker case.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-docker-first-policy-and-safe-degradation/22-CONTEXT.md
@.planning/phases/22-docker-first-policy-and-safe-degradation/22-RESEARCH.md
@.planning/phases/22-docker-first-policy-and-safe-degradation/22-VALIDATION.md
@.planning/phases/22-docker-first-policy-and-safe-degradation/22-VERIFICATION.md
@tools/apdr/src/docker/builder/process.rs
@tools/apdr/src/docker/builder/agent_backend.rs
@tools/apdr/src/docker/builder/mod.rs
@benchmark_ui/state.py
@benchmark_ui/test_state_backend_doctor.py
@scripts/check_phase22_docker_policy.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate docker-first `llm` on real Docker usability and persist the unusable-Docker bypass reason</name>
  <files>tools/apdr/src/docker/builder/process.rs, tools/apdr/src/docker/builder/agent_backend.rs, tools/apdr/src/docker/builder/mod.rs</files>
  <read_first>tools/apdr/src/docker/builder/process.rs, tools/apdr/src/docker/builder/agent_backend.rs, tools/apdr/src/docker/builder/mod.rs, .planning/phases/22-docker-first-policy-and-safe-degradation/22-VERIFICATION.md</read_first>
  <action>In `tools/apdr/src/docker/builder/process.rs`, add a helper that probes Docker usability for validation by first checking `command_on_path("docker")` and then running the exact command `docker info --format {{.ServerVersion}}` through the existing timeout wrapper with an 8 second timeout. Return a structured availability result that distinguishes the exact reasons `docker cli unavailable` and `docker daemon unavailable`. In `tools/apdr/src/docker/builder/agent_backend.rs`, replace the current boolean `command_on_path("docker")` route input with that structured probe so docker-first `llm` falls back to `EnvFirstDockerBypass` for both reasons, and persist the exact unusable-Docker reason into the top-level summary metadata and `docker-bypass.txt` note. In `tools/apdr/src/docker/builder/mod.rs`, add Rust tests named `phase22_policy_daemon_unavailable_falls_back_to_env` and `phase22_policy_daemon_unavailable_writes_bypass_note`, while keeping the existing missing-CLI and host-runtime tests green.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase22_policy_</verify>
  <acceptance_criteria>
    - `tools/apdr/src/docker/builder/process.rs` contains `docker info --format {{.ServerVersion}}`
    - `tools/apdr/src/docker/builder/agent_backend.rs` contains `docker daemon unavailable`
    - `tools/apdr/src/docker/builder/mod.rs` contains `phase22_policy_daemon_unavailable_falls_back_to_env`
    - `tools/apdr/src/docker/builder/mod.rs` contains `phase22_policy_daemon_unavailable_writes_bypass_note`
    - the bypass note content includes `docker_bypass_reason: docker daemon unavailable`
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase22_policy_` exits 0
  </acceptance_criteria>
  <done>Docker-first `llm` now degrades to env when Docker is installed but unusable, and that reason is explicit in metadata plus debug artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze the unusable-Docker case in Doctor wording and the Phase 22 proof contract</name>
  <files>benchmark_ui/state.py, benchmark_ui/test_state_backend_doctor.py, scripts/check_phase22_docker_policy.py, .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-slice.json, .planning/phases/22-docker-first-policy-and-safe-degradation/22-DOCKER-POLICY-PROOF.md</files>
  <read_first>benchmark_ui/state.py, benchmark_ui/test_state_backend_doctor.py, scripts/check_phase22_docker_policy.py, .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-slice.json, .planning/phases/22-docker-first-policy-and-safe-degradation/22-VERIFICATION.md</read_first>
  <action>In `benchmark_ui/state.py`, update the APDR `llm` Doctor and runtime wording so it explicitly covers both `docker cli unavailable` and `docker daemon unavailable` as cases that degrade to env validation. In `benchmark_ui/test_state_backend_doctor.py`, add assertions that the daemon-unavailable warning mentions env degradation for APDR `llm`. In `scripts/check_phase22_docker_policy.py`, extend `EXPECTED_CASES` from 4 to 5 by adding an exact case with `case_id: docker-daemon-unavailable`, `relative_path: contracts/docker-daemon-unavailable/snippet.py`, `requested_llm_validation_policy: docker-first`, `llm_validation_route: env-first-docker-bypass`, `expected_first_hop: env`, `docker_bypass_reason: docker daemon unavailable`, and `required_debug_artifacts: [\"docker-bypass.txt\"]`. Mirror that exact case in `22-docker-policy-slice.json`, and update `22-DOCKER-POLICY-PROOF.md` so the reviewer-facing contract explicitly says Phase 22 covers installed-but-unusable Docker, not only missing Docker CLI.</action>
  <verify>/bin/zsh -lc "python3 -m unittest benchmark_ui.test_state_backend_doctor && python3 scripts/check_phase22_docker_policy.py --slice-json .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-slice.json --status-json .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-proof-status.json --probe-only"</verify>
  <acceptance_criteria>
    - `benchmark_ui/state.py` contains `docker daemon unavailable`
    - `benchmark_ui/test_state_backend_doctor.py` contains `docker daemon unavailable`
    - `scripts/check_phase22_docker_policy.py` contains `docker-daemon-unavailable`
    - `.planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-slice.json` contains `docker-daemon-unavailable`
    - `.planning/phases/22-docker-first-policy-and-safe-degradation/22-DOCKER-POLICY-PROOF.md` contains `docker daemon unavailable`
    - `python3 -m unittest benchmark_ui.test_state_backend_doctor` exits 0
    - `python3 scripts/check_phase22_docker_policy.py --slice-json .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-slice.json --status-json .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-proof-status.json --probe-only` exits 0
  </acceptance_criteria>
  <done>The operator message and deterministic proof now freeze the installed-but-unusable Docker fallback that verification found missing.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml phase22_policy_`
- [ ] `python3 -m unittest benchmark_ui.test_state_backend_doctor`
- [ ] `python3 scripts/check_phase22_docker_policy.py --slice-json .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-slice.json --status-json .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-proof-status.json --probe-only`
- [ ] `rg -n 'docker daemon unavailable|docker-daemon-unavailable|docker info --format' tools/apdr/src/docker/builder/process.rs tools/apdr/src/docker/builder/agent_backend.rs tools/apdr/src/docker/builder/mod.rs benchmark_ui/state.py benchmark_ui/test_state_backend_doctor.py scripts/check_phase22_docker_policy.py .planning/phases/22-docker-first-policy-and-safe-degradation/22-docker-policy-slice.json .planning/phases/22-docker-first-policy-and-safe-degradation/22-DOCKER-POLICY-PROOF.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Docker-first `llm` degrades to env when Docker is present but unusable, not only when the CLI is missing
- Phase 22 proof and operator wording both cover the same unusable-Docker fallback contract

</success_criteria>

<output>
After completion, create `.planning/phases/22-docker-first-policy-and-safe-degradation/22-04-SUMMARY.md`
</output>
