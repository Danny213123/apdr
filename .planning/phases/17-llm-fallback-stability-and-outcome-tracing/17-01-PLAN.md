---
phase: 17-llm-fallback-stability-and-outcome-tracing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/apdr/docker_agent/graph.py
  - tools/apdr/docker_agent/state.py
  - tools/apdr/src/docker/builder/agent_backend.rs
  - tools/apdr/src/docker/builder/mod.rs
autonomous: true
requirements:
  - AGT-07
  - AGT-08
user_setup: []
must_haves:
  truths:
    - The `llm` validation backend must survive post-env fallback invocation without reproducing the March 30, 2026 `confidence` state-key crash.
    - Non-pass agent results must be returned as structured fallback summaries instead of disappearing into `None` and silently reverting to an env-only result.
    - Env validation attempts must remain visible before the terminal fallback attempt so later artifact work can expose the full path truth.
  artifacts:
    - path: "tools/apdr/docker_agent/graph.py"
      provides: "LangGraph graph wiring that no longer collides on the `confidence` name"
      contains: "graph.add_node(\"confidence_check\""
    - path: "tools/apdr/src/docker/builder/agent_backend.rs"
      provides: "Structured parsing and summary shaping for `passed`, `abstained`, and `failed` agent outcomes"
      contains: "status == \"abstained\""
    - path: "tools/apdr/src/docker/builder/mod.rs"
      provides: "Phase 17 Rust regression tests for non-pass agent results and preserved llm fallback history"
      contains: "phase17_llm_"
  key_links:
    - from: "tools/apdr/src/docker/builder/agent_backend.rs"
      to: "runs/20260330-020943-apdr/benchmark-context.log"
      via: "The live March 30 benchmark already records the exact fallback failure signature that Phase 17 must eliminate."
      pattern: "ValueError: 'confidence' is already being used as a state key"
    - from: "tools/apdr/src/docker/builder/agent_backend.rs"
      to: ".planning/phases/17-llm-fallback-stability-and-outcome-tracing/17-CONTEXT.md"
      via: "Phase 17 locks the fallback vocabulary to `passed`, `abstained`, and `failed` and forbids collapsing back into an unlabeled env-only result."
      pattern: "terminal agent outcome using the milestone vocabulary"
---

<objective>
Remove the immediate LangGraph crash path and make the Rust fallback path keep structured agent outcomes instead of throwing them away.

Purpose: The live benchmark already proves that env failures reach the LangGraph seam and then die with a `confidence` state-key error. Phase 17 starts by making that seam survive, record a terminal agent status, and retain env attempt history so later plans can surface the truth in saved artifacts.
Output: hardened LangGraph graph wiring, structured fallback parsing in Rust, and targeted regression tests.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-llm-fallback-stability-and-outcome-tracing/17-CONTEXT.md
@.planning/phases/17-llm-fallback-stability-and-outcome-tracing/17-RESEARCH.md
@.planning/phases/17-llm-fallback-stability-and-outcome-tracing/17-VALIDATION.md
@runs/20260330-020943-apdr/benchmark-context.log
@tools/apdr/docker_agent/graph.py
@tools/apdr/docker_agent/state.py
@tools/apdr/docker_agent/__main__.py
@tools/apdr/src/docker/builder/agent_backend.rs
@tools/apdr/src/docker/builder/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the `confidence` collision and keep structured non-pass agent results</name>
  <files>tools/apdr/docker_agent/graph.py, tools/apdr/docker_agent/state.py, tools/apdr/src/docker/builder/agent_backend.rs</files>
  <read_first>tools/apdr/docker_agent/graph.py, tools/apdr/docker_agent/state.py, tools/apdr/docker_agent/__main__.py, tools/apdr/src/docker/builder/agent_backend.rs, .planning/phases/17-llm-fallback-stability-and-outcome-tracing/17-CONTEXT.md, runs/20260330-020943-apdr/benchmark-context.log</read_first>
  <action>Rename the LangGraph node `confidence` to `confidence_check` in `tools/apdr/docker_agent/graph.py`, update the entry point and conditional-edge references to `confidence_check`, and keep the state fields `confidence` and `confidence_reason` unchanged in `tools/apdr/docker_agent/state.py`. In `tools/apdr/src/docker/builder/agent_backend.rs`, change agent-result handling so JSON results with `status` equal to `passed`, `abstained`, or `failed` all produce a `ValidationSummary` instead of returning `None`. Set `succeeded` to `true` only for `passed`, copy the terminal status into `summary.status`, derive `summary.reason` from `error_detail`, `confidence_reason`, or a fallback string, and append one synthetic `ValidationAttempt` with `validation_backend` set to `llm`, `status` equal to the terminal agent status, `artifact_dir` pointing at the agent output directory, and `log_excerpt` set to the fallback reason.</action>
  <verify>python3 -m py_compile tools/apdr/docker_agent/__main__.py tools/apdr/docker_agent/graph.py tools/apdr/docker_agent/state.py</verify>
  <acceptance_criteria>
    - `tools/apdr/docker_agent/graph.py` contains `graph.add_node("confidence_check"`
    - `tools/apdr/docker_agent/graph.py` contains `graph.set_entry_point("confidence_check")`
    - `tools/apdr/src/docker/builder/agent_backend.rs` contains `status == "abstained"`
    - `tools/apdr/src/docker/builder/agent_backend.rs` contains `status == "failed"`
    - `tools/apdr/src/docker/builder/agent_backend.rs` contains `ValidationAttempt`
    - `python3 -m py_compile tools/apdr/docker_agent/__main__.py tools/apdr/docker_agent/graph.py tools/apdr/docker_agent/state.py` exits 0
  </acceptance_criteria>
  <done>The llm fallback path no longer dies on the known `confidence` collision and now keeps a structured terminal agent result even when the agent does not pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 17 Rust regression tests for fallback outcome retention</name>
  <files>tools/apdr/src/docker/builder/mod.rs</files>
  <read_first>tools/apdr/src/docker/builder/mod.rs, tools/apdr/src/docker/builder/agent_backend.rs, .planning/phases/17-llm-fallback-stability-and-outcome-tracing/17-RESEARCH.md</read_first>
  <action>Add Rust tests to `tools/apdr/src/docker/builder/mod.rs` with names prefixed `phase17_llm_`. Create at least these three tests: `phase17_llm_parse_agent_result_accepts_failed_status`, `phase17_llm_parse_agent_result_accepts_abstained_status`, and `phase17_llm_merge_preserves_env_attempts_before_agent_attempt`. The failed-status test must parse a JSON line with `{\"status\":\"failed\",\"confidence_reason\":\"state key crash\",\"attempts\":[]}` and assert that it returns `Some(ValidationSummary)` with `succeeded == false` and `status == "failed"`. The abstained-status test must parse a JSON line with `{\"status\":\"abstained\",\"confidence_reason\":\"low confidence\",\"attempts\":[]}` and assert that it returns `Some(ValidationSummary)` with `status == "abstained"`. The merge test must assert that two env attempts remain first in the combined `attempts` list before the synthetic llm attempt.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase17_llm_</verify>
  <acceptance_criteria>
    - `tools/apdr/src/docker/builder/mod.rs` contains `phase17_llm_parse_agent_result_accepts_failed_status`
    - `tools/apdr/src/docker/builder/mod.rs` contains `phase17_llm_parse_agent_result_accepts_abstained_status`
    - `tools/apdr/src/docker/builder/mod.rs` contains `phase17_llm_merge_preserves_env_attempts_before_agent_attempt`
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase17_llm_` exits 0
  </acceptance_criteria>
  <done>Phase 17 now has targeted Rust coverage that locks the crash fix and non-pass fallback semantics in place.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m py_compile tools/apdr/docker_agent/__main__.py tools/apdr/docker_agent/graph.py tools/apdr/docker_agent/state.py`
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml phase17_llm_`
- [ ] `rg -n 'confidence_check|phase17_llm_|status == "abstained"|status == "failed"' tools/apdr/docker_agent/graph.py tools/apdr/src/docker/builder/agent_backend.rs tools/apdr/src/docker/builder/mod.rs`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The known March 30 `confidence` state-key crash path is removed or neutralized
- Non-pass fallback outcomes now survive as structured Rust summaries instead of disappearing into an env-only result

</success_criteria>

<output>
After completion, create `.planning/phases/17-llm-fallback-stability-and-outcome-tracing/17-01-SUMMARY.md`
</output>
