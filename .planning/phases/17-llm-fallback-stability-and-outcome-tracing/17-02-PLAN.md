---
phase: 17-llm-fallback-stability-and-outcome-tracing
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - tools/apdr/src/lib.rs
  - tools/apdr/src/resolver/retry_loop.rs
  - tools/apdr/src/resolver/recovery_diagnostics.rs
  - tools/apdr/test_executor.py
  - benchmark_ui/runner.py
  - benchmark_ui/service.py
  - benchmark_ui/test_runner_events.py
autonomous: true
requirements:
  - AGT-07
  - AGT-08
user_setup: []
must_haves:
  truths:
    - Saved case artifacts must answer two questions directly: whether fallback was invoked and how it ended.
    - The fallback outcome vocabulary must be exactly `passed`, `abstained`, or `failed` when fallback ran.
    - Benchmark UI ingestion must preserve fallback metadata without changing pass/skip/fail classification rules based on fallback outcome alone.
  artifacts:
    - path: "tools/apdr/src/lib.rs"
      provides: "ValidationSummary fields and report serialization for fallback invocation and outcome truth"
      contains: "pub fallback_invoked: bool"
    - path: "tools/apdr/test_executor.py"
      provides: "Per-case output YAML keys for fallback invocation, outcome, and reason"
      contains: "fallback_outcome:"
    - path: "benchmark_ui/runner.py"
      provides: "Benchmark result shaping that carries fallback metadata through API and event surfaces"
      contains: "fallbackOutcome"
  key_links:
    - from: "tools/apdr/src/lib.rs"
      to: ".planning/phases/13-measurement-and-run-contract-hardening/13-MEASUREMENT-CONTRACT.md"
      via: "Phase 17 should extend the existing reviewer-facing artifact contract rather than invent a separate debugging channel."
      pattern: "Artifacts should preserve these fields"
    - from: "benchmark_ui/service.py"
      to: ".planning/phases/17-llm-fallback-stability-and-outcome-tracing/17-CONTEXT.md"
      via: "The context locks the operator-facing questions to fallback invocation truth and terminal outcome visibility."
      pattern: "The artifact contract should answer two operator questions quickly"
---

<objective>
Thread the fallback invocation and terminal outcome through APDR's saved artifacts and benchmark readers without broadening into Phase 18 backend-routing work.

Purpose: Plan 01 makes the fallback survive. Phase 17 still fails if operators cannot inspect that path in per-case artifacts. This plan adds explicit fallback fields, preserves them through final summary shaping, serializes them into output YAML, and keeps the benchmark UI from flattening them away.
Output: explicit fallback metadata in Rust summaries, output files, and benchmark result consumers.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-llm-fallback-stability-and-outcome-tracing/17-CONTEXT.md
@.planning/phases/17-llm-fallback-stability-and-outcome-tracing/17-RESEARCH.md
@.planning/phases/17-llm-fallback-stability-and-outcome-tracing/17-VALIDATION.md
@.planning/phases/13-measurement-and-run-contract-hardening/13-MEASUREMENT-CONTRACT.md
@tools/apdr/src/lib.rs
@tools/apdr/src/resolver/retry_loop.rs
@tools/apdr/src/resolver/recovery_diagnostics.rs
@tools/apdr/test_executor.py
@benchmark_ui/runner.py
@benchmark_ui/service.py
@benchmark_ui/test_runner_events.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit fallback fields and preserve them through final APDR summaries</name>
  <files>tools/apdr/src/lib.rs, tools/apdr/src/resolver/retry_loop.rs, tools/apdr/src/resolver/recovery_diagnostics.rs</files>
  <read_first>tools/apdr/src/lib.rs, tools/apdr/src/resolver/retry_loop.rs, tools/apdr/src/resolver/recovery_diagnostics.rs, .planning/phases/17-llm-fallback-stability-and-outcome-tracing/17-CONTEXT.md, .planning/phases/13-measurement-and-run-contract-hardening/13-MEASUREMENT-CONTRACT.md</read_first>
  <action>Add three explicit fields to `ValidationSummary` in `tools/apdr/src/lib.rs`: `fallback_invoked: bool`, `fallback_outcome: Option<String>`, and `fallback_reason: Option<String>`. Render them into the text report and summary output with the exact keys `fallback_invoked`, `fallback_outcome`, and `fallback_reason`. In `tools/apdr/src/resolver/retry_loop.rs` and `tools/apdr/src/resolver/recovery_diagnostics.rs`, preserve these fields when final validation status, reason, and backend metadata are inferred so a failed or abstained fallback is still visible after retry-loop finalization. Use the locked vocabulary values `passed`, `abstained`, and `failed` for `fallback_outcome`, and keep `fallback_reason` populated with the best available agent detail.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase17_llm_</verify>
  <acceptance_criteria>
    - `tools/apdr/src/lib.rs` contains `pub fallback_invoked: bool`
    - `tools/apdr/src/lib.rs` contains `pub fallback_outcome: Option<String>`
    - `tools/apdr/src/lib.rs` contains `pub fallback_reason: Option<String>`
    - `tools/apdr/src/lib.rs` contains `fallback_invoked: {}`
    - `tools/apdr/src/lib.rs` contains `fallback_outcome: {}`
    - `tools/apdr/src/lib.rs` contains `fallback_reason: {}`
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase17_llm_` exits 0
  </acceptance_criteria>
  <done>APDR's in-memory and text artifact surfaces now carry explicit fallback invocation and outcome truth.</done>
</task>

<task type="auto">
  <name>Task 2: Serialize fallback metadata into case outputs and benchmark result readers</name>
  <files>tools/apdr/test_executor.py, benchmark_ui/runner.py, benchmark_ui/service.py, benchmark_ui/test_runner_events.py</files>
  <read_first>tools/apdr/test_executor.py, benchmark_ui/runner.py, benchmark_ui/service.py, benchmark_ui/test_runner_events.py, tools/apdr/src/lib.rs, .planning/phases/17-llm-fallback-stability-and-outcome-tracing/17-CONTEXT.md</read_first>
  <action>Update `tools/apdr/test_executor.py` to write the exact YAML keys `fallback_invoked`, `fallback_outcome`, and `fallback_reason` from the APDR summary output into every `output_data_*.yml` file. In `benchmark_ui/runner.py` and `benchmark_ui/service.py`, read those keys from `output_metadata`, expose them as result fields named `fallbackInvoked`, `fallbackOutcome`, and `fallbackReason`, and keep `_result_succeeded` and `_result_skipped` logic driven by `validation_status`, `validation_reason`, and existing success rules rather than fallback outcome alone. Add unit coverage in `benchmark_ui/test_runner_events.py` that builds a tier3 result with fallback metadata and asserts the new fields survive result shaping while pass or fail classification stays unchanged.</action>
  <verify>python3 -m unittest benchmark_ui.test_runner_events</verify>
  <acceptance_criteria>
    - `tools/apdr/test_executor.py` contains `fallback_invoked`
    - `tools/apdr/test_executor.py` contains `fallback_outcome`
    - `tools/apdr/test_executor.py` contains `fallback_reason`
    - `benchmark_ui/runner.py` contains `fallbackOutcome`
    - `benchmark_ui/service.py` contains `fallbackOutcome`
    - `benchmark_ui/test_runner_events.py` contains `fallbackOutcome`
    - `python3 -m unittest benchmark_ui.test_runner_events` exits 0
  </acceptance_criteria>
  <done>Saved case outputs and benchmark result readers now preserve fallback metadata without redefining the meaning of pass, skip, or fail.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml phase17_llm_`
- [ ] `python3 -m unittest benchmark_ui.test_runner_events`
- [ ] `rg -n 'fallback_invoked|fallback_outcome|fallback_reason' tools/apdr/src/lib.rs tools/apdr/test_executor.py`
- [ ] `rg -n 'fallbackOutcome|fallbackInvoked|fallbackReason' benchmark_ui/runner.py benchmark_ui/service.py benchmark_ui/test_runner_events.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Per-case APDR artifacts explicitly record fallback invocation and terminal outcome
- Benchmark result readers preserve fallback metadata without misclassifying success or skip state

</success_criteria>

<output>
After completion, create `.planning/phases/17-llm-fallback-stability-and-outcome-tracing/17-02-SUMMARY.md`
</output>
