---
phase: 03-validation-pipeline-throughput
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/apdr/src/docker/builder.rs
autonomous: true
requirements:
  - VAL-01
  - VAL-02
  - VAL-05
user_setup: []
must_haves:
  truths:
    - Env validation no longer discovers cache sources, prepares attempt paths, and mutates `ValidationAttempt` state inline inside one long loop body.
    - Validated env reuse prefers archive or legacy-dir cache sources before cold builds and falls back cleanly without duplicating metadata handling.
    - Env-to-Docker escalation preserves the env attempt history and keeps Windows path and cache semantics intact.
  artifacts:
    - path: "tools/apdr/src/docker/builder.rs"
      provides: "Explicit cache-source and attempt-path helpers for env validation"
      contains: "enum ValidatedEnvCacheSource"
    - path: "tools/apdr/src/docker/builder.rs"
      provides: "Prepared per-attempt workspace metadata for env validation"
      contains: "struct EnvAttemptPaths"
    - path: "tools/apdr/src/docker/builder.rs"
      provides: "Shared helper for env cache restore or cold build materialization"
      contains: "fn materialize_env_for_attempt"
  key_links:
    - from: "tools/apdr/src/docker/builder.rs"
      to: "tools/apdr/src/cache/maintenance.rs"
      via: "validated-env archive and legacy-dir reuse still depend on existing cache maintenance helpers"
      pattern: "touch_archive_marker"
    - from: "tools/apdr/src/docker/builder.rs"
      to: ".planning/phases/01-baseline-and-guardrails/01-HOTSPOT-AUDIT.md"
      via: "Phase 3 attacks the top-ranked throughput hotspot by making env attempt orchestration cheaper and more reviewable"
      pattern: "validate_requirements_env"
    - from: "tools/apdr/src/docker/builder.rs"
      to: ".planning/phases/02-resolver-memory-and-algorithm-efficiency/02-RESOLVER-DELTA.md"
      via: "validation-path improvements must stay distinguishable from the cached validation noise recorded after Phase 2"
      pattern: "validation_duration_ms"
---

<objective>
Reduce avoidable env-validation orchestration cost before touching benchmark reporting or closeout artifacts.

Purpose: `validate_requirements_env()` is still the main validation hot path and it duplicates cache detection, workspace preparation, restore-or-build branching, and attempt-history work inside one large loop.
Output: Explicit cache-source and attempt-path helpers, shared restore-or-build logic, and targeted validation-pipeline unit tests in `docker/builder.rs`.
</objective>

<execution_context>
@C:\Users\danny\.codex\get-shit-done\workflows\execute-plan.md
@C:\Users\danny\.codex\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\apdr\.planning\PROJECT.md
@D:\apdr\.planning\ROADMAP.md
@D:\apdr\.planning\STATE.md
@D:\apdr\.planning\phases\03-validation-pipeline-throughput\03-RESEARCH.md
@D:\apdr\.planning\phases\03-validation-pipeline-throughput\03-VALIDATION.md
@D:\apdr\.planning\phases\01-baseline-and-guardrails\01-HOTSPOT-AUDIT.md
@D:\apdr\.planning\phases\01-baseline-and-guardrails\01-baseline.json
@D:\apdr\.planning\phases\02-resolver-memory-and-algorithm-efficiency\02-RESOLVER-DELTA.md
@D:\apdr\tools\apdr\src\docker\builder.rs
@D:\apdr\tools\apdr\src\cache\maintenance.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract cache-source detection and per-attempt path preparation</name>
  <files>tools/apdr/src/docker/builder.rs</files>
  <read_first>tools/apdr/src/docker/builder.rs, tools/apdr/src/cache/maintenance.rs, .planning/phases/03-validation-pipeline-throughput/03-RESEARCH.md</read_first>
  <action>In `tools/apdr/src/docker/builder.rs`, introduce `enum ValidatedEnvCacheSource { Archive(PathBuf), LegacyDir(PathBuf), None }` and `struct EnvAttemptPaths` to own the per-attempt workspace data currently assembled inline in `validate_requirements_env()`: `work_dir`, `env_dir`, `build_log_path`, `run_log_path`, `combined_log_path`, `metadata_path`, `context_snapshot_path`, `install_requirements_path`, and the computed `env_label`. Add a helper named `prepare_env_validation_attempt(...)` that writes `requirements.txt`, `requirements-install.txt`, `smoke_test.py`, and `snippet.py`, snapshots the benchmark context tail, computes `build_key`, records `used_cached_lockfile`, and returns the selected `ValidatedEnvCacheSource` for that attempt. Update `validate_requirements_env()` so it calls the helper once per candidate Python version instead of rebuilding the same path and `ValidationAttempt` fields inline.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml validation_pipeline_detects_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/docker/builder.rs` contains `enum ValidatedEnvCacheSource`
    - `tools/apdr/src/docker/builder.rs` contains `struct EnvAttemptPaths`
    - `tools/apdr/src/docker/builder.rs` contains `fn prepare_env_validation_attempt(`
    - `tools/apdr/src/docker/builder.rs` contains `ValidatedEnvCacheSource::Archive`
    - `cargo test --manifest-path tools/apdr/Cargo.toml validation_pipeline_detects_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>Env validation has one helper for cache-source detection and per-attempt workspace preparation instead of a large inline setup block.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize restore-or-build execution and backend-history merge</name>
  <files>tools/apdr/src/docker/builder.rs</files>
  <read_first>tools/apdr/src/docker/builder.rs, tools/apdr/src/cache/maintenance.rs</read_first>
  <action>Add `fn materialize_env_for_attempt(...)` in `tools/apdr/src/docker/builder.rs` that takes the prepared attempt paths, the chosen `ValidatedEnvCacheSource`, and the current `ValidationAttempt`, then performs one of three paths: restore from archive, copy from legacy dir, or fall back to `create_and_install_env()`. The helper must validate that restored envs contain `bin/` or `Scripts/`, touch the correct archive or legacy cache marker on success, clear `used_cached_env` and `validated_env_cache_hit` when restore fails, and return the build logs plus exit code and duration through one shared result shape. Also add `fn merge_backend_retry_history(...)` so env-to-Docker fallback merges env attempts ahead of Docker attempts in one place instead of open-coding that list surgery in `validate_requirements()`. Replace the current inline `if cache_hit { ... } else { ... }` and env-summary merge blocks with those helpers while keeping post-success cleanup, build keys, and summary totals unchanged.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml validation_pipeline_merges_env_history_before_docker_attempts -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/docker/builder.rs` contains `fn materialize_env_for_attempt(`
    - `tools/apdr/src/docker/builder.rs` contains `fn merge_backend_retry_history(`
    - `tools/apdr/src/docker/builder.rs` contains `ValidatedEnvCacheSource::LegacyDir`
    - `tools/apdr/src/docker/builder.rs` contains `attempt.validated_env_cache_hit = false`
    - `cargo test --manifest-path tools/apdr/Cargo.toml validation_pipeline_merges_env_history_before_docker_attempts -- --nocapture` exits 0
  </acceptance_criteria>
  <done>Cache restore, cold build fallback, and env-to-Docker attempt-history merging are all centralized behind explicit helpers.</done>
</task>

<task type="auto">
  <name>Task 3: Add validation-pipeline unit tests for cache detection and history ordering</name>
  <files>tools/apdr/src/docker/builder.rs</files>
  <read_first>tools/apdr/src/docker/builder.rs, tools/apdr/src/cache/maintenance.rs</read_first>
  <action>Extend the existing test module in `tools/apdr/src/docker/builder.rs` with `validation_pipeline_detects_archive_cache_source()`, `validation_pipeline_detects_legacy_env_cache_source()`, and `validation_pipeline_merges_env_history_before_docker_attempts()`. Use temp directories to exercise archive and legacy-dir cache-source detection without creating real virtualenvs. The merge-history test must build small `ValidationSummary` values and prove that env attempt entries stay ahead of Docker attempt entries after backend escalation. Keep these tests targeted enough that the `validation_pipeline_` cargo filter remains the quick verification path for Phase 3.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml validation_pipeline_ -- --nocapture && cargo clippy --manifest-path tools/apdr/Cargo.toml --all-targets -- -D warnings</verify>
  <acceptance_criteria>
    - `tools/apdr/src/docker/builder.rs` contains `fn validation_pipeline_detects_archive_cache_source()`
    - `tools/apdr/src/docker/builder.rs` contains `fn validation_pipeline_detects_legacy_env_cache_source()`
    - `tools/apdr/src/docker/builder.rs` contains `fn validation_pipeline_merges_env_history_before_docker_attempts()`
    - `cargo test --manifest-path tools/apdr/Cargo.toml validation_pipeline_ -- --nocapture` exits 0
    - `cargo clippy --manifest-path tools/apdr/Cargo.toml --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>The new env-attempt helpers are protected by fast unit tests and the workspace stays clippy-clean.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml validation_pipeline_ -- --nocapture`
- [ ] `cargo clippy --manifest-path tools/apdr/Cargo.toml --all-targets -- -D warnings`
- [ ] `rg -n "ValidatedEnvCacheSource|EnvAttemptPaths|prepare_env_validation_attempt|materialize_env_for_attempt|merge_backend_retry_history" tools/apdr/src/docker/builder.rs`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Env validation uses explicit helper types for cache-source detection and attempt staging
- Cache restore and cold build fallback no longer duplicate large inline branches
- Env-to-Docker history merging stays deterministic and test-covered
</success_criteria>

<output>
After completion, create `.planning/phases/03-validation-pipeline-throughput/03-01-SUMMARY.md`
</output>
