---
phase: 03-validation-pipeline-throughput
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - tools/apdr/src/docker/builder.rs
  - scripts/measure_apdr_baseline.py
  - scripts/check_apdr_regression.py
autonomous: true
requirements:
  - VAL-02
  - VAL-03
  - VAL-04
  - VAL-05
user_setup: []
must_haves:
  truths:
    - Backend-attempt orchestration no longer reruns the `docker_agent` import probe or hand-parses JSON on every validation-agent attempt.
    - Benchmark artifacts expose backend, cache-hit, and env-create or install or smoke stage information per sample instead of only total validation time.
    - The regression gate can evaluate validation sub-stage regressions directly instead of only pass rate, total duration, and total validation duration.
  artifacts:
    - path: "tools/apdr/src/docker/builder.rs"
      provides: "Cached Docker-agent probe and JSON-backed agent-result parsing"
      contains: "DOCKER_AGENT_IMPORTABLE"
    - path: "scripts/measure_apdr_baseline.py"
      provides: "Per-sample validation-stage and cache-awareness reporting in the baseline harness"
      contains: "Env create ms"
    - path: "scripts/check_apdr_regression.py"
      provides: "Optional thresholds for validation sub-stage regressions"
      contains: "--max-env-create-regression-pct"
  key_links:
    - from: "tools/apdr/src/docker/builder.rs"
      to: "scripts/measure_apdr_baseline.py"
      via: "builder-side attempt metadata becomes reviewer-visible benchmark output"
      pattern: "validation_backend"
    - from: "scripts/check_apdr_regression.py"
      to: ".planning/phases/01-baseline-and-guardrails/01-baseline.json"
      via: "Phase 3 still gates candidate artifacts against the original milestone baseline"
      pattern: "validation_duration_ms"
    - from: "scripts/measure_apdr_baseline.py"
      to: ".planning/phases/02-resolver-memory-and-algorithm-efficiency/02-RESOLVER-DELTA.md"
      via: "the reporting path must now separate warm-path cache reuse from real validation work more clearly than Phase 2 could"
      pattern: "samples"
---

<objective>
Make backend attempts and benchmark reporting cheap enough and clear enough that Phase 3 can prove real validation wins.

Purpose: The phase still lacks explicit visibility into which backend ran, which stage dominated, and whether a result came from a cached validated env or a cold build.
Output: Cached backend probing in `docker/builder.rs`, richer benchmark reporting in `measure_apdr_baseline.py`, and a stricter regression gate for validation sub-stages.
</objective>

<execution_context>
@C:\Users\danny\.codex\get-shit-done\workflows\execute-plan.md
@C:\Users\danny\.codex\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\apdr\.planning\PROJECT.md
@D:\apdr\.planning\ROADMAP.md
@D:\apdr\.planning\STATE.md
@D:\apdr\.planning\phases\03-validation-pipeline-throughput\03-RESEARCH.md
@D:\apdr\.planning\phases\03-validation-pipeline-throughput\03-VALIDATION.md
@D:\apdr\.planning\phases\03-validation-pipeline-throughput\03-01-PLAN.md
@D:\apdr\.planning\phases\01-baseline-and-guardrails\01-baseline.json
@D:\apdr\tools\apdr\src\docker\builder.rs
@D:\apdr\scripts\measure_apdr_baseline.py
@D:\apdr\scripts\check_apdr_regression.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cache Docker-agent availability and parse agent results as JSON</name>
  <files>tools/apdr/src/docker/builder.rs</files>
  <read_first>tools/apdr/src/docker/builder.rs, .planning/phases/03-validation-pipeline-throughput/03-RESEARCH.md</read_first>
  <action>In `tools/apdr/src/docker/builder.rs`, add a cached availability probe for the Python `docker_agent` module using `OnceLock` so repeated `attempt_langgraph_agent()` calls stop launching `python3 -c "import docker_agent"` every time. Introduce a static named `DOCKER_AGENT_IMPORTABLE` and a small helper that records whether the agent can be imported from the discovered parent path. Keep the current `find_docker_agent_parent()` search order intact. In the same pass, replace the manual `extract_json_string` and `extract_json_number` parsing in `parse_agent_result()` with `serde_json::from_str::<serde_json::Value>` and normal field access so `status`, `selected_python_version`, and `total_duration_ms` come from real JSON parsing. Do not change the current fallback behavior when the agent is unavailable or returns a non-passed status.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml validation_pipeline_caches_docker_agent_probe -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/docker/builder.rs` contains `DOCKER_AGENT_IMPORTABLE`
    - `tools/apdr/src/docker/builder.rs` contains `serde_json::from_str::<serde_json::Value>`
    - `tools/apdr/src/docker/builder.rs` does not contain `fn extract_json_string(`
    - `tools/apdr/src/docker/builder.rs` does not contain `fn extract_json_number(`
    - `cargo test --manifest-path tools/apdr/Cargo.toml validation_pipeline_caches_docker_agent_probe -- --nocapture` exits 0
  </acceptance_criteria>
  <done>The agent backend no longer pays a fresh import probe or string-parser cost on every invocation.</done>
</task>

<task type="auto">
  <name>Task 2: Surface backend, cache, and validation-stage detail in the benchmark harness</name>
  <files>scripts/measure_apdr_baseline.py</files>
  <read_first>scripts/measure_apdr_baseline.py, .planning/phases/01-baseline-and-guardrails/01-BASELINE.md, .planning/phases/02-resolver-memory-and-algorithm-efficiency/02-RESOLVER-CANDIDATE.md</read_first>
  <action>Extend `scripts/measure_apdr_baseline.py` so each sample record captures reviewer-visible validation detail beyond total validation time. Keep the existing aggregate keys, but update the per-sample JSON and Markdown output to include the sample `validation_backend`, `env_create_duration_ms`, `install_duration_ms`, `smoke_duration_ms`, `llm_calls`, and cache indicators derived from the APDR artifacts. Parse the needed cache-hit and backend-escalation data from `resolution-report.txt` when it is not already present in `output_data_*.yml`. Update the Markdown sample table and per-sample detail section so reviewers can see `Backend`, `Env create ms`, `Install ms`, `Smoke ms`, and whether the sample reused a cached validated env. Preserve deterministic sample ordering and the existing JSON top-level keys so earlier phases remain readable.</action>
  <verify>python -m py_compile scripts/measure_apdr_baseline.py && python scripts/measure_apdr_baseline.py --help</verify>
  <acceptance_criteria>
    - `scripts/measure_apdr_baseline.py` contains `Env create ms`
    - `scripts/measure_apdr_baseline.py` contains `Install ms`
    - `scripts/measure_apdr_baseline.py` contains `Smoke ms`
    - `scripts/measure_apdr_baseline.py` contains `Backend`
    - `python -m py_compile scripts/measure_apdr_baseline.py` exits 0
  </acceptance_criteria>
  <done>The benchmark harness exposes stage-level validation cost and cache or backend context per sample instead of burying it inside total validation time.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the regression gate for env-create, install, and smoke regressions</name>
  <files>scripts/check_apdr_regression.py</files>
  <read_first>scripts/check_apdr_regression.py, scripts/measure_apdr_baseline.py, .planning/phases/01-baseline-and-guardrails/01-02-SUMMARY.md</read_first>
  <action>Update `scripts/check_apdr_regression.py` so it accepts optional threshold flags `--max-env-create-regression-pct`, `--max-install-regression-pct`, and `--max-smoke-regression-pct` in addition to the existing total, validation, and pass-rate thresholds. Add rows for those metrics to the printed comparison table and include them in the failure summary only when the corresponding threshold flag is provided. Keep the existing default behavior unchanged when the new flags are omitted, so earlier phases can still use the script without extra arguments.</action>
  <verify>python -m py_compile scripts/check_apdr_regression.py && python scripts/check_apdr_regression.py --help</verify>
  <acceptance_criteria>
    - `scripts/check_apdr_regression.py` contains `--max-env-create-regression-pct`
    - `scripts/check_apdr_regression.py` contains `--max-install-regression-pct`
    - `scripts/check_apdr_regression.py` contains `--max-smoke-regression-pct`
    - `scripts/check_apdr_regression.py` contains `env_create_duration_ms`
    - `python -m py_compile scripts/check_apdr_regression.py` exits 0
  </acceptance_criteria>
  <done>The regression gate can now protect validation sub-stage throughput instead of only total duration and total validation duration.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml validation_pipeline_ -- --nocapture`
- [ ] `python -m py_compile scripts/measure_apdr_baseline.py scripts/check_apdr_regression.py`
- [ ] `python scripts/measure_apdr_baseline.py --help`
- [ ] `python scripts/check_apdr_regression.py --help`
- [ ] `rg -n "DOCKER_AGENT_IMPORTABLE|Env create ms|Install ms|Smoke ms|--max-env-create-regression-pct|--max-install-regression-pct|--max-smoke-regression-pct" tools/apdr/src/docker/builder.rs scripts/measure_apdr_baseline.py scripts/check_apdr_regression.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Backend attempt probing is cached instead of repeated on every agent attempt
- Benchmark artifacts show backend and stage-level validation detail per sample
- The regression gate can protect env-create, install, and smoke throughput explicitly
</success_criteria>

<output>
After completion, create `.planning/phases/03-validation-pipeline-throughput/03-02-SUMMARY.md`
</output>
