---
phase: 24-env-first-vs-docker-first-comparison-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run_phase24_policy_comparison.py
  - .planning/phases/24-env-first-vs-docker-first-comparison-harness/24-comparison-slice.json
  - .planning/phases/24-env-first-vs-docker-first-comparison-harness/24-env-first-fixture-summary.json
  - .planning/phases/24-env-first-vs-docker-first-comparison-harness/24-docker-first-fixture-summary.json
autonomous: true
requirements:
  - CMP-01
user_setup: []
must_haves:
  truths:
    - Phase 24 must compare the same fixed slice under both policies rather than comparing unrelated runs.
    - Paired artifacts must preserve model, backend, and provenance truth while differing only on `llm_validation_policy`.
    - The harness must work in both probe-only extraction mode and execute-live replay mode.
  artifacts:
    - path: "scripts/run_phase24_policy_comparison.py"
      provides: "Paired extraction and replay harness for env-first and docker-first comparison artifacts"
      contains: "--llm-validation-policy"
    - path: ".planning/phases/24-env-first-vs-docker-first-comparison-harness/24-comparison-slice.json"
      provides: "Locked Phase 24 comparison slice manifest"
      contains: "slice_id"
    - path: ".planning/phases/24-env-first-vs-docker-first-comparison-harness/24-env-first-fixture-summary.json"
      provides: "Deterministic env-first probe fixture"
      contains: "\"llm_validation_policy\": \"env-first\""
    - path: ".planning/phases/24-env-first-vs-docker-first-comparison-harness/24-docker-first-fixture-summary.json"
      provides: "Deterministic docker-first probe fixture"
      contains: "\"llm_validation_policy\": \"docker-first\""
  key_links:
    - from: "scripts/run_phase24_policy_comparison.py"
      to: ".planning/REQUIREMENTS.md"
      via: "CMP-01 requires matched env-first and docker-first artifacts for the same fixed slice and contract."
      pattern: "CMP-01"
    - from: ".planning/phases/24-env-first-vs-docker-first-comparison-harness/24-comparison-slice.json"
      to: ".planning/phases/24-env-first-vs-docker-first-comparison-harness/24-RESEARCH.md"
      via: "Phase 24 must use a real dedicated comparison slice rather than the Phase 23 archetype contract."
      pattern: "Phase 24 therefore needs a separate fixed slice with real snippets and matched contracts"
---

<objective>
Create the paired extraction and replay harness that can materialize env-first and docker-first artifacts from the same fixed slice with matched run contracts.

Purpose: The repo already has replay-manifest and policy-routing seams, but it lacks a single command that can turn those into comparable env-first and docker-first artifacts. This plan creates that harness plus the locked slice and deterministic fixture summaries needed for fast probe-mode validation.
Output: a Phase 24 harness script, a fixed comparison slice, and paired fixture summaries for env-first and docker-first probe runs.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-env-first-vs-docker-first-comparison-harness/24-RESEARCH.md
@.planning/phases/24-env-first-vs-docker-first-comparison-harness/24-VALIDATION.md
@.planning/phases/23-policy-truth-and-failure-semantics/23-RESEARCH.md
@.planning/phases/23-policy-truth-and-failure-semantics/23-POLICY-TRUTH-PROOF.md
@scripts/run_phase20_recovery_benchmark.py
@benchmark_ui/runner.py
@benchmark_ui/service.py
@benchmark_ui/run_contract.py
@tools/apdr/test_executor.py
@tools/apdr/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the Phase 24 comparison slice and paired probe fixtures</name>
  <files>.planning/phases/24-env-first-vs-docker-first-comparison-harness/24-comparison-slice.json, .planning/phases/24-env-first-vs-docker-first-comparison-harness/24-env-first-fixture-summary.json, .planning/phases/24-env-first-vs-docker-first-comparison-harness/24-docker-first-fixture-summary.json</files>
  <read_first>.planning/phases/24-env-first-vs-docker-first-comparison-harness/24-RESEARCH.md, .planning/phases/23-policy-truth-and-failure-semantics/23-POLICY-TRUTH-PROOF.md, .planning/phases/22-docker-first-policy-and-safe-degradation/22-DOCKER-POLICY-PROOF.md, scripts/run_phase20_recovery_benchmark.py</read_first>
  <action>Create `.planning/phases/24-env-first-vs-docker-first-comparison-harness/24-comparison-slice.json` with an exact `slice_id`, a fixed ordered `cases` array, and explicit case ids plus `relative_path` values for a real-snippet comparison slice that will be replayed under both policies. Also create paired deterministic fixture summaries `.planning/phases/24-env-first-vs-docker-first-comparison-harness/24-env-first-fixture-summary.json` and `.planning/phases/24-env-first-vs-docker-first-comparison-harness/24-docker-first-fixture-summary.json`. Both fixtures must keep the same `slice_id`, `validation_backend: llm`, `model_name`, `base_url`, `build_profile`, `cache_state`, `results`, and `historical_results` shape, while differing only on `llm_validation_policy` and policy-sensitive per-case route/timing data. Each fixture result row must carry exact fields `snippet`, `artifact_dir`, `resultOrigin`, `validationPath`, `requestedLlmValidationPolicy`, `failureFamily`, `failureBucket`, `solve_duration_seconds`, `validation_duration_seconds`, `env_create_duration_seconds`, `install_duration_seconds`, `docker_startup_duration_seconds`, and `smoke_duration_seconds`.</action>
  <verify>rg -n 'slice_id|relative_path|llm_validation_policy|validation_backend|docker_startup_duration_seconds' .planning/phases/24-env-first-vs-docker-first-comparison-harness/24-comparison-slice.json .planning/phases/24-env-first-vs-docker-first-comparison-harness/24-env-first-fixture-summary.json .planning/phases/24-env-first-vs-docker-first-comparison-harness/24-docker-first-fixture-summary.json</verify>
  <acceptance_criteria>
    - `.planning/phases/24-env-first-vs-docker-first-comparison-harness/24-comparison-slice.json` contains `"slice_id"`
    - `.planning/phases/24-env-first-vs-docker-first-comparison-harness/24-comparison-slice.json` contains `"relative_path"`
    - `.planning/phases/24-env-first-vs-docker-first-comparison-harness/24-env-first-fixture-summary.json` contains `"llm_validation_policy": "env-first"`
    - `.planning/phases/24-env-first-vs-docker-first-comparison-harness/24-docker-first-fixture-summary.json` contains `"llm_validation_policy": "docker-first"`
    - both fixture summaries contain `"validation_backend": "llm"`
    - both fixture summaries contain `"docker_startup_duration_seconds"`
  </acceptance_criteria>
  <done>The repo now has a locked comparison slice and deterministic paired probe fixtures for env-first and docker-first artifact generation.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Phase 24 paired extraction and replay harness</name>
  <files>scripts/run_phase24_policy_comparison.py</files>
  <read_first>scripts/run_phase20_recovery_benchmark.py, benchmark_ui/runner.py, benchmark_ui/service.py, benchmark_ui/run_contract.py, tools/apdr/test_executor.py, tools/apdr/src/lib.rs, .planning/phases/24-env-first-vs-docker-first-comparison-harness/24-RESEARCH.md</read_first>
  <action>Create `scripts/run_phase24_policy_comparison.py` with exact flags `--slice-json`, `--summary-json`, `--output-json`, `--output-md`, `--mode`, `--llm-validation-policy`, `--probe-only`, `--execute-live`, and `--resume-run-id`. The script must enforce `validation_backend=llm`, load the locked slice, and either extract a comparison artifact from a saved summary or replay the slice through `BenchmarkWorker` with the selected `llm_validation_policy`. The emitted artifact JSON must preserve exact top-level fields `slice_id`, `mode`, `validation_backend`, `llm_validation_policy`, `model_name`, `base_url`, `run_contract`, `historical_results`, `results`, and `counts`. Each case row in `results` must preserve exact fields `relative_path`, `artifact_dir`, `resultOrigin`, `display_status`, `validation_path`, `requested_llm_validation_policy`, `failure_bucket`, `failure_family`, `solve_duration_seconds`, `validation_duration_seconds`, `env_create_duration_seconds`, `install_duration_seconds`, `docker_startup_duration_seconds`, and `smoke_duration_seconds`. In probe mode, make the harness validate the fixture summaries created in Task 1 and fail if the run contract drifts from `validation_backend=llm` or if the artifact case set does not exactly match the locked slice.</action>
  <verify>python3 scripts/run_phase24_policy_comparison.py --slice-json .planning/phases/24-env-first-vs-docker-first-comparison-harness/24-comparison-slice.json --summary-json .planning/phases/24-env-first-vs-docker-first-comparison-harness/24-env-first-fixture-summary.json --output-json /tmp/phase24-env-artifact.json --mode env-first --llm-validation-policy env-first --probe-only && python3 scripts/run_phase24_policy_comparison.py --slice-json .planning/phases/24-env-first-vs-docker-first-comparison-harness/24-comparison-slice.json --summary-json .planning/phases/24-env-first-vs-docker-first-comparison-harness/24-docker-first-fixture-summary.json --output-json /tmp/phase24-docker-artifact.json --mode docker-first --llm-validation-policy docker-first --probe-only</verify>
  <acceptance_criteria>
    - `scripts/run_phase24_policy_comparison.py` contains `--llm-validation-policy`
    - `scripts/run_phase24_policy_comparison.py` contains `validation_backend=llm`
    - `scripts/run_phase24_policy_comparison.py` contains `docker_startup_duration_seconds`
    - `/tmp/phase24-env-artifact.json` contains `"llm_validation_policy": "env-first"` after the verify command
    - `/tmp/phase24-docker-artifact.json` contains `"llm_validation_policy": "docker-first"` after the verify command
    - both generated artifacts contain `"validation_backend": "llm"`
    - both generated artifacts contain `"requested_llm_validation_policy"`
    - both generated artifacts contain `"resultOrigin"`
  </acceptance_criteria>
  <done>Phase 24 can now materialize env-first and docker-first artifacts from the same locked slice with matched contracts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 scripts/run_phase24_policy_comparison.py --slice-json .planning/phases/24-env-first-vs-docker-first-comparison-harness/24-comparison-slice.json --summary-json .planning/phases/24-env-first-vs-docker-first-comparison-harness/24-env-first-fixture-summary.json --output-json /tmp/phase24-env-artifact.json --mode env-first --llm-validation-policy env-first --probe-only`
- [ ] `python3 scripts/run_phase24_policy_comparison.py --slice-json .planning/phases/24-env-first-vs-docker-first-comparison-harness/24-comparison-slice.json --summary-json .planning/phases/24-env-first-vs-docker-first-comparison-harness/24-docker-first-fixture-summary.json --output-json /tmp/phase24-docker-artifact.json --mode docker-first --llm-validation-policy docker-first --probe-only`
- [ ] `rg -n 'llm_validation_policy|validation_backend|requested_llm_validation_policy|docker_startup_duration_seconds|resultOrigin' scripts/run_phase24_policy_comparison.py /tmp/phase24-env-artifact.json /tmp/phase24-docker-artifact.json`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The same fixed slice can produce paired env-first and docker-first artifacts with matched `validation_backend=llm` contracts
- Probe mode validates deterministic fixture summaries without needing a live run

</success_criteria>

<output>
After completion, create `.planning/phases/24-env-first-vs-docker-first-comparison-harness/24-01-SUMMARY.md`
</output>
