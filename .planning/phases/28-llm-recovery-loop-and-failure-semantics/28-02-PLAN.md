---
phase: 28-llm-recovery-loop-and-failure-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - tools/apdr/src/resolver/recovery_diagnostics.rs
  - tools/apdr/src/recovery/classifier.rs
  - tools/apdr/src/lib.rs
  - tools/apdr/test_executor.py
  - benchmark_ui/runner.py
  - benchmark_ui/service.py
  - benchmark_ui/test_run_contract.py
  - benchmark_ui/test_runner_events.py
autonomous: true
requirements:
  - TRU-01
  - LLM-03
user_setup: []
must_haves:
  truths:
    - Final case artifacts must additively distinguish `llm-no-output`, `provider-tooling-failure`, `docker-infrastructure-failure`, and `dependency-runtime-failure` without removing the existing coarse `failure_family`.
    - The final case report must stop collapsing provider/tooling failures into generic `Unknown` and Docker/runtime seams into misleading `SystemDependency`.
    - Benchmark rows and live events must expose the same additive failure-truth fields as saved case metadata.
  artifacts:
    - path: "tools/apdr/src/lib.rs"
      provides: "Report and summary export for additive failure truth"
      contains: "failure_truth_class"
    - path: "tools/apdr/src/resolver/recovery_diagnostics.rs"
      provides: "Final failure-truth classification logic"
      contains: "failure_truth_detail"
    - path: "benchmark_ui/runner.py"
      provides: "Saved/live export for failure-truth fields"
      contains: "failureTruthClass"
    - path: "benchmark_ui/test_runner_events.py"
      provides: "Benchmark regression tests for failure truth"
      contains: "failureTruthClass"
  key_links:
    - from: "tools/apdr/src/resolver/recovery_diagnostics.rs"
      to: ".planning/REQUIREMENTS.md"
      via: "TRU-01 requires case reports to distinguish model, provider, Docker, and dependency/runtime failures."
      pattern: "TRU-01"
    - from: "benchmark_ui/runner.py"
      to: ".planning/phases/28-llm-recovery-loop-and-failure-semantics/28-RESEARCH.md"
      via: "Saved rows and live events must surface the additive failure truth, not just raw notes."
      pattern: "failure_truth_class"
---

<objective>
Add additive final-failure truth fields and surface them through saved and live benchmark metadata.

Purpose: the recovery loop alone is not enough if final case reports still collapse failures into `Unknown` or `SystemDependency`. Phase 28 needs an explicit failure-truth taxonomy that is machine-readable, preserved in case artifacts, and visible through benchmark readers.
Output: new additive failure-truth fields in case outputs and benchmark surfaces, plus regression tests that prove provider/tooling and Docker failures stay distinct from true dependency/runtime misses.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-llm-recovery-loop-and-failure-semantics/28-RESEARCH.md
@.planning/phases/28-llm-recovery-loop-and-failure-semantics/28-VALIDATION.md
@tools/apdr/src/resolver/recovery_diagnostics.rs
@tools/apdr/src/recovery/classifier.rs
@tools/apdr/src/lib.rs
@tools/apdr/test_executor.py
@benchmark_ui/runner.py
@benchmark_ui/service.py
@benchmark_ui/test_run_contract.py
@benchmark_ui/test_runner_events.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add additive failure-truth classification to final case outputs</name>
  <files>tools/apdr/src/resolver/recovery_diagnostics.rs, tools/apdr/src/recovery/classifier.rs, tools/apdr/src/lib.rs</files>
  <read_first>tools/apdr/src/resolver/recovery_diagnostics.rs, tools/apdr/src/recovery/classifier.rs, tools/apdr/src/lib.rs, runs/20260402-150222-apdr/cases/1045108/resolution-report.txt, runs/20260402-184821-apdr/cases/005bbad123ef309a5bef/resolution-report.txt</read_first>
  <action>Add additive top-level fields `failure_truth_class` and `failure_truth_detail` to final case outputs and report text. Implement an exact mapping that classifies `empty-output`, `invalid-json`, and `schema-validation-failure` as `llm-no-output`; `provider-tooling-failure`, `timeout`, and `transport-failure` as `provider-tooling-failure`; Docker daemon, image handoff, and image-inspect failures as `docker-infrastructure-failure`; and true package/build/runtime misses as `dependency-runtime-failure`. Keep the existing `failure_family` field, but derive the new additive fields from intake failures, recovery outcomes, Docker artifact truth, and final attempt metadata instead of only the last log excerpt.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase28_truth_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/lib.rs` contains `failure_truth_class`
    - `tools/apdr/src/lib.rs` contains `failure_truth_detail`
    - `tools/apdr/src/resolver/recovery_diagnostics.rs` contains `llm-no-output`
    - `tools/apdr/src/resolver/recovery_diagnostics.rs` contains `docker-infrastructure-failure`
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase28_truth_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>Final case artifacts now carry additive truth fields that separate model/provider, Docker, and dependency/runtime failures.</done>
</task>

<task type="auto">
  <name>Task 2: Export failure truth through benchmark rows and live events</name>
  <files>tools/apdr/test_executor.py, benchmark_ui/runner.py, benchmark_ui/service.py, benchmark_ui/test_run_contract.py, benchmark_ui/test_runner_events.py</files>
  <read_first>tools/apdr/test_executor.py, benchmark_ui/runner.py, benchmark_ui/service.py, benchmark_ui/test_run_contract.py, benchmark_ui/test_runner_events.py</read_first>
  <action>Export the additive fields `failure_truth_class`, `failure_truth_detail`, and `recovery_outcome` through APDR summary lines, benchmark output metadata, saved case rows, and live SSE events. Use the exact camelCase reader keys `failureTruthClass`, `failureTruthDetail`, and `recoveryOutcome` in the benchmark layer so saved and live inspection stay aligned. Keep the change additive and case-detail-oriented; do not redesign benchmark tables.</action>
  <verify>python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_runner_events</verify>
  <acceptance_criteria>
    - `tools/apdr/test_executor.py` contains `failure_truth_class`
    - `benchmark_ui/runner.py` contains `failureTruthClass`
    - `benchmark_ui/service.py` contains `failureTruthDetail`
    - `benchmark_ui/test_runner_events.py` contains `recoveryOutcome`
    - `python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_runner_events` exits 0
  </acceptance_criteria>
  <done>The benchmark layer now exposes the same additive failure truth as the underlying case artifacts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml phase28_truth_ -- --nocapture`
- [ ] `python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_runner_events`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Final case reports no longer collapse provider/tooling and Docker failures into generic dependency misses
- Saved and live benchmark surfaces expose additive failure truth fields consistently

</success_criteria>

<output>
After completion, create `.planning/phases/28-llm-recovery-loop-and-failure-semantics/28-02-SUMMARY.md`
</output>
