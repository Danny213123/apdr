---
phase: 28-llm-recovery-loop-and-failure-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/apdr/llm_py/models.py
  - tools/apdr/llm_py/prompts.py
  - tools/apdr/llm_py/actions/recovery.py
  - tools/apdr/src/resolver/tier3_llm/core.rs
  - tools/apdr/src/resolver/retry_loop.rs
  - tools/apdr/tests/test_resolver.rs
  - tools/apdr/llm_py/tests/test_recovery_mock.py
autonomous: true
requirements:
  - LLM-03
user_setup: []
must_haves:
  truths:
    - Phase 28 recovery must consume the Phase 26 authored case plan and the Phase 27 authored/executed Docker artifacts instead of relying only on a flattened error log.
    - Each bounded recovery attempt must produce a structured outcome such as `applied`, `abstained`, `provider-failure`, or `exhausted`, not just an implicit note in iteration history.
    - Reviewers must be able to reconstruct why a retry happened from durable recovery artifacts rather than from scattered log excerpts.
  artifacts:
    - path: "tools/apdr/llm_py/models.py"
      provides: "Structured recovery request/response contract"
      contains: "recovery_outcome"
    - path: "tools/apdr/src/resolver/retry_loop.rs"
      provides: "Bounded artifact-aware recovery loop"
      contains: "recovery-attempts.json"
    - path: "tools/apdr/tests/test_resolver.rs"
      provides: "Phase 28 recovery regression tests"
      contains: "phase28_recovery_"
  key_links:
    - from: "tools/apdr/llm_py/actions/recovery.py"
      to: ".planning/phases/27-llm-authored-docker-validation-and-artifact-truth/27-DOCKER-ARTIFACT-PROOF.md"
      via: "Recovery must consume the authored-versus-executed Docker truth that Phase 27 froze."
      pattern: "executed Docker truth is preserved separately"
    - from: "tools/apdr/src/resolver/retry_loop.rs"
      to: ".planning/REQUIREMENTS.md"
      via: "LLM-03 requires bounded recovery changes using prior attempt logs and artifacts."
      pattern: "LLM-03"
---

<objective>
Add the structured recovery contract and make the retry loop consume authored and executed artifacts before Phase 28 starts classifying final failure truth.

Purpose: Phase 27 made Docker truth inspectable, but the recovery action still only sees a flattened error log. Phase 28 must widen the recovery input contract, persist ordered recovery attempts, and keep the loop bounded when the model abstains or the provider fails.
Output: a structured recovery request/response contract, a durable `recovery-attempts.json` artifact, and regression tests that prove the retry loop uses authored/executed artifact context.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-llm-authored-docker-validation-and-artifact-truth/27-DOCKER-ARTIFACT-PROOF.md
@.planning/phases/28-llm-recovery-loop-and-failure-semantics/28-RESEARCH.md
@.planning/phases/28-llm-recovery-loop-and-failure-semantics/28-VALIDATION.md
@tools/apdr/llm_py/models.py
@tools/apdr/llm_py/prompts.py
@tools/apdr/llm_py/actions/recovery.py
@tools/apdr/src/resolver/tier3_llm/core.rs
@tools/apdr/src/resolver/retry_loop.rs
@tools/apdr/tests/test_resolver.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the recovery request and response contract around authored and executed artifacts</name>
  <files>tools/apdr/llm_py/models.py, tools/apdr/llm_py/prompts.py, tools/apdr/llm_py/actions/recovery.py, tools/apdr/src/resolver/tier3_llm/core.rs, tools/apdr/llm_py/tests/test_recovery_mock.py</files>
  <read_first>.planning/phases/27-llm-authored-docker-validation-and-artifact-truth/27-DOCKER-ARTIFACT-PROOF.md, tools/apdr/llm_py/models.py, tools/apdr/llm_py/prompts.py, tools/apdr/llm_py/actions/recovery.py, tools/apdr/src/resolver/tier3_llm/core.rs, tools/apdr/llm_py/tests/test_recovery_mock.py</read_first>
  <action>Extend the recovery contract so the request carries `authored_plan`, `docker_plan`, `intake_failure`, and explicit attempt artifact pointers such as `combined_log_path`, `executed_dockerfile_path`, `image_inspect_path`, and `executed_image_ref`. Extend the response so it carries exact fields `recovery_outcome`, `failure_class`, and `diagnostic_preview` in addition to the existing package-fix fields. Update the recovery prompt text to explicitly tell the model to use authored case-plan assumptions, authored Docker intent, executed Docker artifacts, and prior recovery history when deciding among `wrong_package`, `version`, `add_package`, `remove_package`, or abstention.</action>
  <verify>python3 -m pytest tools/apdr/llm_py/tests/test_recovery_mock.py tools/apdr/llm_py/tests/test_client_fallbacks.py -q</verify>
  <acceptance_criteria>
    - `tools/apdr/llm_py/models.py` contains `recovery_outcome`
    - `tools/apdr/llm_py/models.py` contains `failure_class`
    - `tools/apdr/llm_py/prompts.py` contains `executed_dockerfile_path`
    - `tools/apdr/src/resolver/tier3_llm/core.rs` parses `recovery_outcome`
    - `python3 -m pytest tools/apdr/llm_py/tests/test_recovery_mock.py tools/apdr/llm_py/tests/test_client_fallbacks.py -q` exits 0
  </acceptance_criteria>
  <done>The LLM recovery path now has a structured artifact-aware contract instead of a log-only fallback prompt.</done>
</task>

<task type="auto">
  <name>Task 2: Persist ordered recovery attempts and bounded outcomes in the retry loop</name>
  <files>tools/apdr/src/resolver/retry_loop.rs, tools/apdr/tests/test_resolver.rs</files>
  <read_first>tools/apdr/src/resolver/retry_loop.rs, tools/apdr/tests/test_resolver.rs, .planning/phases/28-llm-recovery-loop-and-failure-semantics/28-RESEARCH.md</read_first>
  <action>Update the retry loop so every LLM recovery invocation records a machine-readable recovery attempt entry and writes a case-level `recovery-attempts.json` artifact. Each entry must include `attempt_index`, `recovery_outcome`, `failure_class`, `diagnostic_preview`, `authored_plan_path`, `docker_plan_path`, `combined_log_path`, and any applied package changes. Bound repeated provider or no-output failures so the loop stops truthfully after a small fixed number of recovery failures instead of continuing to flatten them into generic notes.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase28_recovery_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/resolver/retry_loop.rs` contains `recovery-attempts.json`
    - `tools/apdr/src/resolver/retry_loop.rs` contains `recovery_outcome`
    - `tools/apdr/tests/test_resolver.rs` contains `phase28_recovery_`
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase28_recovery_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>The retry loop now leaves a durable, bounded, artifact-aware recovery record for every LLM-driven case.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tools/apdr/llm_py/tests/test_recovery_mock.py tools/apdr/llm_py/tests/test_client_fallbacks.py -q`
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml phase28_recovery_ -- --nocapture`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Recovery prompts now consume authored and executed artifact truth
- Cases now preserve a structured `recovery-attempts.json` artifact with bounded recovery outcomes

</success_criteria>

<output>
After completion, create `.planning/phases/28-llm-recovery-loop-and-failure-semantics/28-01-SUMMARY.md`
</output>
