---
phase: "21.1"
slug: repository-footprint-and-download-size-reduction
status: draft
nyquist_compliant: false
wave_0_complete: true
created: 2026-04-01
---

# Phase 21.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Git inventory checks + shell helper checks + Rust `cargo test` + deterministic footprint proof script |
| **Config file** | `tools/apdr/Cargo.toml`, `scripts/check_phase21_1_footprint.py`, and `scripts/cleanup-apdr-footprint.sh` |
| **Quick run command** | `/bin/zsh -lc "python3 scripts/check_phase21_1_footprint.py --repo-root . --mode tracked --status-json /tmp/phase21_1-tracked-status.json --probe-only && cargo test --manifest-path tools/apdr/Cargo.toml --test test_cache phase21_1_cache_ -- --nocapture"` |
| **Full suite command** | `/bin/zsh -lc "python3 scripts/check_phase21_1_footprint.py --repo-root . --mode delta --baseline-json .planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-footprint-baseline.json --candidate-json .planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-footprint-candidate.json --status-json .planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-footprint-proof-status.json --probe-only && cargo test --manifest-path tools/apdr/Cargo.toml --test test_cache phase21_1_cache_ -- --nocapture && bash scripts/cleanup-apdr-footprint.sh --help"` |
| **Estimated runtime** | ~120 seconds |

---

## Sampling Rate

- **After every task commit:** Run `/bin/zsh -lc "python3 scripts/check_phase21_1_footprint.py --repo-root . --mode tracked --status-json /tmp/phase21_1-tracked-status.json --probe-only || cargo test --manifest-path tools/apdr/Cargo.toml --test test_cache phase21_1_cache_ -- --nocapture"`
- **After every plan wave:** Run `/bin/zsh -lc "python3 scripts/check_phase21_1_footprint.py --repo-root . --mode tracked --status-json /tmp/phase21_1-tracked-status.json --probe-only && cargo test --manifest-path tools/apdr/Cargo.toml --test test_cache phase21_1_cache_ -- --nocapture && bash scripts/cleanup-apdr-footprint.sh --help"`
- **Before `$gsd-verify-work`:** Full suite plus the deterministic footprint delta checker must be green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 21.1-01-01 | 01 | 1 | DSK-01 | git-inventory | `python3 scripts/check_phase21_1_footprint.py --repo-root . --mode tracked --status-json /tmp/phase21_1-tracked-status.json --probe-only` | ✅ | ⬜ pending |
| 21.1-01-02 | 01 | 1 | DSK-01 | git-inventory | `python3 scripts/check_phase21_1_footprint.py --repo-root . --mode tracked --status-json /tmp/phase21_1-tracked-status.json --probe-only` | ✅ | ⬜ pending |
| 21.1-02-01 | 02 | 2 | DSK-02 | unit | `cargo test --manifest-path tools/apdr/Cargo.toml --test test_cache phase21_1_cache_ -- --nocapture` | ✅ | ⬜ pending |
| 21.1-02-02 | 02 | 2 | DSK-02 | script-contract | `bash scripts/cleanup-apdr-footprint.sh --help` | ✅ | ⬜ pending |
| 21.1-03-01 | 03 | 3 | DSK-01, DSK-02 | proof-contract | `python3 scripts/check_phase21_1_footprint.py --repo-root . --mode delta --baseline-json .planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-footprint-baseline.json --candidate-json .planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-footprint-candidate.json --status-json .planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-footprint-proof-status.json --probe-only` | ✅ | ⬜ pending |
| 21.1-03-02 | 03 | 3 | DSK-01, DSK-02 | proof-contract | `python3 scripts/check_phase21_1_footprint.py --repo-root . --mode delta --baseline-json .planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-footprint-baseline.json --candidate-json .planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-footprint-candidate.json --status-json .planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-footprint-proof-status.json --probe-only` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- Existing infrastructure covers all phase requirements.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Fresh source distribution is materially lighter because tracked APDR build outputs are gone | DSK-01 | Requires human confirmation that the removed paths were generated artifacts and not hidden source dependencies | Inspect `git ls-files 'tools/apdr/target*'` and confirm it returns nothing. Then inspect the repo tree and verify no workflow in `tools/apdr/README.md` depends on checked-in target outputs. |
| The default external cache path is safe for normal APDR workflows | DSK-02 | Requires checking real operator ergonomics, not only a unit assertion | Run an APDR cache command and a normal APDR build flow, then confirm the repo tree does not repopulate `.apdr-cache/` unless explicitly requested. |
| Cleanup remains safe rather than surprising or destructive by default | DSK-02 | Requires human confirmation of command UX | Run `bash scripts/cleanup-apdr-footprint.sh --help` and a dry-run invocation. Confirm the script reports bytes first and requires an explicit apply step before deleting anything. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
