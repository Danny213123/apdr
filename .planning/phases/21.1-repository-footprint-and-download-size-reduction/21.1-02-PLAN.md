---
phase: 21.1-repository-footprint-and-download-size-reduction
plan: 02
type: execute
wave: 2
depends_on:
  - 21.1-01
files_modified:
  - tools/apdr/Cargo.toml
  - tools/apdr/src/lib.rs
  - tools/apdr/src/main.rs
  - tools/apdr/build.sh
  - tools/apdr/README.md
  - scripts/cleanup-apdr-footprint.sh
autonomous: true
requirements:
  - DSK-02
user_setup: []
must_haves:
  truths:
    - APDR must stop defaulting its heavy runtime cache inside the repo tree when an external per-user cache location is available.
    - Developers need a supported cleanup path for APDR cache bytes and Cargo target bytes that is safe by default and explicit when destructive actions occur.
    - Common APDR build helpers should steer heavy build outputs away from the repo tree or at minimum honor an external target-dir override by default.
  artifacts:
    - path: "tools/apdr/src/lib.rs"
      provides: "Centralized default APDR cache-path resolution outside the repo tree"
      contains: "default_apdr_cache_path"
    - path: "scripts/cleanup-apdr-footprint.sh"
      provides: "Supported local cleanup helper for APDR cache and target bytes"
      contains: "--dry-run"
    - path: "tools/apdr/build.sh"
      provides: "Build helper that honors an external APDR target dir"
      contains: "APDR_TARGET_DIR"
  key_links:
    - from: "tools/apdr/src/lib.rs"
      to: ".planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-RESEARCH.md"
      via: "Research identified the in-repo default `.apdr-cache` path as a root cause of local repo growth."
      pattern: "tool_root.join(\".apdr-cache\")"
    - from: "scripts/cleanup-apdr-footprint.sh"
      to: "scripts/cleanup-envs.sh"
      via: "The repo already accepts explicit cleanup helpers for large benchmark leftovers, so Phase 21.1 should follow the same operator pattern."
      pattern: "dry-run"
---

<objective>
Move heavy APDR cache and build defaults out of the repo tree where practical, and add a supported cleanup path for the remaining local footprint.

Purpose: Even after tracked source-distribution bloat is removed, normal APDR usage still creates multi-gigabyte `.apdr-cache` and `target` trees inside the repo. This plan makes cache location safer by default, steers common build output toward an external target dir, and adds a cleanup helper that can reclaim bytes without turning APDR rebuildability into guesswork.
Output: external-first APDR cache defaults, a cleanup helper, and updated build/docs surfaces for local footprint control.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-CONTEXT.md
@.planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-RESEARCH.md
@.planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-VALIDATION.md
@tools/apdr/Cargo.toml
@tools/apdr/src/lib.rs
@tools/apdr/src/main.rs
@tools/apdr/build.sh
@tools/apdr/README.md
@scripts/cleanup-envs.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize external-first APDR cache defaults and lock them with Rust tests</name>
  <files>tools/apdr/Cargo.toml, tools/apdr/src/lib.rs, tools/apdr/src/main.rs</files>
  <read_first>tools/apdr/Cargo.toml, tools/apdr/src/lib.rs, tools/apdr/src/main.rs, .planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-RESEARCH.md</read_first>
  <action>In `tools/apdr/src/lib.rs`, add an exact helper named `default_apdr_cache_path(tool_root: &Path) -> PathBuf` that resolves in this order: a non-empty `APDR_CACHE_DIR`, otherwise a per-user cache directory outside the repo tree using the `dirs` crate, otherwise `tool_root.join(".apdr-cache")` as the last fallback only when no external cache dir is available. Update `ResolveConfig::for_tool_root` and the cache-command default path in `tools/apdr/src/main.rs` to call that helper instead of hardcoding `tool_root.join(".apdr-cache")`. If needed, add the `dirs` crate to `tools/apdr/Cargo.toml`. Add tests with exact names `phase21_1_cache_prefers_apdr_cache_dir` and `phase21_1_cache_uses_external_default_when_env_unset`, and make them pass via `cargo test --manifest-path tools/apdr/Cargo.toml phase21_1_cache_ -- --nocapture`.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase21_1_cache_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/lib.rs` contains `default_apdr_cache_path`
    - `tools/apdr/src/lib.rs` contains `APDR_CACHE_DIR`
    - `tools/apdr/src/main.rs` contains `default_apdr_cache_path`
    - `tools/apdr/Cargo.toml` contains `dirs`
    - `tools/apdr/src/lib.rs` contains `phase21_1_cache_prefers_apdr_cache_dir`
    - `tools/apdr/src/lib.rs` contains `phase21_1_cache_uses_external_default_when_env_unset`
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase21_1_cache_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>APDR cache defaults no longer force the repo tree to absorb runtime cache growth when an external cache location is available.</done>
</task>

<task type="auto">
  <name>Task 2: Add a supported footprint cleanup helper and external-target build default</name>
  <files>tools/apdr/build.sh, tools/apdr/README.md, scripts/cleanup-apdr-footprint.sh</files>
  <read_first>tools/apdr/build.sh, tools/apdr/README.md, scripts/cleanup-envs.sh, .planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-RESEARCH.md</read_first>
  <action>Create `scripts/cleanup-apdr-footprint.sh` with exact flags `--dry-run`, `--apply`, `--cache-path`, and `--target-dir`. The script must default to `--dry-run`, report byte counts for the resolved cache path, `tools/apdr/target`, and any `tools/apdr/target-*` directories, and require `--apply` before deleting anything. In apply mode, it must run `cargo clean --manifest-path tools/apdr/Cargo.toml --target-dir "$TARGET_DIR"` when the target dir exists, remove extra local `tools/apdr/target-*` directories if present, and run `cargo run --manifest-path tools/apdr/Cargo.toml -- cache --cache-path "$CACHE_PATH" prune --max-validated-envs 8 --max-validated-env-gb 1 --max-wheelhouse-gb 1`. Update `tools/apdr/build.sh` so it exports `CARGO_TARGET_DIR=\"${APDR_TARGET_DIR:-$HOME/.cache/apdr/target}\"` before `cargo build --release` and prints the resolved target dir. Update `tools/apdr/README.md` to document `APDR_CACHE_DIR`, `APDR_TARGET_DIR`, and the cleanup helper under the cache/build sections.</action>
  <verify>/bin/zsh -lc "bash scripts/cleanup-apdr-footprint.sh --help"</verify>
  <acceptance_criteria>
    - `scripts/cleanup-apdr-footprint.sh` contains `--dry-run`
    - `scripts/cleanup-apdr-footprint.sh` contains `--apply`
    - `scripts/cleanup-apdr-footprint.sh` contains `cargo clean --manifest-path tools/apdr/Cargo.toml`
    - `scripts/cleanup-apdr-footprint.sh` contains `cache prune --max-validated-envs 8 --max-validated-env-gb 1 --max-wheelhouse-gb 1`
    - `tools/apdr/build.sh` contains `APDR_TARGET_DIR`
    - `tools/apdr/build.sh` contains `CARGO_TARGET_DIR`
    - `tools/apdr/README.md` contains `APDR_CACHE_DIR`
    - `tools/apdr/README.md` contains `cleanup-apdr-footprint.sh`
    - `bash scripts/cleanup-apdr-footprint.sh --help` exits 0
  </acceptance_criteria>
  <done>Developers now have a supported way to keep APDR cache and build outputs from quietly consuming the repo tree.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml phase21_1_cache_ -- --nocapture`
- [ ] `bash scripts/cleanup-apdr-footprint.sh --help`
- [ ] `rg -n 'default_apdr_cache_path|APDR_CACHE_DIR|APDR_TARGET_DIR|cleanup-apdr-footprint' tools/apdr/Cargo.toml tools/apdr/src/lib.rs tools/apdr/src/main.rs tools/apdr/build.sh tools/apdr/README.md scripts/cleanup-apdr-footprint.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- APDR cache defaults avoid in-repo growth when an external cache dir is available
- The repo has a supported cleanup path for local APDR cache and target bytes

</success_criteria>

<output>
After completion, create `.planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-02-SUMMARY.md`
</output>
