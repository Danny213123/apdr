---
phase: 21.1-repository-footprint-and-download-size-reduction
plan: 02
subsystem: apdr-cache-and-build
tags: [rust, shell, apdr, cache, cleanup]
requires: [21.1-01]
provides:
  - "Shared external-first APDR cache default across runtime, cache commands, and learned-family persistence"
  - "Focused Phase 21.1 cache regression tests in `tools/apdr/tests/test_cache.rs`"
  - "Supported APDR cleanup helper plus external-target build default documentation"
affects: [phase-21.1-plan-03, apdr-cli, local-footprint]
tech-stack:
  added:
    - "dirs"
  patterns:
    - "All default APDR cache lookups route through one helper before any command-specific override"
    - "Cleanup tooling defaults to reporting first and requires explicit apply mode for destructive actions"
key-files:
  created:
    - scripts/cleanup-apdr-footprint.sh
  modified:
    - tools/apdr/Cargo.toml
    - tools/apdr/Cargo.lock
    - tools/apdr/src/lib.rs
    - tools/apdr/src/main.rs
    - tools/apdr/src/resolver/family_knowledge/learned.rs
    - tools/apdr/tests/test_cache.rs
    - tools/apdr/build.sh
    - tools/apdr/README.md
key-decisions:
  - "Use `dirs::cache_dir().join(\"apdr\")` as the shared default cache root when `APDR_CACHE_DIR` is unset."
  - "Keep the build helper's default external target dir explicit as `$HOME/.cache/apdr/target` while still honoring `APDR_TARGET_DIR`."
patterns-established:
  - "Phase-prefixed cache tests lock environment-variable precedence under a serialized env lock."
  - "Cleanup scripts report byte totals up front and refuse destructive behavior without `--apply`."
requirements-completed: [DSK-02]
duration: 26 min
completed: 2026-04-01
---

# Phase 21.1 Plan 02: Repository Footprint and Download Size Reduction Summary

**APDR now prefers an external per-user cache root across its runtime entry points, and the repo includes a supported cleanup path for large cache and target directories.**

## Performance

- **Duration:** 26 min
- **Completed:** 2026-04-01
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments

- Added `default_apdr_cache_path` in [lib.rs](/Users/dannyguan/Documents/fse-aiware-python-dependencies/tools/apdr/src/lib.rs) and routed `ResolveConfig`, cache commands, `classify-log`, and learned-family persistence through it.
- Added focused `phase21_1_cache_` regressions in [test_cache.rs](/Users/dannyguan/Documents/fse-aiware-python-dependencies/tools/apdr/tests/test_cache.rs) to lock `APDR_CACHE_DIR` precedence, external-default behavior, and learned-family path alignment.
- Updated [build.sh](/Users/dannyguan/Documents/fse-aiware-python-dependencies/tools/apdr/build.sh) to default Cargo output to `APDR_TARGET_DIR` or `$HOME/.cache/apdr/target`, added [cleanup-apdr-footprint.sh](/Users/dannyguan/Documents/fse-aiware-python-dependencies/scripts/cleanup-apdr-footprint.sh), and documented the new cache/build controls in [README.md](/Users/dannyguan/Documents/fse-aiware-python-dependencies/tools/apdr/README.md).

## Task Commits

1. **Task 1: Centralize cache defaults and add cache tests** - `5bbfe39`
2. **Task 2: Add cleanup helper and external-target build default** - `5362896`

## Verification

- `CARGO_TARGET_DIR="$HOME/.cache/apdr/target" cargo test --manifest-path tools/apdr/Cargo.toml --test test_cache phase21_1_cache_ -- --nocapture`
- `bash scripts/cleanup-apdr-footprint.sh --help`
- `rg -n 'default_apdr_cache_path|APDR_CACHE_DIR|APDR_TARGET_DIR|cleanup-apdr-footprint|phase21_1_cache_' tools/apdr/Cargo.toml tools/apdr/src/lib.rs tools/apdr/src/main.rs tools/apdr/src/resolver/family_knowledge/learned.rs tools/apdr/tests/test_cache.rs tools/apdr/build.sh tools/apdr/README.md scripts/cleanup-apdr-footprint.sh`

## Deviations from Plan

### Auto-fixed Issues

**1. Added the `dirs` dependency during verification rather than pre-edit setup**
- **Found during:** Task 1 verification
- **Issue:** The new shared cache helper needed `dirs::cache_dir()`, which introduced a fresh dependency and required one network-backed Cargo resolution pass.
- **Fix:** Added `dirs` to APDR's manifest, let Cargo update the lockfile, and re-ran the focused test target against an external `CARGO_TARGET_DIR`.
- **Impact:** No scope drift. The dependency is directly tied to the planned helper behavior.

---

**Total deviations:** 1 auto-fixed

## Issues Encountered

- The focused Cargo test emitted the pre-existing dead-code warnings in `tools/apdr/src/resolver/targeted_recovery.rs`; they remained non-blocking and out of scope for this plan.

## Next Phase Readiness

- Plan 03 can now measure a candidate state where tracked source bloat is gone and future APDR cache/build growth defaults away from the repo tree.
- The cleanup helper gives the proof note a concrete operator path instead of pointing reviewers at ad hoc manual deletions.

## Self-Check: PASSED

- Found `.planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-02-SUMMARY.md`
- Found task commits `5bbfe39` and `5362896`

---
*Phase: 21.1-repository-footprint-and-download-size-reduction*
*Completed: 2026-04-01*
