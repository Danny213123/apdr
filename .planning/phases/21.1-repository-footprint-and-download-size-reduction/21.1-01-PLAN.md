---
phase: 21.1-repository-footprint-and-download-size-reduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/apdr/.gitignore
  - scripts/check_phase21_1_footprint.py
autonomous: true
requirements:
  - DSK-01
user_setup: []
must_haves:
  truths:
    - Fresh source checkouts and GitHub downloads must stop including tracked APDR Cargo output trees.
    - Ignore coverage must catch custom APDR target directories such as `target-fix-*` and `target-test-*`, not just the default `target/`.
    - The repo must gain an automated tracked-artifact guard so generated APDR build output cannot quietly re-enter source control.
  artifacts:
    - path: "tools/apdr/.gitignore"
      provides: "APDR-local ignore policy that blocks tracked target-style directories"
      contains: "target*/"
    - path: "scripts/check_phase21_1_footprint.py"
      provides: "Tracked-artifact checker for Phase 21.1 source-distribution regressions"
      contains: "--mode tracked"
  key_links:
    - from: "tools/apdr/.gitignore"
      to: ".planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-CONTEXT.md"
      via: "Phase 21.1 explicitly calls out tracked `tools/apdr/target-*` directories as first-class scope."
      pattern: "Tracked generated build outputs under `tools/apdr/target-*`"
    - from: "scripts/check_phase21_1_footprint.py"
      to: ".planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-RESEARCH.md"
      via: "Research requires a regression guard so target-style directories do not re-enter source control after cleanup."
      pattern: "tracked-artifact guard"
---

<objective>
Remove tracked APDR build-output bloat from source distribution and lock a regression guard around it.

Purpose: The current repo ships generated Cargo target trees under `tools/apdr/target-*`, which makes fresh checkouts and GitHub downloads larger than they should be even though APDR already documents those paths as local build artifacts. This plan removes the tracked offenders, broadens ignore coverage, and adds an automated guard that later proof work can build on.
Output: cleaned Git tracking for APDR target-style directories, stronger ignore coverage, and a Phase 21.1 tracked-artifact checker.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-CONTEXT.md
@.planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-RESEARCH.md
@.planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-VALIDATION.md
@tools/apdr/.gitignore
@tools/apdr/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Broaden APDR ignore coverage and untrack generated target-style directories</name>
  <files>tools/apdr/.gitignore</files>
  <read_first>tools/apdr/.gitignore, .planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-CONTEXT.md, .planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-RESEARCH.md</read_first>
  <action>In `tools/apdr/.gitignore`, keep the existing `target/` rule and add the exact pattern `target*/` so custom target directories such as `target-fix-build`, `target-fix-rerun`, `target-fix-rerun2`, `target-fix-verify`, `target-test-cli`, `target-test-johnny`, `target-test-mecab`, `target-test-presolve`, `target-test-presolve-lib`, `target-test-presolve-unsat2`, and `target-test-serial` are all ignored. Then remove those exact directories from Git tracking with `git rm -r --cached --ignore-unmatch ...` so `git ls-files 'tools/apdr/target*'` becomes empty while local copies can still exist until the user cleans them.</action>
  <verify>/bin/zsh -lc "python3 scripts/check_phase21_1_footprint.py --repo-root . --mode tracked --status-json /tmp/phase21_1-tracked-status.json --probe-only"</verify>
  <acceptance_criteria>
    - `tools/apdr/.gitignore` contains `target*/`
    - `git ls-files 'tools/apdr/target*'` prints no paths
    - `python3 scripts/check_phase21_1_footprint.py --repo-root . --mode tracked --status-json /tmp/phase21_1-tracked-status.json --probe-only` exits 0
  </acceptance_criteria>
  <done>Tracked APDR target-style directories are no longer part of source distribution, and future ones are ignored by default.</done>
</task>

<task type="auto">
  <name>Task 2: Add a deterministic tracked-artifact guard for source-distribution regressions</name>
  <files>scripts/check_phase21_1_footprint.py</files>
  <read_first>scripts/check_phase20_recovery_delta.py, .planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-RESEARCH.md, tools/apdr/.gitignore</read_first>
  <action>Create `scripts/check_phase21_1_footprint.py` with an exact mode flag `--mode tracked`. In tracked mode, the script must inspect the repo root, fail if `git ls-files 'tools/apdr/target*'` or `git ls-files 'tools/apdr/.apdr-cache*'` returns any path, and write a JSON status file containing these exact top-level keys: `mode`, `tracked_target_paths`, `tracked_target_bytes`, `tracked_cache_paths`, and `ok`. Also make the script support `--repo-root`, `--status-json`, and `--probe-only` so later plans can extend the same checker instead of replacing it.</action>
  <verify>/bin/zsh -lc "python3 scripts/check_phase21_1_footprint.py --repo-root . --mode tracked --status-json /tmp/phase21_1-tracked-status.json --probe-only"</verify>
  <acceptance_criteria>
    - `scripts/check_phase21_1_footprint.py` contains `--mode`
    - `scripts/check_phase21_1_footprint.py` contains `tracked_target_paths`
    - `scripts/check_phase21_1_footprint.py` contains `tracked_target_bytes`
    - `scripts/check_phase21_1_footprint.py` contains `--probe-only`
    - `python3 scripts/check_phase21_1_footprint.py --repo-root . --mode tracked --status-json /tmp/phase21_1-tracked-status.json --probe-only` exits 0
  </acceptance_criteria>
  <done>The repo now has an automated source-distribution guard that can fail if tracked APDR build artifacts return.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 scripts/check_phase21_1_footprint.py --repo-root . --mode tracked --status-json /tmp/phase21_1-tracked-status.json --probe-only`
- [ ] `rg -n 'target\\*/|tracked_target_paths|tracked_target_bytes|probe-only' tools/apdr/.gitignore scripts/check_phase21_1_footprint.py`
- [ ] `git ls-files 'tools/apdr/target*'`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Fresh source distribution no longer includes tracked APDR target-style build outputs
- Phase 21.1 has an automated guard against tracked-artifact regression

</success_criteria>

<output>
After completion, create `.planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-01-SUMMARY.md`
</output>
