---
phase: 21.1-repository-footprint-and-download-size-reduction
verified: 2026-04-02T00:24:11Z
status: passed
score: 3/3 must-haves verified
re_verification: false
---

# Phase 21.1: Repository Footprint and Download Size Reduction Verification Report

**Phase Goal:** The repo becomes materially lighter to download and keep locally by removing or relocating heavyweight tool artifacts, especially under `tools/apdr`, without breaking normal APDR development flows.
**Verified:** 2026-04-02T00:24:11Z
**Status:** passed
**Re-verification:** No - initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Fresh source checkouts no longer include tracked APDR `target-*` build outputs | VERIFIED | `git ls-files 'tools/apdr/target*'` is empty, `tools/apdr/.gitignore` now contains `target*/`, and `21.1-footprint-proof-status.json` reports `source_delta_bytes: -5551066900` |
| 2 | APDR now defaults cache and build growth away from the repo tree and exposes a supported cleanup path for existing local bloat | VERIFIED | `tools/apdr/src/lib.rs`, `tools/apdr/src/main.rs`, and `tools/apdr/src/resolver/family_knowledge/learned.rs` now share `default_apdr_cache_path`; `tools/apdr/build.sh` defaults Cargo output to `APDR_TARGET_DIR`; `scripts/cleanup-apdr-footprint.sh` provides explicit dry-run and apply flows |
| 3 | Phase 21.1 ships a deterministic proof package that distinguishes source-distributed bytes from local cache and target bytes | VERIFIED | `scripts/check_phase21_1_footprint.py` now supports `tracked`, `local`, and `delta` modes; the baseline/candidate artifact pair and `21.1-FOOTPRINT-DELTA.md` preserve separate tracked-source, cache, target, and tool-tree metrics |

**Score:** 3/3 must-haves verified

## Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `tools/apdr/.gitignore` | Ignore policy that covers custom APDR target directories | VERIFIED | Contains both `target/` and `target*/` |
| `scripts/check_phase21_1_footprint.py` | Deterministic tracked/local/delta checker for Phase 21.1 | VERIFIED | Supports `--mode tracked`, `--mode local`, `--mode delta`, plus the required delta output fields |
| `tools/apdr/src/lib.rs` | Shared external-first APDR cache default | VERIFIED | Exposes `default_apdr_cache_path` and uses it in `ResolveConfig::for_tool_root` |
| `tools/apdr/src/main.rs` | Cache commands and classify-log use the shared cache default | VERIFIED | `classify_log_command` and `parse_cache_args` now call `default_apdr_cache_path` |
| `tools/apdr/src/resolver/family_knowledge/learned.rs` | Learned-family persistence uses the shared cache default | VERIFIED | `learned_families_path()` now joins `learned_families.json` onto `default_apdr_cache_path(...)` |
| `tools/apdr/tests/test_cache.rs` | Focused Phase 21.1 cache regressions | VERIFIED | `phase21_1_cache_` tests all pass |
| `tools/apdr/build.sh` | External-target build default | VERIFIED | Exports `CARGO_TARGET_DIR="${APDR_TARGET_DIR:-$HOME/.cache/apdr/target}"` |
| `tools/apdr/README.md` | Operator-facing docs for cache/build defaults and cleanup helper | VERIFIED | Documents `APDR_CACHE_DIR`, `APDR_TARGET_DIR`, and `cleanup-apdr-footprint.sh` |
| `scripts/cleanup-apdr-footprint.sh` | Supported cleanup helper | VERIFIED | Supports `--dry-run`, `--apply`, `--cache-path`, and `--target-dir`; the proof run removed `14.0GiB` from the repo-local APDR cache |
| `21.1-footprint-baseline.json` | Stable pre-fix footprint artifact | VERIFIED | Preserves the saved tracked-source baseline plus pre-cleanup local bytes |
| `21.1-footprint-candidate.json` | Stable post-fix footprint artifact | VERIFIED | Shows `tracked_target_paths: []`, `local_target_bytes: 0`, and the reduced cache/tool-tree bytes |
| `21.1-footprint-proof-status.json` | Final delta gate output | VERIFIED | Reports negative deltas for source, cache, and target bytes with `ok: true` |
| `21.1-FOOTPRINT-DELTA.md` | Reviewer-facing footprint note | VERIFIED | Includes `## Source Distribution`, `## Local Footprint`, `## Cleanup Path`, and `## Caveats` |

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Tracked APDR source-bloat guard stays green | `python3 scripts/check_phase21_1_footprint.py --repo-root . --mode tracked --status-json /tmp/phase21_1-tracked-status.json --probe-only` | Exit code `0` | PASS |
| Phase 21.1 cache-path regressions stay green | `CARGO_TARGET_DIR="$HOME/.cache/apdr/target" cargo test --manifest-path tools/apdr/Cargo.toml --test test_cache phase21_1_cache_ -- --nocapture` | `3` tests passed, `0` failed | PASS |
| Cleanup helper contract is available | `bash scripts/cleanup-apdr-footprint.sh --help` | Exit code `0` | PASS |
| Cleanup helper can reclaim repo-local APDR bytes without rebuilding inside the repo tree | `bash scripts/cleanup-apdr-footprint.sh --apply --cache-path tools/apdr/.apdr-cache --target-dir /tmp/phase21_1-apdr-target` | Removed `182292` files / `13.9GiB`; cache fell from `16422084455` bytes to `1381115464` bytes | PASS |
| Final footprint delta gate stays green | `python3 scripts/check_phase21_1_footprint.py --repo-root . --mode delta --baseline-json .planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-footprint-baseline.json --candidate-json .planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-footprint-candidate.json --status-json .planning/phases/21.1-repository-footprint-and-download-size-reduction/21.1-footprint-proof-status.json --probe-only` | Exit code `0`; `source_delta_bytes=-5551066900`, `cache_delta_bytes=-15041748090`, `target_delta_bytes=-20451258961` | PASS |

## Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| DSK-01 | 21.1-01, 21.1-03 | Fresh source checkouts and GitHub downloads should not include heavyweight APDR build artifacts | SATISFIED | `git ls-files 'tools/apdr/target*'` is empty and the delta checker records `source_delta_bytes: -5551066900` |
| DSK-02 | 21.1-02, 21.1-03 | Developers should be able to avoid or reclaim large local APDR cache and build directories | SATISFIED | Shared external cache/build defaults landed, the cleanup helper is documented, and the proof candidate reduced `local_cache_bytes` and `local_target_bytes` sharply |

## Human Verification Required

No human gate blocks Phase 21.1 completion. The proof package is already grounded in the live local working tree and a saved pre-fix tracked snapshot.

## Gaps Summary

No Phase 21.1 execution gaps remain. The cleanup proof does not rewrite old Git history, so `.git` size remains essentially unchanged; that is an explicit caveat, not an unresolved blocker.

---

_Verified: 2026-04-02T00:24:11Z_
_Verifier: Codex inline execution_
