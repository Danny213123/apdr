# Phase 21.1: Repository Footprint and Download Size Reduction - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-01
**Phase:** 21.1-repository-footprint-and-download-size-reduction
**Areas discussed:** Footprint target, Reduction strategy, Priority area, Delivery shape

---

## Footprint target

| Option | Description | Selected |
|--------|-------------|----------|
| Source-download size only | Focus only on what GitHub or a fresh checkout downloads | |
| Local generated size only | Focus only on build/cache directories that grow after local use | |
| Both source and local footprint | Reduce both repo-distributed bytes and large local generated bytes | ✓ |

**User's choice:** Both source and local footprint
**Notes:** Inferred from the user's request that the project is too large both on GitHub download and in local disk usage.

---

## Reduction strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Docs-only cleanup guidance | Add instructions telling developers what to delete manually | |
| Structural/default reduction first | Remove tracked generated artifacts and change defaults so the repo stays smaller by default | ✓ |
| History rewrite first | Treat Git history surgery as the primary solution immediately | |

**User's choice:** Structural/default reduction first
**Notes:** The user asked to add a real phase for reducing footprint, so the recommended default is structural reduction rather than only cleanup advice.

---

## Priority area

| Option | Description | Selected |
|--------|-------------|----------|
| Whole repo broad sweep | Start by auditing every directory evenly | |
| `tools/` first | Focus first on the tool tree that currently dominates the footprint | ✓ |
| Non-tool assets first | Start with docs, images, and benchmark outputs outside `tools/` | |

**User's choice:** `tools/` first
**Notes:** Directly aligned with the user's complaint that `tools/` is taking a lot of gigs.

---

## Delivery shape

| Option | Description | Selected |
|--------|-------------|----------|
| One-time cleanup only | Shrink the current tree once, but do not change future defaults | |
| Structural reduction plus supported cleanup path | Keep future checkouts/builds smaller and provide a safe reclaim path for local heavy outputs | ✓ |
| Defer cleanup tooling | Focus only on tracked repo bytes and leave local cache cleanup for later | |

**User's choice:** Structural reduction plus supported cleanup path
**Notes:** Recommended default chosen so the phase addresses both ongoing local growth and source-distributed size.

---

## Evidence captured during discussion

- `tools` is about `29G` locally.
- `tools/apdr/.apdr-cache` is about `15G`.
- `tools/apdr/target` is about `6.7G`.
- Several `tools/apdr/target-fix-*` and `tools/apdr/target-test-*` directories are tracked in Git and inflate source-download size.

## the agent's Discretion

- Exact mechanism for removing tracked build outputs from source distribution.
- Exact supported cleanup or relocation UX for large APDR caches and build directories.
- Exact proof format for before/after footprint evidence.

## Deferred Ideas

- Full Git-history rewrite or hosted artifact migration if current-tree cleanup is not enough.
- Docker-first routing work, which remains the downstream v2.4 scope after this inserted phase.
