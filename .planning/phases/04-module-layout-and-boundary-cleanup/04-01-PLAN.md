---
phase: 04-module-layout-and-boundary-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/apdr/src/resolver/mod.rs
  - tools/apdr/src/resolver/retry_loop.rs
  - tools/apdr/src/resolver/recovery_diagnostics.rs
  - tools/apdr/src/resolver/artifacts.rs
  - tools/apdr/tests/test_resolver.rs
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-05
user_setup: []
must_haves:
  truths:
    - `tools/apdr/src/resolver/mod.rs` is reduced to a readable orchestration layer instead of containing retry mutation, diagnostics, and artifact-writing internals inline.
    - Resolver retry-loop behavior still flows through `resolve_path(...)` and remains covered by the existing resolver test suite after the file split.
    - Reviewers can follow the main resolver control flow without tracing thousands of lines of helper implementations in one file.
  artifacts:
    - path: "tools/apdr/src/resolver/retry_loop.rs"
      provides: "Dedicated home for retry-loop mutation, dependency updates, and requirement rendering helpers"
      contains: "fn validate_with_retries("
    - path: "tools/apdr/src/resolver/recovery_diagnostics.rs"
      provides: "Dedicated home for failure-signature, extraction, and recovery-diagnostics helpers"
      contains: "fn failure_signature("
    - path: "tools/apdr/src/resolver/artifacts.rs"
      provides: "Dedicated home for parse, solver, and iteration artifact writers"
      contains: "fn write_iteration_snapshot("
  key_links:
    - from: "tools/apdr/src/resolver/mod.rs"
      to: "tools/apdr/src/resolver/retry_loop.rs"
      via: "the top-level resolver entrypoint should delegate retry mutation and dependency-state edits into the retry-loop module"
      pattern: "validate_with_retries"
    - from: "tools/apdr/src/resolver/mod.rs"
      to: "tools/apdr/src/resolver/recovery_diagnostics.rs"
      via: "recovery-specific parsing and failure metadata should leave the entrypoint file"
      pattern: "failure_signature"
    - from: "tools/apdr/src/resolver/mod.rs"
      to: "tools/apdr/src/resolver/artifacts.rs"
      via: "artifact persistence and formatting should sit behind a dedicated module boundary"
      pattern: "write_iteration_snapshot"
---

<objective>
Split the monolithic resolver entrypoint into reviewable submodules while keeping `resolve_path(...)` behavior stable.

Purpose: `tools/apdr/src/resolver/mod.rs` is the largest active Rust file in the codebase and still mixes orchestration, retry mutation, recovery diagnostics, and artifact writing in one place.
Output: A smaller `resolver/mod.rs`, three focused sibling modules, and targeted resolver verification that proves the structural refactor did not break behavior.
</objective>

<execution_context>
@C:\Users\danny\.codex\get-shit-done\workflows\execute-plan.md
@C:\Users\danny\.codex\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\apdr\.planning\PROJECT.md
@D:\apdr\.planning\ROADMAP.md
@D:\apdr\.planning\STATE.md
@D:\apdr\.planning\phases\04-module-layout-and-boundary-cleanup\04-RESEARCH.md
@D:\apdr\.planning\phases\04-module-layout-and-boundary-cleanup\04-VALIDATION.md
@D:\apdr\.planning\phases\02-resolver-memory-and-algorithm-efficiency\02-02-SUMMARY.md
@D:\apdr\tools\apdr\src\resolver\mod.rs
@D:\apdr\tools\apdr\tests\test_resolver.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract retry-loop mutation into `resolver/retry_loop.rs`</name>
  <files>tools/apdr/src/resolver/mod.rs, tools/apdr/src/resolver/retry_loop.rs</files>
  <read_first>tools/apdr/src/resolver/mod.rs, tools/apdr/src/resolver/pre_solve.rs, tools/apdr/tests/test_resolver.rs</read_first>
  <action>In `tools/apdr/src/resolver/mod.rs`, add `mod retry_loop;` and move the retry-mutation cluster into a new file `tools/apdr/src/resolver/retry_loop.rs`. The moved cluster must include `validate_with_retries(...)`, `RetryLoopState`, `render_requirements(...)`, `render_requirements_if_dirty(...)`, `dependency_index_by_package(...)`, `dependency_index_by_import(...)`, `dedupe_dependencies(...)`, `update_package_version(...)`, `ensure_dependency(...)`, `try_build_failure_alternatives(...)`, `upsert_dependency(...)`, `apply_compatibility_overrides(...)`, and the `debug_*` helpers that directly exercise dependency mutation. Update `resolver/mod.rs` so `resolve_path(...)` imports and calls the new retry-loop module instead of keeping those implementations inline. Keep the current entrypoint signature and return types unchanged.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/resolver/mod.rs` contains `mod retry_loop;`
    - `tools/apdr/src/resolver/retry_loop.rs` contains `fn validate_with_retries(`
    - `tools/apdr/src/resolver/retry_loop.rs` contains `struct RetryLoopState`
    - `tools/apdr/src/resolver/retry_loop.rs` contains `fn render_requirements_if_dirty(`
    - `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>The resolver retry loop and dependency mutation helpers no longer live inline in the entrypoint file.</done>
</task>

<task type="auto">
  <name>Task 2: Extract recovery diagnostics and artifact writers into dedicated modules</name>
  <files>tools/apdr/src/resolver/mod.rs, tools/apdr/src/resolver/recovery_diagnostics.rs, tools/apdr/src/resolver/artifacts.rs</files>
  <read_first>tools/apdr/src/resolver/mod.rs, tools/apdr/src/resolver/family_knowledge.rs, tools/apdr/src/resolver/tier3_llm.rs</read_first>
  <action>In `tools/apdr/src/resolver/mod.rs`, add `mod recovery_diagnostics;` and `mod artifacts;`, then move the non-entrypoint helper clusters into those files. `tools/apdr/src/resolver/recovery_diagnostics.rs` must own `failure_signature(...)`, `apply_llm_recovery_hint(...)`, `update_failure_metadata(...)`, `learned_pattern_key(...)`, `environment_specific_note(...)`, `infer_validation_status(...)`, `infer_validation_reason(...)`, and the `extract_*` helpers that classify missing modules, syntax failures, build dependencies, dependency conflicts, and Python-version mismatches. `tools/apdr/src/resolver/artifacts.rs` must own `write_parse_artifacts(...)`, `write_solver_artifacts(...)`, `write_state_artifacts(...)`, `write_iteration_snapshot(...)`, `format_dependency_state(...)`, and `format_classifier(...)`. Update `resolver/mod.rs` imports so `resolve_path(...)` still calls those helpers through the new modules without changing output filenames or report content.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/resolver/mod.rs` contains `mod recovery_diagnostics;`
    - `tools/apdr/src/resolver/mod.rs` contains `mod artifacts;`
    - `tools/apdr/src/resolver/recovery_diagnostics.rs` contains `fn failure_signature(`
    - `tools/apdr/src/resolver/artifacts.rs` contains `fn write_iteration_snapshot(`
    - `tools/apdr/src/resolver/artifacts.rs` contains `fn write_solver_artifacts(`
    - `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>Resolver diagnostics and artifact persistence are separated from orchestration and retry mutation.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve resolver entrypoints and prove the top-level file is materially smaller</name>
  <files>tools/apdr/src/resolver/mod.rs, tools/apdr/tests/test_resolver.rs</files>
  <read_first>tools/apdr/src/resolver/mod.rs, tools/apdr/src/resolver/retry_loop.rs, tools/apdr/src/resolver/recovery_diagnostics.rs, tools/apdr/src/resolver/artifacts.rs</read_first>
  <action>Finish the split by making `tools/apdr/src/resolver/mod.rs` an orchestration file that retains `pub fn resolve_path(...)`, the existing module exports, and only the helper imports needed to wire the extracted modules together. If any resolver tests rely on moved private helpers, update `tools/apdr/tests/test_resolver.rs` to cover the public behavior through `resolve_path(...)` or the existing debug helpers instead of reaching back into the old file layout. Keep the post-split `resolver/mod.rs` under `2500` lines so the file-size reduction is explicit and reviewable.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture && cargo clippy --manifest-path tools/apdr/Cargo.toml --all-targets -- -D warnings</verify>
  <acceptance_criteria>
    - `tools/apdr/src/resolver/mod.rs` contains `pub fn resolve_path(`
    - `tools/apdr/src/resolver/mod.rs` does not contain `fn write_iteration_snapshot(`
    - `tools/apdr/src/resolver/mod.rs` does not contain `fn failure_signature(`
    - `@(Get-Content tools/apdr/src/resolver/mod.rs).Count` is less than `2500`
    - `cargo clippy --manifest-path tools/apdr/Cargo.toml --all-targets -- -D warnings` exits 0
    - `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>The resolver entrypoint stays stable, tests still pass, and the top-level file is no longer monolithic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture`
- [ ] `cargo clippy --manifest-path tools/apdr/Cargo.toml --all-targets -- -D warnings`
- [ ] `rg -n "mod retry_loop;|mod recovery_diagnostics;|mod artifacts;" tools/apdr/src/resolver/mod.rs`
- [ ] `@(Get-Content tools/apdr/src/resolver/mod.rs).Count`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Resolver orchestration, retry mutation, diagnostics, and artifacts live behind separate module boundaries
- `resolve_path(...)` remains the readable resolver entrypoint
- The split lowers review surface area without changing behavior
</success_criteria>

<output>
After completion, create `.planning/phases/04-module-layout-and-boundary-cleanup/04-01-SUMMARY.md`
</output>
