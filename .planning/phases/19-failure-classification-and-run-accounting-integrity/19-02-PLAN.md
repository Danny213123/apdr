---
phase: 19-failure-classification-and-run-accounting-integrity
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - benchmark_ui/runner.py
  - benchmark_ui/service.py
  - benchmark_ui/test_run_contract.py
  - benchmark_ui/test_runner_events.py
  - benchmark_ui/test_resume_accounting.py
autonomous: true
requirements:
  - EVD-07
  - EVD-09
  - VAL-04
user_setup: []
must_haves:
  truths:
    - Host-runtime and framework-runtime skips must never be reclassified as benchmark passes.
    - Resumed historical results must stay distinguishable from live results produced by the current run.
    - Operators still need a usable combined resume view, but proof and comparison tooling must have a live-only path.
  artifacts:
    - path: "benchmark_ui/runner.py"
      provides: "Resume storage that keeps historical rows separate from live run rows"
      contains: "historical_results"
    - path: "benchmark_ui/service.py"
      provides: "Reader helpers that expose result provenance and stop skip-to-pass upgrades"
      contains: "resultOrigin"
    - path: "benchmark_ui/test_resume_accounting.py"
      provides: "Contract coverage for Phase 19 resume accounting"
      contains: "test_resume_summary_keeps_historical_results_out_of_live_counts"
  key_links:
    - from: "benchmark_ui/service.py"
      to: ".planning/REQUIREMENTS.md"
      via: "Phase 19 explicitly forbids resumed-run summaries from marking host-runtime skips as successes."
      pattern: "skipped host-runtime cases as successes"
    - from: "benchmark_ui/runner.py"
      to: ".planning/phases/19-failure-classification-and-run-accounting-integrity/19-RESEARCH.md"
      via: "Historical rows are currently injected into the new run's `results` list, which contaminates live-only conclusions."
      pattern: "summary[\"results\"] = resume_results"
---

<objective>
Make benchmark accounting trustworthy by keeping skips as skips and keeping historical resume rows out of live-only conclusions.

Purpose: Even with better APDR classification, the benchmark layer still misreports some host-runtime skips as passes and currently mixes resumed historical rows straight into the new run's result list. This plan fixes both accounting issues while keeping the operator-facing resume experience intact.
Output: corrected skip/pass accounting, explicit result provenance, and benchmark reader tests that lock the new contract.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-failure-classification-and-run-accounting-integrity/19-RESEARCH.md
@.planning/phases/19-failure-classification-and-run-accounting-integrity/19-VALIDATION.md
@runs/20260330-020943-apdr/summary.json
@benchmark_ui/runner.py
@benchmark_ui/service.py
@benchmark_ui/test_run_contract.py
@benchmark_ui/test_runner_events.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove host-runtime skip-to-pass reclassification</name>
  <files>benchmark_ui/runner.py, benchmark_ui/service.py, benchmark_ui/test_run_contract.py, benchmark_ui/test_resume_accounting.py</files>
  <read_first>benchmark_ui/runner.py, benchmark_ui/service.py, benchmark_ui/test_run_contract.py, .planning/phases/19-failure-classification-and-run-accounting-integrity/19-RESEARCH.md</read_first>
  <action>Delete or replace the current special-case logic in `benchmark_ui/runner.py::_run_single`, `benchmark_ui/service.py::_result_succeeded`, and `benchmark_ui/service.py::_result_skipped` that turns host-runtime skips into passes when requirements exist and returncode is zero. Drive the new decision from Phase 19's explicit APDR classification fields when available, but preserve backward-compatible behavior for older runs by treating `skipped-host-runtime` and `host-runtime-required` as skips. Add regression tests that prove those cases remain `SKIP`, not `PASS`.</action>
  <verify>python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_resume_accounting</verify>
  <acceptance_criteria>
    - `benchmark_ui/runner.py` no longer contains a path that flips `skipped-host-runtime` to pass
    - `benchmark_ui/service.py` no longer treats `skipped-host-runtime` or `host-runtime-required` as success candidates
    - `benchmark_ui/test_resume_accounting.py` covers the host-runtime skip accounting contract
    - `python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_resume_accounting` exits 0
  </acceptance_criteria>
  <done>Benchmark results now keep host-runtime and framework-runtime validation outcomes in the skip bucket.</done>
</task>

<task type="auto">
  <name>Task 2: Separate historical resume rows from live rows and expose provenance</name>
  <files>benchmark_ui/runner.py, benchmark_ui/service.py, benchmark_ui/test_run_contract.py, benchmark_ui/test_runner_events.py, benchmark_ui/test_resume_accounting.py</files>
  <read_first>benchmark_ui/runner.py, benchmark_ui/service.py, benchmark_ui/test_runner_events.py, benchmark_ui/test_run_contract.py, .planning/phases/19-failure-classification-and-run-accounting-integrity/19-RESEARCH.md</read_first>
  <action>Change resume storage so a resumed run keeps the prior run's rows in a dedicated collection such as `historical_results` while `results` remains the live output of the current run. In `benchmark_ui/service.py`, add helpers that can build both a combined operational view and a live-only comparison view, and expose explicit provenance on case rows via a field such as `resultOrigin`. Update tests so resumed historical rows never appear as live case completions or live-only counts, while the operational combined view still reports resumed progress correctly.</action>
  <verify>python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_runner_events benchmark_ui.test_resume_accounting</verify>
  <acceptance_criteria>
    - `benchmark_ui/runner.py` contains `historical_results`
    - `benchmark_ui/service.py` contains `resultOrigin`
    - `benchmark_ui/test_resume_accounting.py` contains `test_resume_summary_keeps_historical_results_out_of_live_counts`
    - `benchmark_ui/test_runner_events.py` locks that resumed rows do not masquerade as newly emitted live case completions
    - `python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_runner_events benchmark_ui.test_resume_accounting` exits 0
  </acceptance_criteria>
  <done>Resumed runs now preserve provenance so operator views stay useful and proof/comparison logic can isolate current-run conclusions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_runner_events benchmark_ui.test_resume_accounting`
- [ ] `rg -n 'historical_results|resultOrigin|skipped-host-runtime|host-runtime-required' benchmark_ui/runner.py benchmark_ui/service.py benchmark_ui/test_run_contract.py benchmark_ui/test_runner_events.py benchmark_ui/test_resume_accounting.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Host-runtime skips no longer count as successes
- Resumed historical rows are distinguishable from live current-run rows

</success_criteria>

<output>
After completion, create `.planning/phases/19-failure-classification-and-run-accounting-integrity/19-02-SUMMARY.md`
</output>
