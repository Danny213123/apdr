---
phase: 19-failure-classification-and-run-accounting-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/apdr/src/resolver/recovery_diagnostics.rs
  - tools/apdr/src/resolver/retry_loop.rs
  - tools/apdr/src/lib.rs
  - tools/apdr/test_executor.py
autonomous: true
requirements:
  - VAL-04
user_setup: []
must_haves:
  truths:
    - APDR must preserve environment-specific validation outcomes as distinct from real dependency-resolution misses.
    - Classification truth must survive into emitted artifacts instead of staying trapped inside retry-loop heuristics.
    - Genuine dependency misses such as repeated missing modules must keep their failure semantics rather than being over-skipped.
  artifacts:
    - path: "tools/apdr/src/resolver/recovery_diagnostics.rs"
      provides: "Central failure-family classification for environment-specific versus dependency-resolution outcomes"
      contains: "classify_failure_family"
    - path: "tools/apdr/src/lib.rs"
      provides: "Summary/report fields that surface Phase 19 classification truth"
      contains: "failure_family"
    - path: "tools/apdr/test_executor.py"
      provides: "Serialized artifact keys for failure family, failure bucket, and skip candidacy"
      contains: "failure_family"
  key_links:
    - from: "tools/apdr/src/resolver/recovery_diagnostics.rs"
      to: ".planning/REQUIREMENTS.md"
      via: "Phase 19 is specifically about separating environment-specific failures from dependency-resolution misses."
      pattern: "environment-specific cases are not counted as generic mapping misses"
    - from: "tools/apdr/src/resolver/retry_loop.rs"
      to: ".planning/phases/18-backend-escalation-and-path-truth/18-CONTEXT.md"
      via: "Phase 19 must preserve, not overwrite, the routed backend truth that Phase 18 already established."
      pattern: "surface actual route truth separately"
---

<objective>
Make APDR's own output say clearly whether a validation failure is environment-specific or a real dependency-resolution miss.

Purpose: The retry loop already knows when the host, framework runtime, or platform makes validation impossible, but that truth is not durable enough once artifacts cross into benchmark accounting. This plan locks the classification core first so later benchmark changes can consume direct truth instead of heuristics.
Output: summary-level classification fields, serialized artifact metadata, and Rust regression coverage for the critical boundaries.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-failure-classification-and-run-accounting-integrity/19-RESEARCH.md
@.planning/phases/19-failure-classification-and-run-accounting-integrity/19-VALIDATION.md
@runs/20260330-020943-apdr/summary.json
@tools/apdr/src/resolver/mod.rs
@tools/apdr/src/resolver/retry_loop.rs
@tools/apdr/src/resolver/recovery_diagnostics.rs
@tools/apdr/src/lib.rs
@tools/apdr/test_executor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a durable failure-family classification and serialize it</name>
  <files>tools/apdr/src/resolver/recovery_diagnostics.rs, tools/apdr/src/resolver/retry_loop.rs, tools/apdr/src/lib.rs, tools/apdr/test_executor.py</files>
  <read_first>tools/apdr/src/resolver/recovery_diagnostics.rs, tools/apdr/src/resolver/retry_loop.rs, tools/apdr/src/lib.rs, tools/apdr/test_executor.py, .planning/phases/19-failure-classification-and-run-accounting-integrity/19-RESEARCH.md</read_first>
  <action>In `tools/apdr/src/resolver/recovery_diagnostics.rs`, add a helper such as `classify_failure_family(...)` that derives a higher-level family from the validation summary and retry-loop signals. It should distinguish at least environment-specific or framework-runtime outcomes from dependency-resolution failures without destroying the existing `validation_status` and `failure_bucket` detail. Thread that family into `tools/apdr/src/lib.rs` summary/report output, and extend `tools/apdr/test_executor.py` to serialize stable keys such as `failure_family`, `failure_bucket`, and `skip_candidate` so benchmark readers can consume direct classification truth.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase19_classification_</verify>
  <acceptance_criteria>
    - `tools/apdr/src/resolver/recovery_diagnostics.rs` contains `classify_failure_family`
    - `tools/apdr/src/lib.rs` contains `failure_family`
    - `tools/apdr/test_executor.py` contains `failure_family`
    - environment-specific APDR outcomes still preserve the existing routed backend fields from Phase 18
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase19_classification_` exits 0
  </acceptance_criteria>
  <done>APDR artifacts now say whether a failure is environment-specific or dependency-resolution-related instead of relying only on raw status strings.</done>
</task>

<task type="auto">
  <name>Task 2: Lock classification boundaries with Rust regression tests</name>
  <files>tools/apdr/src/resolver/recovery_diagnostics.rs, tools/apdr/src/lib.rs</files>
  <read_first>tools/apdr/src/resolver/recovery_diagnostics.rs, tools/apdr/src/lib.rs, runs/20260330-020943-apdr/summary.json, .planning/phases/19-failure-classification-and-run-accounting-integrity/19-RESEARCH.md</read_first>
  <action>Add Rust tests prefixed `phase19_classification_` that cover at least these boundaries: a host-runtime or hardware-required skip remains environment-specific, a framework-runtime requirement remains environment-specific, a repeated missing-module case remains dependency-resolution, and a genuine build failure keeps its dependency-resolution semantics unless the retry loop explicitly marked it as environment-specific. Include at least one report or summary-lines assertion so the new classification fields are visible outside the internal structs.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase19_classification_</verify>
  <acceptance_criteria>
    - `tools/apdr/src/resolver/recovery_diagnostics.rs` contains tests prefixed `phase19_classification_`
    - at least one `phase19_classification_` test covers an environment-specific skip
    - at least one `phase19_classification_` test covers a real dependency miss that stays a failure
    - `tools/apdr/src/lib.rs` exposes the new classification field in report text or summary lines
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase19_classification_` exits 0
  </acceptance_criteria>
  <done>Phase 19 classification semantics are locked before benchmark accounting starts consuming them.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml phase19_classification_`
- [ ] `rg -n 'classify_failure_family|failure_family|failure_bucket|skip_candidate|phase19_classification_' tools/apdr/src/resolver/recovery_diagnostics.rs tools/apdr/src/resolver/retry_loop.rs tools/apdr/src/lib.rs tools/apdr/test_executor.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- APDR artifacts clearly distinguish environment-specific failures from dependency-resolution misses
- Phase 18 backend-path truth remains intact alongside the new classification fields

</success_criteria>

<output>
After completion, create `.planning/phases/19-failure-classification-and-run-accounting-integrity/19-01-SUMMARY.md`
</output>
