---
phase: 18-backend-escalation-and-path-truth
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - tools/apdr/src/lib.rs
  - tools/apdr/src/resolver/retry_loop.rs
  - tools/apdr/src/docker/builder/env_backend.rs
  - tools/apdr/test_executor.py
  - benchmark_ui/runner.py
  - benchmark_ui/service.py
  - benchmark_ui/test_runner_events.py
  - benchmark_ui/test_run_contract.py
autonomous: true
requirements:
  - VAL-01
  - VAL-02
user_setup: []
must_haves:
  truths:
    - Top-level `validation_backend` must remain the configured run mode for comparability, even when actual validation hops change underneath.
    - Saved artifacts and benchmark readers must expose the actual backend path separately so operators can see `env`, `docker`, and `llm-agent` truth.
    - The new path data must survive the Rust-to-Python boundary and benchmark event ingestion without breaking existing success accounting.
  artifacts:
    - path: "tools/apdr/src/lib.rs"
      provides: "Summary-level path field distinct from the configured backend"
      contains: "validation_path"
    - path: "tools/apdr/test_executor.py"
      provides: "Serialized output fields for configured backend, escalated backend, and actual path"
      contains: "validationPath"
    - path: "benchmark_ui/test_run_contract.py"
      provides: "Reader contract coverage for backend-path truth"
      contains: "validationPath"
  key_links:
    - from: "tools/apdr/src/lib.rs"
      to: ".planning/phases/13-measurement-and-run-contract-hardening/13-MEASUREMENT-CONTRACT.md"
      via: "Phase 13 keeps configured backend labels stable for comparisons, so path truth must be modeled separately."
      pattern: "configured backend"
    - from: "benchmark_ui/service.py"
      to: ".planning/phases/18-backend-escalation-and-path-truth/18-CONTEXT.md"
      via: "Phase 18 explicitly separates requested mode from actual route truth."
      pattern: "top-level `validation_backend` stays as the requested run mode"
---

<objective>
Expose actual backend path truth without sacrificing the configured-mode contract that benchmark comparisons rely on.

Purpose: Once Phase 18 routing changes land, saved artifacts and UI readers need to show what actually happened instead of flattening everything back to `llm`. This plan adds that path surface and locks it through the wrapper and benchmark reader tests.
Output: new summary path metadata, serialized route truth, and benchmark reader coverage.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-backend-escalation-and-path-truth/18-CONTEXT.md
@.planning/phases/18-backend-escalation-and-path-truth/18-RESEARCH.md
@.planning/phases/18-backend-escalation-and-path-truth/18-VALIDATION.md
@.planning/phases/13-measurement-and-run-contract-hardening/13-MEASUREMENT-CONTRACT.md
@tools/apdr/src/lib.rs
@tools/apdr/src/resolver/retry_loop.rs
@tools/apdr/src/docker/builder/env_backend.rs
@tools/apdr/test_executor.py
@benchmark_ui/runner.py
@benchmark_ui/service.py
@benchmark_ui/test_runner_events.py
@benchmark_ui/test_run_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add summary-level backend path metadata and serialize it end to end</name>
  <files>tools/apdr/src/lib.rs, tools/apdr/src/resolver/retry_loop.rs, tools/apdr/src/docker/builder/env_backend.rs, tools/apdr/test_executor.py</files>
  <read_first>tools/apdr/src/lib.rs, tools/apdr/src/resolver/retry_loop.rs, tools/apdr/src/docker/builder/env_backend.rs, tools/apdr/test_executor.py, .planning/phases/13-measurement-and-run-contract-hardening/13-MEASUREMENT-CONTRACT.md</read_first>
  <action>Add a top-level `validation_path: Option<String>` to `ValidationSummary` or equivalent summary metadata, derive it from actual attempts and escalations, and keep `validation_backend` equal to the configured mode. Serialize the new path field and `escalated_backend` through the Rust output boundary into `tools/apdr/test_executor.py` using stable keys such as `validation_path`/`validationPath` without removing existing fields.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase18_backend_</verify>
  <acceptance_criteria>
    - `tools/apdr/src/lib.rs` contains `validation_path`
    - `tools/apdr/test_executor.py` contains `validationPath`
    - output metadata keeps `validation_backend` alongside the new path field
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase18_backend_` exits 0
  </acceptance_criteria>
  <done>Artifacts now carry both the requested backend mode and the actual validation path as separate truths.</done>
</task>

<task type="auto">
  <name>Task 2: Teach benchmark readers and tests to preserve path truth</name>
  <files>benchmark_ui/runner.py, benchmark_ui/service.py, benchmark_ui/test_runner_events.py, benchmark_ui/test_run_contract.py</files>
  <read_first>benchmark_ui/runner.py, benchmark_ui/service.py, benchmark_ui/test_runner_events.py, benchmark_ui/test_run_contract.py, tools/apdr/test_executor.py, .planning/phases/18-backend-escalation-and-path-truth/18-RESEARCH.md</read_first>
  <action>Update benchmark reader code so APDR events and saved-run ingestion preserve the new `validationPath` and `escalatedBackend` fields. Keep existing success and skip classification logic stable, but make route truth available to service summaries and tests. Extend the Python tests so at least one fixture exercises a routed path such as `env->docker` or `env->docker->llm-agent` while the configured backend remains `llm`.</action>
  <verify>python3 -m unittest benchmark_ui.test_runner_events benchmark_ui.test_run_contract</verify>
  <acceptance_criteria>
    - `benchmark_ui/runner.py` contains `validationPath`
    - `benchmark_ui/service.py` contains `escalatedBackend`
    - `benchmark_ui/test_runner_events.py` asserts routed path preservation
    - `benchmark_ui/test_run_contract.py` asserts configured backend and path remain distinct
    - `python3 -m unittest benchmark_ui.test_runner_events benchmark_ui.test_run_contract` exits 0
  </acceptance_criteria>
  <done>Benchmark ingestion keeps actual backend route truth without collapsing it back into the configured mode.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml phase18_backend_`
- [ ] `python3 -m unittest benchmark_ui.test_runner_events benchmark_ui.test_run_contract`
- [ ] `rg -n 'validation_path|validationPath|escalatedBackend' tools/apdr/src/lib.rs tools/apdr/test_executor.py benchmark_ui/runner.py benchmark_ui/service.py benchmark_ui/test_runner_events.py benchmark_ui/test_run_contract.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Top-level configured backend remains stable for comparison
- Actual route truth is available to saved artifacts and benchmark readers

</success_criteria>

<output>
After completion, create `.planning/phases/18-backend-escalation-and-path-truth/18-02-SUMMARY.md`
</output>
