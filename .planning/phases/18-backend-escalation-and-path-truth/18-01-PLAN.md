---
phase: 18-backend-escalation-and-path-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/apdr/src/docker/builder/agent_backend.rs
  - tools/apdr/src/docker/builder/docker_backend.rs
  - tools/apdr/src/docker/builder/mod.rs
autonomous: true
requirements:
  - VAL-01
  - WIN-02
user_setup: []
must_haves:
  truths:
    - The `llm` validation backend must route eligible env failures through a deterministic Docker stage before any final LangGraph fallback.
    - Docker escalation in `llm` mode must stay targeted to backend-recoverable failure signals rather than retrying every env failure.
    - The routing change must not silently degrade supported Windows or pure Docker correctness paths.
  artifacts:
    - path: "tools/apdr/src/docker/builder/agent_backend.rs"
      provides: "Env-first `llm` routing that conditionally escalates to Docker before agent fallback"
      contains: "llm_env_failure_requires_docker_escalation"
    - path: "tools/apdr/src/docker/builder/docker_backend.rs"
      provides: "Deterministic Docker validation entrypoint that stays Docker-only when used as the middle hop"
      contains: "validate_requirements_docker_deterministic"
    - path: "tools/apdr/src/docker/builder/mod.rs"
      provides: "Phase 18 routing and eligibility regression tests"
      contains: "phase18_backend_"
  key_links:
    - from: "tools/apdr/src/docker/builder/agent_backend.rs"
      to: ".planning/phases/18-backend-escalation-and-path-truth/18-CONTEXT.md"
      via: "Phase 18 locks the required route order to env first, Docker second, and agent last."
      pattern: "llm stays env-first, then docker, then llm-agent"
    - from: "tools/apdr/src/docker/builder/docker_backend.rs"
      to: "runs/20260330-020943-apdr/summary.json"
      via: "The live March 30 baseline has large packaging-style failure buckets that justify targeted Docker escalation."
      pattern: "environment-build-failed"
---

<objective>
Insert the missing Docker middle stage into `llm` validation without widening the route beyond the Phase 18 contract.

Purpose: The live baseline proves that `llm` mode currently goes from env failure straight to agent fallback, skipping the Docker recovery path that already exists for env mode. This plan adds the deterministic routing core and regression coverage that later artifact work can rely on.
Output: targeted `llm`-mode Docker escalation, a Docker-only middle-hop entrypoint, and Rust tests that lock route order plus eligibility.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-backend-escalation-and-path-truth/18-CONTEXT.md
@.planning/phases/18-backend-escalation-and-path-truth/18-RESEARCH.md
@.planning/phases/18-backend-escalation-and-path-truth/18-VALIDATION.md
@runs/20260330-020943-apdr/summary.json
@tools/apdr/src/docker/builder/mod.rs
@tools/apdr/src/docker/builder/agent_backend.rs
@tools/apdr/src/docker/builder/docker_backend.rs
@tools/apdr/src/docker/builder/env_backend.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add targeted Docker escalation to the `llm` route</name>
  <files>tools/apdr/src/docker/builder/agent_backend.rs, tools/apdr/src/docker/builder/docker_backend.rs</files>
  <read_first>tools/apdr/src/docker/builder/agent_backend.rs, tools/apdr/src/docker/builder/docker_backend.rs, tools/apdr/src/docker/builder/env_backend.rs, .planning/phases/18-backend-escalation-and-path-truth/18-CONTEXT.md</read_first>
  <action>In `tools/apdr/src/docker/builder/agent_backend.rs`, add a narrow helper named `llm_env_failure_requires_docker_escalation(...)` that classifies env failures using backend-recoverable signals already available from the current env and Docker helpers. After env validation fails, call a deterministic Docker validation stage before final agent fallback when the helper returns true. In `tools/apdr/src/docker/builder/docker_backend.rs`, expose a Docker-only entrypoint named `validate_requirements_docker_deterministic(...)` or equivalent that performs Docker validation without recursively invoking LangGraph work, so the `llm` route can stay `env -> docker -> llm-agent` rather than `env -> docker -> agent -> ...`.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase18_backend_</verify>
  <acceptance_criteria>
    - `tools/apdr/src/docker/builder/agent_backend.rs` contains `llm_env_failure_requires_docker_escalation`
    - `tools/apdr/src/docker/builder/docker_backend.rs` contains `validate_requirements_docker_deterministic`
    - `tools/apdr/src/docker/builder/agent_backend.rs` contains both a Docker call and a later agent fallback path
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase18_backend_` exits 0
  </acceptance_criteria>
  <done>Eligible `llm`-mode env failures can now try Docker before the final agent stage, and the route order is deterministic.</done>
</task>

<task type="auto">
  <name>Task 2: Lock targeted eligibility and platform correctness with Rust tests</name>
  <files>tools/apdr/src/docker/builder/mod.rs</files>
  <read_first>tools/apdr/src/docker/builder/mod.rs, tools/apdr/src/docker/builder/agent_backend.rs, tools/apdr/src/docker/builder/docker_backend.rs, .planning/phases/18-backend-escalation-and-path-truth/18-RESEARCH.md</read_first>
  <action>Add Rust tests to `tools/apdr/src/docker/builder/mod.rs` with names prefixed `phase18_backend_`. Include at least `phase18_backend_interpreter_failure_is_docker_eligible`, `phase18_backend_build_timeout_is_docker_eligible`, `phase18_backend_host_runtime_failure_is_not_docker_eligible`, and `phase18_backend_llm_path_tries_docker_before_agent`. The path-order test should assert the route order required by Phase 18, and the negative eligibility test should prove that host-runtime or framework-style failures do not trigger blanket Docker escalation.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase18_backend_</verify>
  <acceptance_criteria>
    - `tools/apdr/src/docker/builder/mod.rs` contains `phase18_backend_interpreter_failure_is_docker_eligible`
    - `tools/apdr/src/docker/builder/mod.rs` contains `phase18_backend_build_timeout_is_docker_eligible`
    - `tools/apdr/src/docker/builder/mod.rs` contains `phase18_backend_host_runtime_failure_is_not_docker_eligible`
    - `tools/apdr/src/docker/builder/mod.rs` contains `phase18_backend_llm_path_tries_docker_before_agent`
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase18_backend_` exits 0
  </acceptance_criteria>
  <done>Phase 18 has direct Rust coverage for eligibility boundaries and the required `env -> docker -> llm-agent` route order.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml phase18_backend_`
- [ ] `rg -n 'llm_env_failure_requires_docker_escalation|validate_requirements_docker_deterministic|phase18_backend_' tools/apdr/src/docker/builder/agent_backend.rs tools/apdr/src/docker/builder/docker_backend.rs tools/apdr/src/docker/builder/mod.rs`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Eligible `llm`-mode env failures have a deterministic Docker middle stage
- Phase 18 routing does not regress Windows or Docker correctness behavior

</success_criteria>

<output>
After completion, create `.planning/phases/18-backend-escalation-and-path-truth/18-01-SUMMARY.md`
</output>
