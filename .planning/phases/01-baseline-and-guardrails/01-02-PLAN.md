---
phase: 01-baseline-and-guardrails
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - scripts/check_apdr_regression.py
  - .planning/phases/01-baseline-and-guardrails/01-HOTSPOT-AUDIT.md
  - tools/apdr/README.md
autonomous: true
requirements:
  - BASE-03
  - BASE-04
  - BASE-05
user_setup: []
must_haves:
  truths:
    - The modernization milestone has one documented command set for fmt, clippy, targeted tests, baseline capture, memory capture, and regression comparison.
    - Hotspot prioritization for later Rust work is tied to measured evidence, not only static code smell observations.
    - Later phases can compare candidate runs against the Phase 1 baseline and fail fast on unacceptable regressions.
  artifacts:
    - path: "scripts/check_apdr_regression.py"
      provides: "Baseline-vs-candidate comparison gate with explicit timing and pass-rate thresholds"
      contains: "--baseline"
    - path: ".planning/phases/01-baseline-and-guardrails/01-HOTSPOT-AUDIT.md"
      provides: "Evidence-backed ranking of the next Rust hotspots to optimize"
      contains: "resolver/mod.rs"
    - path: "tools/apdr/README.md"
      provides: "Published modernization guardrail command set for future phases"
      contains: "Modernization guardrails"
  key_links:
    - from: "scripts/check_apdr_regression.py"
      to: ".planning/phases/01-baseline-and-guardrails/01-baseline.json"
      via: "consumes baseline artifacts from Plan 01-01"
      pattern: "pass_rate"
    - from: ".planning/phases/01-baseline-and-guardrails/01-HOTSPOT-AUDIT.md"
      to: ".planning/phases/01-baseline-and-guardrails/01-memory-profile.json"
      via: "uses captured runtime and memory evidence to justify the ranking"
      pattern: "peak_rss_bytes"
    - from: "tools/apdr/README.md"
      to: "scripts/check_apdr_regression.py"
      via: "documents the exact regression command future phases must run"
      pattern: "python scripts/check_apdr_regression.py"
---

<objective>
Turn the raw baseline artifacts into reusable modernization guardrails.

Purpose: Later phases need a documented command set, a measurable regression gate, and a ranked hotspot list so optimization work stays disciplined.
Output: A comparison script, an evidence-backed hotspot audit, and a published guardrail command set in the APDR README.
</objective>

<execution_context>
@C:\Users\danny\.codex\get-shit-done\workflows\execute-plan.md
@C:\Users\danny\.codex\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\apdr\.planning\PROJECT.md
@D:\apdr\.planning\ROADMAP.md
@D:\apdr\.planning\STATE.md
@D:\apdr\.planning\phases\01-baseline-and-guardrails\01-RESEARCH.md
@D:\apdr\.planning\phases\01-baseline-and-guardrails\01-VALIDATION.md
@D:\apdr\.planning\phases\01-baseline-and-guardrails\01-baseline.json
@D:\apdr\.planning\phases\01-baseline-and-guardrails\01-memory-profile.json
@D:\apdr\.planning\phases\01-baseline-and-guardrails\01-BASELINE.md
@D:\apdr\.planning\codebase\CONCERNS.md
@D:\apdr\tools\apdr\README.md
@D:\apdr\tools\apdr\src\resolver\mod.rs
@D:\apdr\tools\apdr\src\docker\builder.rs
@D:\apdr\tools\apdr\src\resolver\pre_solve.rs
@D:\apdr\tools\apdr\src\resolver\tier3_llm.rs
@D:\apdr\tools\apdr\src\resolver\pypi_client.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the baseline regression gate script</name>
  <files>scripts/check_apdr_regression.py</files>
  <read_first>scripts/check_apdr_regression.py, .planning/phases/01-baseline-and-guardrails/01-baseline.json, .planning/phases/01-baseline-and-guardrails/01-BASELINE.md, scripts/measure_apdr_baseline.py</read_first>
  <action>Create `scripts/check_apdr_regression.py` as a Python CLI that reads `--baseline` and `--candidate` JSON artifacts produced by the baseline harness. The script must compare `pass_rate`, `solve_duration_ms`, `validation_duration_ms`, `env_create_duration_ms`, `install_duration_ms`, and `smoke_duration_ms`, and support explicit thresholds `--max-total-regression-pct`, `--max-validation-regression-pct`, and `--min-pass-rate-delta`. Exit with code 0 when the candidate is within thresholds and non-zero when the candidate regresses beyond thresholds. Print a short comparison table to stdout so future phases can use the script both in local runs and in CI-like checks.</action>
  <verify>python -m py_compile scripts/check_apdr_regression.py && python scripts/check_apdr_regression.py --help</verify>
  <acceptance_criteria>
    - `scripts/check_apdr_regression.py` contains `--baseline` and `--candidate`
    - `scripts/check_apdr_regression.py` contains `--max-total-regression-pct`
    - `scripts/check_apdr_regression.py` contains `--max-validation-regression-pct`
    - `scripts/check_apdr_regression.py` contains `--min-pass-rate-delta`
    - `python scripts/check_apdr_regression.py --help` exits 0
  </acceptance_criteria>
  <done>The repo has a reusable gate that can fail future work on pass-rate or timing regression against the Phase 1 baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Write the evidence-backed hotspot audit</name>
  <files>.planning/phases/01-baseline-and-guardrails/01-HOTSPOT-AUDIT.md</files>
  <read_first>.planning/phases/01-baseline-and-guardrails/01-HOTSPOT-AUDIT.md, .planning/phases/01-baseline-and-guardrails/01-baseline.json, .planning/phases/01-baseline-and-guardrails/01-memory-profile.json, .planning/codebase/CONCERNS.md, tools/apdr/src/resolver/mod.rs, tools/apdr/src/docker/builder.rs, tools/apdr/src/resolver/pre_solve.rs, tools/apdr/src/resolver/tier3_llm.rs, tools/apdr/src/resolver/pypi_client.rs</read_first>
  <action>Create `.planning/phases/01-baseline-and-guardrails/01-HOTSPOT-AUDIT.md` with a ranked table of the top optimization candidates for Phase 2 and Phase 3. The table must include the file, the runtime signal from `01-baseline.json`, the memory or process signal from `01-memory-profile.json`, at least one static signal such as line count or `.clone()` density, and a one-line rationale for why the file should be attacked next. The audit must explicitly include `tools/apdr/src/resolver/mod.rs`, `tools/apdr/src/docker/builder.rs`, `tools/apdr/src/resolver/pre_solve.rs`, `tools/apdr/src/resolver/tier3_llm.rs`, and `tools/apdr/src/resolver/pypi_client.rs`, and it must end with a recommended order of attack for Phase 2 and Phase 3.</action>
  <verify>rg -n "resolver/mod.rs|docker/builder.rs|pre_solve.rs|tier3_llm.rs|pypi_client.rs" .planning/phases/01-baseline-and-guardrails/01-HOTSPOT-AUDIT.md</verify>
  <acceptance_criteria>
    - `.planning/phases/01-baseline-and-guardrails/01-HOTSPOT-AUDIT.md` contains `resolver/mod.rs`
    - `.planning/phases/01-baseline-and-guardrails/01-HOTSPOT-AUDIT.md` contains `docker/builder.rs`
    - `.planning/phases/01-baseline-and-guardrails/01-HOTSPOT-AUDIT.md` contains `pre_solve.rs`
    - `.planning/phases/01-baseline-and-guardrails/01-HOTSPOT-AUDIT.md` contains `tier3_llm.rs`
    - `.planning/phases/01-baseline-and-guardrails/01-HOTSPOT-AUDIT.md` contains `pypi_client.rs`
  </acceptance_criteria>
  <done>The milestone has a measured hotspot ranking that later phases can execute against directly.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the modernization guardrail command set</name>
  <files>tools/apdr/README.md</files>
  <read_first>tools/apdr/README.md, .planning/phases/01-baseline-and-guardrails/01-BASELINE.md, .planning/phases/01-baseline-and-guardrails/01-HOTSPOT-AUDIT.md, scripts/check_apdr_regression.py</read_first>
  <action>Add a new `## Modernization guardrails` section to `tools/apdr/README.md`. In that section, publish the exact commands future phases must run: `cargo fmt --manifest-path tools/apdr/Cargo.toml --all --check`, `cargo clippy --manifest-path tools/apdr/Cargo.toml --all-targets -- -D warnings`, `cargo test --manifest-path tools/apdr/Cargo.toml`, `python scripts/measure_apdr_baseline.py --fixtures-root tools/apdr/tests/fixtures --limit 3 --validation-backend env --output-json .planning/phases/01-baseline-and-guardrails/01-baseline.json --output-md .planning/phases/01-baseline-and-guardrails/01-BASELINE.md`, `python scripts/profile_apdr_memory.py --snippet tools/apdr/tests/fixtures/sample_snippet.py --validation-backend env --output-json .planning/phases/01-baseline-and-guardrails/01-memory-profile.json`, and `python scripts/check_apdr_regression.py --baseline .planning/phases/01-baseline-and-guardrails/01-baseline.json --candidate <candidate-json>`. Give each command a one-line purpose so later execution phases know when to use it.</action>
  <verify>rg -n "Modernization guardrails|cargo fmt --manifest-path tools/apdr/Cargo.toml --all --check|cargo clippy --manifest-path tools/apdr/Cargo.toml --all-targets -- -D warnings|python scripts/check_apdr_regression.py" tools/apdr/README.md</verify>
  <acceptance_criteria>
    - `tools/apdr/README.md` contains `## Modernization guardrails`
    - `tools/apdr/README.md` contains `cargo fmt --manifest-path tools/apdr/Cargo.toml --all --check`
    - `tools/apdr/README.md` contains `cargo clippy --manifest-path tools/apdr/Cargo.toml --all-targets -- -D warnings`
    - `tools/apdr/README.md` contains `python scripts/measure_apdr_baseline.py`
    - `tools/apdr/README.md` contains `python scripts/check_apdr_regression.py`
  </acceptance_criteria>
  <done>The repo has one documented command set for Phase 2+ guardrails, benchmark comparison, and regression blocking.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m py_compile scripts/check_apdr_regression.py`
- [ ] `python scripts/check_apdr_regression.py --help`
- [ ] `.planning/phases/01-baseline-and-guardrails/01-HOTSPOT-AUDIT.md` references the agreed hotspot files
- [ ] `tools/apdr/README.md` contains the modernization guardrail command block
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Later phases can compare candidate runs against the Phase 1 baseline with explicit thresholds
- The Phase 2 and Phase 3 starting points are justified by measured evidence, not only general codebase concerns
- The APDR README documents the command set future modernization work must follow
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-and-guardrails/01-02-SUMMARY.md`
</output>
