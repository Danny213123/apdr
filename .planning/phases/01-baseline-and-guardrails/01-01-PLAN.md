---
phase: 01-baseline-and-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/measure_apdr_baseline.py
  - scripts/profile_apdr_memory.py
  - tools/apdr/tests/test_cli.rs
  - .planning/phases/01-baseline-and-guardrails/01-baseline.json
  - .planning/phases/01-baseline-and-guardrails/01-memory-profile.json
  - .planning/phases/01-baseline-and-guardrails/01-BASELINE.md
autonomous: true
requirements:
  - BASE-01
  - BASE-02
user_setup:
  - Ensure the local Python interpreters APDR expects are available before recording env-validation baselines.
  - If you choose Docker validation during capture, start Docker Desktop first.
must_haves:
  truths:
    - Phase 1 has a deterministic baseline harness that records end-to-end timing, validation timing, and pass-rate data from the current APDR workflow.
    - Phase 1 has a separate memory-sensitive capture path that records peak process memory for representative APDR runs.
    - The timing summary contract used by the baseline harness is protected by automated tests before later optimization phases rely on it.
  artifacts:
    - path: "scripts/measure_apdr_baseline.py"
      provides: "Deterministic APDR baseline harness that writes machine-readable timing and status aggregates"
      contains: "solve_duration_ms"
    - path: "scripts/profile_apdr_memory.py"
      provides: "Cross-platform memory capture wrapper for APDR benchmark runs"
      contains: "peak_rss_bytes"
    - path: "tools/apdr/tests/test_cli.rs"
      provides: "Regression coverage for CLI summary keys consumed by baseline tooling"
      contains: "SOLVE_DURATION_MS="
    - path: ".planning/phases/01-baseline-and-guardrails/01-baseline.json"
      provides: "Current baseline snapshot with timing and status totals"
      contains: "\"validation_duration_ms\""
    - path: ".planning/phases/01-baseline-and-guardrails/01-memory-profile.json"
      provides: "Peak-memory snapshot for representative APDR runs"
      contains: "\"peak_rss_bytes\""
  key_links:
    - from: "scripts/measure_apdr_baseline.py"
      to: "tools/apdr/test_executor.py"
      via: "reuses output_data summary fields instead of parsing ad-hoc text"
      pattern: "output_data_"
    - from: "tools/apdr/tests/test_cli.rs"
      to: "tools/apdr/src/lib.rs"
      via: "locks the summary_lines contract used by the baseline harness"
      pattern: "SOLVE_DURATION_MS="
    - from: "scripts/profile_apdr_memory.py"
      to: ".planning/phases/01-baseline-and-guardrails/01-memory-profile.json"
      via: "writes the machine-readable memory snapshot consumed by later hotspot ranking"
      pattern: "peak_rss_bytes"
---

<objective>
Create the measurement foundation for the modernization milestone before any performance refactor starts.

Purpose: Phase 2 and Phase 3 need a trusted before-state for timing and memory; without it, later speedups and regressions are anecdotal.
Output: A deterministic baseline harness, a memory profiler wrapper, protected CLI summary contract tests, and initial baseline artifacts committed into the Phase 1 directory.
</objective>

<execution_context>
@C:\Users\danny\.codex\get-shit-done\workflows\execute-plan.md
@C:\Users\danny\.codex\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\apdr\.planning\PROJECT.md
@D:\apdr\.planning\ROADMAP.md
@D:\apdr\.planning\STATE.md
@D:\apdr\.planning\phases\01-baseline-and-guardrails\01-RESEARCH.md
@D:\apdr\.planning\phases\01-baseline-and-guardrails\01-VALIDATION.md
@D:\apdr\tools\apdr\src\lib.rs
@D:\apdr\tools\apdr\test_executor.py
@D:\apdr\benchmark_ui\runner.py
@D:\apdr\benchmark_ui\state.py
@D:\apdr\tools\apdr\tests\test_cli.rs
@D:\apdr\tools\apdr\README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the deterministic baseline harness and protect the summary contract</name>
  <files>scripts/measure_apdr_baseline.py, tools/apdr/tests/test_cli.rs</files>
  <read_first>scripts/measure_apdr_baseline.py, tools/apdr/tests/test_cli.rs, tools/apdr/test_executor.py, tools/apdr/src/lib.rs, benchmark_ui/runner.py, benchmark_ui/state.py</read_first>
  <action>Create `scripts/measure_apdr_baseline.py` as a repo-level Python CLI that accepts `--fixtures-root`, `--dataset-root`, `--limit`, `--validation-backend`, `--output-json`, `--output-md`, and `--context-log`. Make it sort snippet inputs deterministically, invoke `tools/apdr/test_executor.py` for each case, parse the generated `output_data_<python>.yml` files, and aggregate pass/fail/skip counts plus `solve_duration_ms`, `validation_duration_ms`, `env_create_duration_ms`, `install_duration_ms`, and `smoke_duration_ms`. Write the machine-readable aggregate to the exact path provided by `--output-json`, and include the sample list and command used for each run. In `tools/apdr/tests/test_cli.rs`, add or extend a contract test so APDR CLI output is asserted to contain the exact summary keys `SOLVE_DURATION_MS=`, `VALIDATION_DURATION_MS=`, `ENV_CREATE_DURATION_MS=`, `INSTALL_DURATION_MS=`, and `SMOKE_DURATION_MS=`; this locks the output format that the new baseline harness consumes.</action>
  <verify>python -m py_compile scripts/measure_apdr_baseline.py && cargo test --manifest-path tools/apdr/Cargo.toml --test test_cli</verify>
  <acceptance_criteria>
    - `scripts/measure_apdr_baseline.py` contains `parser.add_argument("--fixtures-root"` and `parser.add_argument("--output-json"`
    - `scripts/measure_apdr_baseline.py` contains the exact strings `solve_duration_ms`, `validation_duration_ms`, `env_create_duration_ms`, `install_duration_ms`, and `smoke_duration_ms`
    - `tools/apdr/tests/test_cli.rs` contains `SOLVE_DURATION_MS=` and `VALIDATION_DURATION_MS=`
    - `cargo test --manifest-path tools/apdr/Cargo.toml --test test_cli` exits 0
  </acceptance_criteria>
  <done>The baseline harness can emit a machine-readable aggregate and the CLI summary contract it depends on is covered by Rust tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add the cross-platform memory capture wrapper</name>
  <files>scripts/profile_apdr_memory.py</files>
  <read_first>scripts/profile_apdr_memory.py, scripts/measure_apdr_baseline.py, tools/apdr/test_executor.py, benchmark_ui/state.py</read_first>
  <action>Create `scripts/profile_apdr_memory.py` as a Python CLI that accepts `--snippet`, `--output-json`, `--validation-backend`, `--python-command`, and `--context-log`. The script must launch APDR through `tools/apdr/test_executor.py`, record the exact command it ran, and capture a peak-memory metric named `peak_rss_bytes`. On Windows, query the child process with PowerShell `Get-Process` and capture `PeakWorkingSet64`; on Unix-like systems, use `resource.getrusage(resource.RUSAGE_CHILDREN).ru_maxrss` and normalize it to bytes. Write a JSON artifact containing `snippet`, `command`, `validation_backend`, `duration_ms`, `peak_rss_bytes`, and the resulting APDR status fields to the exact file passed by `--output-json`.</action>
  <verify>python -m py_compile scripts/profile_apdr_memory.py && python scripts/profile_apdr_memory.py --help</verify>
  <acceptance_criteria>
    - `scripts/profile_apdr_memory.py` contains `peak_rss_bytes`
    - `scripts/profile_apdr_memory.py` contains `PeakWorkingSet64`
    - `scripts/profile_apdr_memory.py` contains `resource.RUSAGE_CHILDREN`
    - `python scripts/profile_apdr_memory.py --help` exits 0
  </acceptance_criteria>
  <done>The repo has a cross-platform wrapper for collecting peak APDR process memory without modifying the Rust core yet.</done>
</task>

<task type="auto">
  <name>Task 3: Capture and document the initial baseline artifacts</name>
  <files>.planning/phases/01-baseline-and-guardrails/01-baseline.json, .planning/phases/01-baseline-and-guardrails/01-memory-profile.json, .planning/phases/01-baseline-and-guardrails/01-BASELINE.md</files>
  <read_first>.planning/phases/01-baseline-and-guardrails/01-baseline.json, .planning/phases/01-baseline-and-guardrails/01-memory-profile.json, .planning/phases/01-baseline-and-guardrails/01-BASELINE.md, scripts/measure_apdr_baseline.py, scripts/profile_apdr_memory.py, .planning/phases/01-baseline-and-guardrails/01-RESEARCH.md</read_first>
  <action>Run the new harness on a bounded deterministic sample before any optimization lands. Use `tools/apdr/tests/fixtures` as the first sample source and keep the initial run small enough to be repeatable during planning verification. Write the aggregate to `.planning/phases/01-baseline-and-guardrails/01-baseline.json` and the memory snapshot to `.planning/phases/01-baseline-and-guardrails/01-memory-profile.json`. Then create `.planning/phases/01-baseline-and-guardrails/01-BASELINE.md` with the exact commands used, the sample list or sample-count rule, pass/fail/skip totals, and the recorded timing and memory figures. Make the Markdown file explicitly say this is the pre-optimization baseline for the v2.0 modernization milestone.</action>
  <verify>python scripts/measure_apdr_baseline.py --fixtures-root tools/apdr/tests/fixtures --limit 3 --validation-backend env --output-json .planning/phases/01-baseline-and-guardrails/01-baseline.json --output-md .planning/phases/01-baseline-and-guardrails/01-BASELINE.md && python scripts/profile_apdr_memory.py --snippet tools/apdr/tests/fixtures/sample_snippet.py --validation-backend env --output-json .planning/phases/01-baseline-and-guardrails/01-memory-profile.json</verify>
  <acceptance_criteria>
    - `.planning/phases/01-baseline-and-guardrails/01-baseline.json` contains `"solve_duration_ms"` and `"validation_duration_ms"`
    - `.planning/phases/01-baseline-and-guardrails/01-memory-profile.json` contains `"peak_rss_bytes"`
    - `.planning/phases/01-baseline-and-guardrails/01-BASELINE.md` contains `Pre-optimization baseline`
    - `.planning/phases/01-baseline-and-guardrails/01-BASELINE.md` contains `Pass rate`
    - `.planning/phases/01-baseline-and-guardrails/01-BASELINE.md` contains `Validation duration`
  </acceptance_criteria>
  <done>Phase 1 has committed baseline artifacts that later optimization phases can compare against directly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m py_compile scripts/measure_apdr_baseline.py scripts/profile_apdr_memory.py`
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml --test test_cli`
- [ ] `.planning/phases/01-baseline-and-guardrails/01-baseline.json` and `.planning/phases/01-baseline-and-guardrails/01-memory-profile.json` exist and contain the expected metric keys
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The baseline harness records timing and status aggregates without inventing a second output format
- The memory capture path records a peak-memory field named `peak_rss_bytes`
- The committed artifacts clearly represent the pre-optimization state of APDR
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-and-guardrails/01-01-SUMMARY.md`
</output>
