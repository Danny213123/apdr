---
phase: 07-failure-baseline-parity-slice
plan: 03
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - scripts/check_phase7_baseline.py
  - .planning/phases/07-failure-baseline-parity-slice/07-BASELINE.md
autonomous: true
requirements:
  - REC-01
  - FAM-04
user_setup: []
must_haves:
  truths:
    - Phase 7 closes with a checker that can prove the generated baseline artifacts still match the stopped run and the `pllm` CSV without fresh benchmark execution.
    - The final baseline note makes the 70-case canonical slice, normalized buckets, touched-family subset, and 17-case tier1 watchlist explicit for Phase 8.
    - Existing targeted resolver coverage remains part of the guardrail for the family snapshot boundary.
  artifacts:
    - path: "scripts/check_phase7_baseline.py"
      provides: "Deterministic verification of the parity manifest, family snapshot manifest, and baseline note"
      contains: "--parity-manifest"
    - path: ".planning/phases/07-failure-baseline-parity-slice/07-BASELINE.md"
      provides: "Reviewer-facing closeout note for the canonical slice, snapshot set, and Phase 8 handoff"
      contains: "## Phase 8 Handoff"
  key_links:
    - from: "scripts/check_phase7_baseline.py"
      to: ".planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json"
      via: "the checker must confirm the generated manifest still matches the stopped-run plus `pllm` overlap contract"
      pattern: "\"canonical_case_count\": 70"
    - from: "scripts/check_phase7_baseline.py"
      to: ".planning/phases/07-failure-baseline-parity-slice/07-family-snapshot-manifest.json"
      via: "the checker must confirm every family snapshot case is still a member of the canonical manifest and points to a copied fixture"
      pattern: "\"fixture_path\""
---

<objective>
Finish Phase 7 with a rerunnable baseline check and a reviewer-facing handoff that makes the canonical slice and family-snapshot boundary explicit before Phase 8 changes behavior.

Purpose: Phase 7 should end with one command that can prove the generated artifacts are still internally consistent, plus a baseline note that future phases can cite instead of reopening the March 27, 2026 benchmark data.
Output: `scripts/check_phase7_baseline.py` and `07-BASELINE.md`.
</objective>

<execution_context>
@C:\Users\danny\.codex\get-shit-done\workflows\execute-plan.md
@C:\Users\danny\.codex\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\apdr\.planning\PROJECT.md
@D:\apdr\.planning\ROADMAP.md
@D:\apdr\.planning\STATE.md
@D:\apdr\.planning\phases\07-failure-baseline-parity-slice\07-CONTEXT.md
@D:\apdr\.planning\phases\07-failure-baseline-parity-slice\07-RESEARCH.md
@D:\apdr\.planning\phases\07-failure-baseline-parity-slice\07-VALIDATION.md
@D:\apdr\.planning\phases\07-failure-baseline-parity-slice\07-tier3-parity-manifest.json
@D:\apdr\.planning\phases\07-failure-baseline-parity-slice\07-family-snapshot-manifest.json
@D:\apdr\tools\apdr\tests\test_resolver.rs
@D:\apdr\runs\20260327-150339-apdr\summary.json
@D:\apdr\pllm_results\csv\summary-all-runs.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the Phase 7 baseline checker</name>
  <files>scripts/check_phase7_baseline.py</files>
  <read_first>scripts/check_phase7_baseline.py, .planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json, .planning/phases/07-failure-baseline-parity-slice/07-family-snapshot-manifest.json, runs/20260327-150339-apdr/summary.json, pllm_results/csv/summary-all-runs.csv, .planning/phases/07-failure-baseline-parity-slice/07-RESEARCH.md</read_first>
  <action>Create `scripts/check_phase7_baseline.py` as a deterministic validator. The script must accept `--summary-json`, `--pllm-csv`, `--parity-manifest`, `--family-manifest`, and `--baseline-md`. Recompute the canonical overlap from the raw inputs, assert that the generated parity manifest still contains exactly `70` canonical tier3 case IDs and exactly `17` excluded tier1 watchlist IDs, assert that the manifest's `normalized_bucket_totals` matches the per-case `normalized_bucket` values, assert that every family snapshot case exists in the canonical manifest, and assert that every recorded `fixture_path` exists on disk. The script must exit non-zero when any invariant fails and print the failing invariant name.</action>
  <verify>python -m py_compile scripts/check_phase7_baseline.py</verify>
  <acceptance_criteria>
    - `scripts/check_phase7_baseline.py` exists
    - `scripts/check_phase7_baseline.py` contains `canonical_case_count`
    - `scripts/check_phase7_baseline.py` contains `tier1_watchlist_count`
    - `scripts/check_phase7_baseline.py` contains `fixture_path`
    - `python -m py_compile scripts/check_phase7_baseline.py` exits 0
  </acceptance_criteria>
  <done>The repo now has a deterministic validator for the Phase 7 parity and family-snapshot artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Write the reviewer-facing Phase 7 baseline note</name>
  <files>.planning/phases/07-failure-baseline-parity-slice/07-BASELINE.md</files>
  <read_first>.planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json, .planning/phases/07-failure-baseline-parity-slice/07-family-snapshot-manifest.json, .planning/phases/07-failure-baseline-parity-slice/07-TIER3-PARITY-MANIFEST.md, .planning/phases/07-failure-baseline-parity-slice/07-FAMILY-SNAPSHOTS.md</read_first>
  <action>Create `.planning/phases/07-failure-baseline-parity-slice/07-BASELINE.md` with these exact section headings: `## Commands`, `## Artifact Links`, `## Canonical Slice`, `## Normalized Buckets`, `## Touched Family Snapshots`, `## Tier1 Watchlist`, `## Verification`, and `## Phase 8 Handoff`. In `## Canonical Slice`, state that the baseline is the 70-case tier3 APDR-failed and `pllm`-passing slice from March 27, 2026. In `## Tier1 Watchlist`, state that the remaining 17 overlap cases are documented but outside the Phase 7 contract. In `## Phase 8 Handoff`, state that only the touched-family subset is protected for the first data-driven migration pass.</action>
  <verify>rg -n '## Canonical Slice|## Normalized Buckets|## Touched Family Snapshots|## Tier1 Watchlist|## Phase 8 Handoff' .planning/phases/07-failure-baseline-parity-slice/07-BASELINE.md</verify>
  <acceptance_criteria>
    - `.planning/phases/07-failure-baseline-parity-slice/07-BASELINE.md` exists
    - `.planning/phases/07-failure-baseline-parity-slice/07-BASELINE.md` contains `70-case tier3`
    - `.planning/phases/07-failure-baseline-parity-slice/07-BASELINE.md` contains `17 overlap cases`
    - `.planning/phases/07-failure-baseline-parity-slice/07-BASELINE.md` contains `## Phase 8 Handoff`
    - `rg -n '## Canonical Slice|## Normalized Buckets|## Touched Family Snapshots|## Tier1 Watchlist|## Phase 8 Handoff' .planning/phases/07-failure-baseline-parity-slice/07-BASELINE.md` exits 0
  </acceptance_criteria>
  <done>The Phase 7 baseline and handoff are documented in one reviewer-facing note.</done>
</task>

<task type="auto">
  <name>Task 3: Run the baseline checker and keep targeted resolver coverage green</name>
  <files>.planning/phases/07-failure-baseline-parity-slice/07-BASELINE.md</files>
  <read_first>scripts/check_phase7_baseline.py, .planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json, .planning/phases/07-failure-baseline-parity-slice/07-family-snapshot-manifest.json, .planning/phases/07-failure-baseline-parity-slice/07-BASELINE.md, tools/apdr/tests/test_resolver.rs</read_first>
  <action>Run `python scripts/check_phase7_baseline.py --summary-json runs/20260327-150339-apdr/summary.json --pllm-csv pllm_results/csv/summary-all-runs.csv --parity-manifest .planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json --family-manifest .planning/phases/07-failure-baseline-parity-slice/07-family-snapshot-manifest.json --baseline-md .planning/phases/07-failure-baseline-parity-slice/07-BASELINE.md` and then run `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture`. Do not broaden this phase-close loop into a live benchmark rerun or the full Rust suite. If the checker fails, fix the generated Phase 7 artifacts instead of weakening the checker.</action>
  <verify>python scripts/check_phase7_baseline.py --summary-json runs/20260327-150339-apdr/summary.json --pllm-csv pllm_results/csv/summary-all-runs.csv --parity-manifest .planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json --family-manifest .planning/phases/07-failure-baseline-parity-slice/07-family-snapshot-manifest.json --baseline-md .planning/phases/07-failure-baseline-parity-slice/07-BASELINE.md && cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture</verify>
  <acceptance_criteria>
    - `python scripts/check_phase7_baseline.py --summary-json runs/20260327-150339-apdr/summary.json --pllm-csv pllm_results/csv/summary-all-runs.csv --parity-manifest .planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json --family-manifest .planning/phases/07-failure-baseline-parity-slice/07-family-snapshot-manifest.json --baseline-md .planning/phases/07-failure-baseline-parity-slice/07-BASELINE.md` exits 0
    - `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>The Phase 7 baseline is now rerunnable and guarded by both artifact validation and targeted resolver coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m py_compile scripts/check_phase7_baseline.py`
- [ ] `rg -n '## Canonical Slice|## Normalized Buckets|## Touched Family Snapshots|## Tier1 Watchlist|## Phase 8 Handoff' .planning/phases/07-failure-baseline-parity-slice/07-BASELINE.md`
- [ ] `python scripts/check_phase7_baseline.py --summary-json runs/20260327-150339-apdr/summary.json --pllm-csv pllm_results/csv/summary-all-runs.csv --parity-manifest .planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json --family-manifest .planning/phases/07-failure-baseline-parity-slice/07-family-snapshot-manifest.json --baseline-md .planning/phases/07-failure-baseline-parity-slice/07-BASELINE.md`
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The generated Phase 7 artifacts can be revalidated locally without rerunning the benchmark
- The baseline note makes the canonical slice, family-snapshot subset, and tier1 watchlist explicit
- Targeted resolver coverage stays green around the family-runtime boundary
</success_criteria>

<output>
After completion, create `.planning/phases/07-failure-baseline-parity-slice/07-03-SUMMARY.md`
</output>
