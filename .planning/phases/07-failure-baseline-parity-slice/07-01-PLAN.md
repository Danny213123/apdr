---
phase: 07-failure-baseline-parity-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build_phase7_parity_manifest.py
  - .planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json
  - .planning/phases/07-failure-baseline-parity-slice/07-TIER3-PARITY-MANIFEST.md
autonomous: true
requirements:
  - REC-01
user_setup: []
must_haves:
  truths:
    - Phase 7 produces one reproducible case-level manifest for the fixed 70-case tier3 APDR-failed and `pllm`-passing slice.
    - The manifest preserves raw APDR failure fields while adding one deterministic `normalized_bucket` per case.
    - The reviewer summary explicitly documents the excluded 17-case tier1 watchlist instead of treating it as part of the canonical slice.
  artifacts:
    - path: ".planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json"
      provides: "Machine-readable Phase 7 source of truth for the canonical tier3 parity slice"
      contains: "\"canonical_case_count\": 70"
    - path: ".planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json"
      provides: "Case-level normalized failure buckets with raw APDR values preserved"
      contains: "\"normalized_bucket\""
    - path: ".planning/phases/07-failure-baseline-parity-slice/07-TIER3-PARITY-MANIFEST.md"
      provides: "Reviewer-readable summary of the canonical slice, bucket totals, and tier1 watchlist"
      contains: "## Canonical Slice"
  key_links:
    - from: ".planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json"
      to: "runs/20260327-150339-apdr/summary.json"
      via: "the canonical case ID must come from the summary's `artifact_dir` basename and the `tier` field must come from the summary's stored `tier` value"
      pattern: "\"artifact_dir\""
    - from: ".planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json"
      to: "pllm_results/csv/summary-all-runs.csv"
      via: "the `pllm_pass_count` field must be joined from the CSV `name` and `passed` columns"
      pattern: "passed"
---

<objective>
Create the canonical Phase 7 parity baseline so later accuracy work compares against one fixed tier3 target instead of re-deriving the March 27, 2026 overlap ad hoc.

Purpose: Phase 7 starts by turning the stopped APDR run and `pllm` comparison into a reproducible 70-case manifest with one normalized milestone bucket per case and an explicit 17-case tier1 watchlist.
Output: `scripts/build_phase7_parity_manifest.py`, `07-tier3-parity-manifest.json`, and `07-TIER3-PARITY-MANIFEST.md`.
</objective>

<execution_context>
@C:\Users\danny\.codex\get-shit-done\workflows\execute-plan.md
@C:\Users\danny\.codex\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\apdr\.planning\PROJECT.md
@D:\apdr\.planning\ROADMAP.md
@D:\apdr\.planning\STATE.md
@D:\apdr\.planning\phases\07-failure-baseline-parity-slice\07-CONTEXT.md
@D:\apdr\.planning\phases\07-failure-baseline-parity-slice\07-RESEARCH.md
@D:\apdr\.planning\phases\07-failure-baseline-parity-slice\07-VALIDATION.md
@D:\apdr\runs\20260327-150339-apdr\summary.json
@D:\apdr\runs\20260327-150339-apdr\benchmark-context.log
@D:\apdr\pllm_results\csv\summary-all-runs.csv
@D:\apdr\tools\apdr\src\resolver\recovery_diagnostics.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the canonical tier3 parity-manifest generator</name>
  <files>scripts/build_phase7_parity_manifest.py</files>
  <read_first>scripts/build_phase7_parity_manifest.py, runs/20260327-150339-apdr/summary.json, pllm_results/csv/summary-all-runs.csv, tools/apdr/src/resolver/recovery_diagnostics.rs, .planning/phases/07-failure-baseline-parity-slice/07-RESEARCH.md</read_first>
  <action>Create `scripts/build_phase7_parity_manifest.py` as a deterministic local artifact generator. The script must accept `--summary-json`, `--pllm-csv`, `--output-json`, and `--output-md`. Join cases by using the basename of each summary `artifact_dir` and the CSV `name` column. Use the summary's stored `tier` field instead of inferring tier from `llm_calls`. Keep only APDR-failed, non-skipped cases where `pllm` passed at least once, and then keep only the `tier3` subset for the canonical manifest while also counting the excluded `tier1` overlap cases. For every canonical case, preserve `raw_validation_status`, `raw_failure_bucket`, `validation_reason`, `requirements`, `snippet`, `report_path`, and `log_tail`, then compute `normalized_bucket` with this exact precedence: summary `failure_bucket`, summary `validation_status`, `failure_bucket:` from `resolution-report.txt`, `validation_status:` from `resolution-report.txt`, known `log_tail` labels (`module-not-found`, `version-not-found`, `dependency-conflict`, `import-error`, `syntax-error`, `environment-build-failed`), else `unclassified`. Emit deterministic case ordering by `case_id`.</action>
  <verify>python -m py_compile scripts/build_phase7_parity_manifest.py</verify>
  <acceptance_criteria>
    - `scripts/build_phase7_parity_manifest.py` exists
    - `scripts/build_phase7_parity_manifest.py` contains `--summary-json`
    - `scripts/build_phase7_parity_manifest.py` contains `normalized_bucket`
    - `scripts/build_phase7_parity_manifest.py` contains `resolution-report.txt`
    - `python -m py_compile scripts/build_phase7_parity_manifest.py` exits 0
  </acceptance_criteria>
  <done>The repo now has a deterministic generator for the canonical Phase 7 parity slice.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the JSON-first canonical manifest and reviewer summary</name>
  <files>.planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json, .planning/phases/07-failure-baseline-parity-slice/07-TIER3-PARITY-MANIFEST.md</files>
  <read_first>scripts/build_phase7_parity_manifest.py, runs/20260327-150339-apdr/summary.json, pllm_results/csv/summary-all-runs.csv, .planning/phases/07-failure-baseline-parity-slice/07-CONTEXT.md</read_first>
  <action>Run `python scripts/build_phase7_parity_manifest.py --summary-json runs/20260327-150339-apdr/summary.json --pllm-csv pllm_results/csv/summary-all-runs.csv --output-json .planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json --output-md .planning/phases/07-failure-baseline-parity-slice/07-TIER3-PARITY-MANIFEST.md`. The JSON output must record `canonical_case_count: 70`, `tier1_watchlist_count: 17`, a `normalized_bucket_totals` object, and one case array with preserved raw fields plus `normalized_bucket`. The Markdown summary must contain these exact headings: `## Source Inputs`, `## Canonical Slice`, `## Normalized Buckets`, `## Representative Cases`, and `## Tier1 Watchlist`. In the watchlist section, explicitly say the 17 tier1 cases are outside the Phase 7 contract.</action>
  <verify>python scripts/build_phase7_parity_manifest.py --summary-json runs/20260327-150339-apdr/summary.json --pllm-csv pllm_results/csv/summary-all-runs.csv --output-json .planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json --output-md .planning/phases/07-failure-baseline-parity-slice/07-TIER3-PARITY-MANIFEST.md</verify>
  <acceptance_criteria>
    - `.planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json` exists
    - `.planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json` contains `"canonical_case_count": 70`
    - `.planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json` contains `"tier1_watchlist_count": 17`
    - `.planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json` contains `"normalized_bucket_totals"`
    - `.planning/phases/07-failure-baseline-parity-slice/07-TIER3-PARITY-MANIFEST.md` contains `## Canonical Slice`
    - `.planning/phases/07-failure-baseline-parity-slice/07-TIER3-PARITY-MANIFEST.md` contains `outside the Phase 7 contract`
  </acceptance_criteria>
  <done>The canonical tier3 parity baseline is captured in a deterministic JSON manifest plus a reviewer-readable Markdown summary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m py_compile scripts/build_phase7_parity_manifest.py`
- [ ] `python scripts/build_phase7_parity_manifest.py --summary-json runs/20260327-150339-apdr/summary.json --pllm-csv pllm_results/csv/summary-all-runs.csv --output-json .planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json --output-md .planning/phases/07-failure-baseline-parity-slice/07-TIER3-PARITY-MANIFEST.md`
- [ ] `rg -n 'canonical_case_count|tier1_watchlist_count|normalized_bucket' .planning/phases/07-failure-baseline-parity-slice/07-tier3-parity-manifest.json`
- [ ] `rg -n '## Canonical Slice|## Normalized Buckets|## Tier1 Watchlist' .planning/phases/07-failure-baseline-parity-slice/07-TIER3-PARITY-MANIFEST.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The canonical Phase 7 slice is fixed to the 70 tier3 parity cases
- Raw APDR fields are preserved next to one deterministic normalized bucket
- The excluded 17-case tier1 overlap is documented as a watchlist, not absorbed into the baseline
</success_criteria>

<output>
After completion, create `.planning/phases/07-failure-baseline-parity-slice/07-01-SUMMARY.md`
</output>
