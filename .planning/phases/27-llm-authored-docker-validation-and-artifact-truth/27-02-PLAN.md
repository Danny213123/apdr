---
phase: 27-llm-authored-docker-validation-and-artifact-truth
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - tools/apdr/src/docker/templates.rs
  - tools/apdr/src/docker/builder/docker_backend.rs
  - tools/apdr/src/docker/builder/mod.rs
  - tools/apdr/docker_agent/tools/docker_ops.py
  - tools/apdr/src/lib.rs
  - tools/apdr/test_executor.py
  - benchmark_ui/service.py
  - benchmark_ui/test_run_contract.py
  - benchmark_ui/test_runner_events.py
  - tools/apdr/tests/test_resolver.rs
autonomous: true
requirements:
  - LLM-02
  - DKR-01
  - DKR-02
user_setup: []
must_haves:
  truths:
    - The executed Dockerfile and commands must be derived from the authored Docker plan and preserved as executed truth, not reconstructed after the fact.
    - Docker build-to-run handoff must verify a locally usable image reference before `docker create` or `docker start`.
    - Summary metadata and benchmark rows must preserve authored-vs-executed Docker artifact paths and the exact image reference used.
  artifacts:
    - path: "tools/apdr/src/docker/builder/docker_backend.rs"
      provides: "Reliable image handoff and executed Docker artifact persistence"
      contains: "docker image inspect"
    - path: "tools/apdr/src/lib.rs"
      provides: "Summary export of authored and executed Docker artifact truth"
      contains: "executed_dockerfile_path"
    - path: "tools/apdr/test_executor.py"
      provides: "Benchmark output metadata for Docker artifact paths and image references"
      contains: "executed_dockerfile_path"
    - path: "benchmark_ui/test_run_contract.py"
      provides: "Contract coverage for authored/executed Docker metadata"
      contains: "executedDockerfilePath"
  key_links:
    - from: "tools/apdr/src/docker/builder/docker_backend.rs"
      to: "runs/20260402-184821-apdr/cases/005bbad123ef309a5bef/.apdr-debug/attempts/attempt-001-py-2_7/combined.log"
      via: "Phase 27 must close the real build-to-run handoff gap shown by the missing-image regression."
      pattern: "Unable to find image"
    - from: "benchmark_ui/service.py"
      to: ".planning/REQUIREMENTS.md"
      via: "DKR-02 requires case-level Docker artifact truth to be inspectable without raw log spelunking."
      pattern: "DKR-02"
---

<objective>
Make the deterministic Docker backend consume the authored Docker plan, preserve executed Docker truth, and fix the image-handoff seam.

Purpose: An authored Docker plan is only valuable if the actual executed Docker backend can use it reliably. This plan hardens the build-to-run path and preserves exact executed Docker artifacts and image references for review.
Output: a deterministic Docker backend that consumes authored Docker intent, a verified build-to-run handoff contract, and additive benchmark metadata for Docker artifact truth.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-llm-authored-docker-validation-and-artifact-truth/27-RESEARCH.md
@.planning/phases/27-llm-authored-docker-validation-and-artifact-truth/27-VALIDATION.md
@.planning/phases/27-llm-authored-docker-validation-and-artifact-truth/27-01-PLAN.md
@tools/apdr/src/docker/templates.rs
@tools/apdr/src/docker/builder/docker_backend.rs
@tools/apdr/src/docker/builder/mod.rs
@tools/apdr/docker_agent/tools/docker_ops.py
@tools/apdr/src/lib.rs
@tools/apdr/test_executor.py
@benchmark_ui/service.py
@benchmark_ui/test_run_contract.py
@benchmark_ui/test_runner_events.py
@runs/20260402-184821-apdr/cases/005bbad123ef309a5bef/.apdr-debug/attempts/attempt-001-py-2_7/combined.log
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render executed Docker inputs from the authored plan and close the image-handoff gap</name>
  <files>tools/apdr/src/docker/templates.rs, tools/apdr/src/docker/builder/docker_backend.rs, tools/apdr/src/docker/builder/mod.rs, tools/apdr/docker_agent/tools/docker_ops.py, tools/apdr/tests/test_resolver.rs</files>
  <read_first>tools/apdr/src/docker/templates.rs, tools/apdr/src/docker/builder/docker_backend.rs, tools/apdr/docker_agent/tools/docker_ops.py, runs/20260402-184821-apdr/cases/005bbad123ef309a5bef/.apdr-debug/attempts/attempt-001-py-2_7/combined.log</read_first>
  <action>Teach the deterministic Docker builder to render the executed Dockerfile from the authored Docker plan plus clearly-labeled deterministic supplements. Harden image handoff by capturing an inspectable local image reference or image ID after build, verifying it before container creation, and only then running `docker create`/`docker start`. Mirror the same rendering contract into `tools/apdr/docker_agent/tools/docker_ops.py` so the Python agent path cannot drift. Add Rust tests prefixed `phase27_handoff_` that lock the image-reference contract and the executed Docker-artifact paths.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase27_handoff_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/docker/builder/docker_backend.rs` verifies a local image reference before `docker create`
    - `tools/apdr/src/docker/builder/docker_backend.rs` persists executed Docker artifacts or image-reference files in the attempt directory
    - `tools/apdr/tests/test_resolver.rs` contains `phase27_handoff_`
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase27_handoff_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>The executed Docker backend is now driven by the authored plan and no longer trusts an unverified post-build tag.</done>
</task>

<task type="auto">
  <name>Task 2: Export Docker artifact truth through case metadata and benchmark surfaces</name>
  <files>tools/apdr/src/lib.rs, tools/apdr/test_executor.py, benchmark_ui/service.py, benchmark_ui/test_run_contract.py, benchmark_ui/test_runner_events.py</files>
  <read_first>tools/apdr/src/lib.rs, tools/apdr/test_executor.py, benchmark_ui/service.py, benchmark_ui/test_run_contract.py, .planning/phases/27-llm-authored-docker-validation-and-artifact-truth/27-RESEARCH.md</read_first>
  <action>Extend report text, summary lines, and benchmark `output_metadata` so cases export authored Docker-plan paths, authored Dockerfile paths, executed Dockerfile paths, build command paths, run command paths, and the exact executed image reference or image ID. Keep the UI additive by exposing these pointers in case-detail metadata rather than redesigning list views. Add Python contract tests that verify saved rows and live events preserve the new Docker artifact truth fields.</action>
  <verify>python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_runner_events</verify>
  <acceptance_criteria>
    - `tools/apdr/src/lib.rs` contains authored and executed Docker artifact summary keys
    - `tools/apdr/test_executor.py` exports the new Docker artifact metadata
    - `benchmark_ui/test_run_contract.py` contains `executedDockerfilePath`
    - `python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_runner_events` exits 0
  </acceptance_criteria>
  <done>Docker artifact truth is now visible through case metadata and benchmark inspection surfaces.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml phase27_handoff_ -- --nocapture`
- [ ] `python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_runner_events`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The executed Docker backend uses a verified local image reference after build
- Reviewers can find both authored and executed Docker artifacts from case metadata without raw directory spelunking

</success_criteria>

<output>
After completion, create `.planning/phases/27-llm-authored-docker-validation-and-artifact-truth/27-02-SUMMARY.md`
</output>
