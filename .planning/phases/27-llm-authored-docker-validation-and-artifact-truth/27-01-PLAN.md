---
phase: 27-llm-authored-docker-validation-and-artifact-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/apdr/llm_py/models.py
  - tools/apdr/llm_py/__main__.py
  - tools/apdr/llm_py/actions/docker_plan.py
  - tools/apdr/src/resolver/tier3_llm/core.rs
  - tools/apdr/src/resolver/mod.rs
  - tools/apdr/src/lib.rs
  - tools/apdr/tests/test_resolver.rs
autonomous: true
requirements:
  - LLM-02
  - DKR-02
user_setup: []
must_haves:
  truths:
    - Phase 27 must add a first-class Docker authoring contract that consumes the Phase 26 authored case plan instead of re-parsing raw snippet text.
    - The repo must preserve authored Docker intent as structured data, not only as a rendered `Dockerfile`.
    - Reviewers must be able to tell which Docker-plan sections were LLM-authored versus deterministic fallback or normalization.
  artifacts:
    - path: "tools/apdr/llm_py/models.py"
      provides: "Structured authored Docker plan models"
      contains: "AuthoredDockerPlan"
    - path: "tools/apdr/llm_py/actions/docker_plan.py"
      provides: "LLM Docker authoring step derived from the authored case plan"
      contains: "docker_plan"
    - path: "tools/apdr/src/lib.rs"
      provides: "Case artifact export for authored Docker plan truth"
      contains: "docker-plan.json"
    - path: "tools/apdr/tests/test_resolver.rs"
      provides: "Phase 27 authoring regression tests"
      contains: "phase27_author_"
  key_links:
    - from: "tools/apdr/llm_py/actions/docker_plan.py"
      to: ".planning/phases/26-llm-case-intake-and-plan-authoring/26-CASE-PLAN-PROOF.md"
      via: "Phase 27 must consume the frozen authored case plan rather than invent a new intake schema."
      pattern: "Phase 27 should consume this authored plan contract"
    - from: "tools/apdr/src/lib.rs"
      to: ".planning/REQUIREMENTS.md"
      via: "DKR-02 requires per-case debug artifacts for authored Docker inputs and final executed artifacts."
      pattern: "DKR-02"
---

<objective>
Add the authored Docker-plan contract and persist it into real case artifacts before the runtime backend starts consuming it.

Purpose: Phase 26 stopped at authored case intent. Phase 27 needs a second structured layer that turns that case intent into Docker-oriented build/runtime intent while keeping authorship truth machine-readable.
Output: a structured authored Docker plan, a case artifact such as `docker-plan.json`, and regression tests that prove the new authoring payload survives the Python-to-Rust seam.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-llm-case-intake-and-plan-authoring/26-CASE-PLAN-PROOF.md
@.planning/phases/27-llm-authored-docker-validation-and-artifact-truth/27-RESEARCH.md
@.planning/phases/27-llm-authored-docker-validation-and-artifact-truth/27-VALIDATION.md
@tools/apdr/llm_py/models.py
@tools/apdr/llm_py/__main__.py
@tools/apdr/src/resolver/tier3_llm/core.rs
@tools/apdr/src/resolver/mod.rs
@tools/apdr/src/lib.rs
@tools/apdr/tests/test_resolver.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the structured authored Docker plan and LLM authoring step</name>
  <files>tools/apdr/llm_py/models.py, tools/apdr/llm_py/__main__.py, tools/apdr/llm_py/actions/docker_plan.py, tools/apdr/src/resolver/tier3_llm/core.rs</files>
  <read_first>.planning/phases/26-llm-case-intake-and-plan-authoring/26-CASE-PLAN-PROOF.md, tools/apdr/llm_py/models.py, tools/apdr/llm_py/__main__.py, tools/apdr/src/resolver/tier3_llm/core.rs</read_first>
  <action>Add a structured Docker authoring model, such as `AuthoredDockerPlan`, that consumes the Phase 26 authored case plan. Create a Python action that asks the LLM for Docker-oriented validation inputs including base image choice, system packages, environment hints, and execution strategy. Wire the Rust tier3 LLM seam so it can request and parse this authored Docker plan without breaking the single-object JSON-line IPC contract.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase27_author_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/llm_py/models.py` contains `AuthoredDockerPlan`
    - `tools/apdr/llm_py/__main__.py` dispatches the Docker authoring step
    - `tools/apdr/src/resolver/tier3_llm/core.rs` parses authored Docker-plan data
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase27_author_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>The repo has a first-class Docker authoring contract and a Python/Rust seam that can transport it.</done>
</task>

<task type="auto">
  <name>Task 2: Persist authored Docker-plan truth into case artifacts</name>
  <files>tools/apdr/src/resolver/mod.rs, tools/apdr/src/lib.rs, tools/apdr/tests/test_resolver.rs</files>
  <read_first>tools/apdr/src/resolver/mod.rs, tools/apdr/src/lib.rs, tools/apdr/tests/test_resolver.rs, .planning/phases/27-llm-authored-docker-validation-and-artifact-truth/27-RESEARCH.md</read_first>
  <action>Carry the authored Docker plan through the resolver pipeline and write a machine-readable artifact such as `docker-plan.json` into the case output directory. Extend report text and summary lines with authored Docker-plan status, artifact paths, and authorship markers so later phases can tell whether Docker inputs were LLM-authored or deterministically normalized. Add Rust tests prefixed `phase27_artifact_` that verify these artifacts and metadata are written consistently.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase27_artifact_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/lib.rs` contains `docker-plan.json`
    - `tools/apdr/src/lib.rs` contains authored Docker-plan summary keys
    - `tools/apdr/tests/test_resolver.rs` contains `phase27_artifact_`
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase27_artifact_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>Phase 27 authored Docker-plan truth is now preserved in real case artifacts and output metadata.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml phase27_author_ -- --nocapture`
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml phase27_artifact_ -- --nocapture`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The repo now has a first-class authored Docker plan derived from the Phase 26 authored case plan
- Case artifacts preserve authored Docker-plan truth before the backend consumes it

</success_criteria>

<output>
After completion, create `.planning/phases/27-llm-authored-docker-validation-and-artifact-truth/27-01-SUMMARY.md`
</output>
