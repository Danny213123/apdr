---
phase: 14-macos-execution-path-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - benchmark_ui/runner.py
  - benchmark_ui/service.py
  - benchmark_ui/state.py
  - scripts/measure_apdr_baseline.py
  - .planning/phases/14-macos-execution-path-optimization/14-macos-replay-slice.json
  - .planning/phases/14-macos-execution-path-optimization/14-windows-guardrail-slice.json
  - benchmark_ui/test_runner_events.py
autonomous: true
requirements:
  - MAC-03
  - WIN-01
user_setup: []
must_haves:
  truths:
    - The Phase 14 replay boundary is locked by explicit manifest files for the macOS replay slice and the representative Windows guardrail slice, not by `snippet_limit` or archive ordering.
    - Benchmark run configs, saved loadouts, historical snapshots, and resumed runs can all carry replay-manifest metadata so the same slice can be replayed without guessing which snippets were intended.
    - `scripts/measure_apdr_baseline.py` can capture an exact manifest-defined slice and preserve the slice id in the generated JSON and Markdown artifacts.
  artifacts:
    - path: ".planning/phases/14-macos-execution-path-optimization/14-macos-replay-slice.json"
      provides: "Locked Phase 14 macOS replay manifest with exact case membership and reviewer-readable rationale"
      contains: "\"slice_id\": \"phase14-macos-replay\""
    - path: ".planning/phases/14-macos-execution-path-optimization/14-windows-guardrail-slice.json"
      provides: "Representative Windows guardrail manifest used to detect regressions from macOS-focused changes"
      contains: "\"slice_id\": \"phase14-windows-guardrail\""
    - path: "benchmark_ui/runner.py"
      provides: "Manifest-aware benchmark execution and saved-run persistence for replay slices"
      contains: "replay_manifest"
  key_links:
    - from: "benchmark_ui/runner.py"
      to: ".planning/ROADMAP.md"
      via: "Phase 14 promises a locked benchmark replay slice, which must remain reproducible across start, resume, and historical inspection."
      pattern: "locked benchmark replay slice"
    - from: "scripts/measure_apdr_baseline.py"
      to: ".planning/REQUIREMENTS.md"
      via: "MAC-03 and WIN-01 both depend on measuring exact slice membership instead of a best-effort sample limit."
      pattern: "locked benchmark slice"
---

<objective>
Lock the Phase 14 replay boundary so every later macOS speed claim and Windows guardrail check refers to the exact same case sets.

Purpose: Phase 13 made runs comparable, but Phase 14 still needs a stable replay target. Without manifest-backed slices, the benchmark boundary can drift whenever dataset contents or ordering change, which would make macOS before/after and Windows guardrail evidence hard to trust.
Output: manifest-backed replay-slice support in `benchmark_ui`, exact macOS and Windows slice JSON files in the phase directory, and manifest-aware capture support in `scripts/measure_apdr_baseline.py`.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/14-macos-execution-path-optimization/14-RESEARCH.md
@.planning/phases/13-measurement-and-run-contract-hardening/13-03-SUMMARY.md
@.planning/phases/13-measurement-and-run-contract-hardening/13-MEASUREMENT-CONTRACT.md
@benchmark_ui/state.py
@benchmark_ui/service.py
@benchmark_ui/runner.py
@scripts/measure_apdr_baseline.py
@benchmark_ui/test_runner_events.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add manifest-backed replay-slice selection and persistence to the benchmark control plane</name>
  <files>benchmark_ui/state.py, benchmark_ui/service.py, benchmark_ui/runner.py, benchmark_ui/test_runner_events.py</files>
  <read_first>benchmark_ui/state.py, benchmark_ui/service.py, benchmark_ui/runner.py, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/14-macos-execution-path-optimization/14-RESEARCH.md</read_first>
  <action>Update the benchmark control plane so Phase 14 runs can select an explicit replay manifest. Add normalized run-config fields such as `replay_manifest`, `replay_slice_id`, or equivalent metadata that can be saved into loadouts, summaries, and historical run hydration. In `benchmark_ui/runner.py`, resolve the manifest to an ordered set of snippet relative paths, filter dataset discovery to exactly those cases, and persist the manifest metadata into `summary.json` and the live run payload so resume and saved-run viewing stay locked to the same boundary. Extend `benchmark_ui/test_runner_events.py` or equivalent unit coverage so manifest-backed case ordering and persistence are locked down.</action>
  <verify>python3 -m unittest benchmark_ui.test_runner_events benchmark_ui.test_service_tier_stats</verify>
  <acceptance_criteria>
    - `benchmark_ui/state.py` contains `replay_manifest`
    - `benchmark_ui/service.py` contains `replay_manifest`
    - `benchmark_ui/runner.py` contains `replay_manifest`
    - `benchmark_ui/runner.py` contains `replay_slice_id`
    - `benchmark_ui/runner.py` contains `summary["replay_manifest"]`
    - `benchmark_ui/test_runner_events.py` contains `replay_manifest`
    - `python3 -m unittest benchmark_ui.test_runner_events benchmark_ui.test_service_tier_stats` exits 0
  </acceptance_criteria>
  <done>The benchmark UI can now run and resume a locked replay slice without relying on brittle `snippet_limit` ordering.</done>
</task>

<task type="auto">
  <name>Task 2: Create the locked slice manifests and make the baseline script consume them directly</name>
  <files>.planning/phases/14-macos-execution-path-optimization/14-macos-replay-slice.json, .planning/phases/14-macos-execution-path-optimization/14-windows-guardrail-slice.json, scripts/measure_apdr_baseline.py</files>
  <read_first>scripts/measure_apdr_baseline.py, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/14-macos-execution-path-optimization/14-RESEARCH.md</read_first>
  <action>Create two explicit slice manifests: one for the locked macOS replay slice and one for the representative Windows guardrail slice. Each manifest should include at minimum a stable `slice_id`, reviewer-readable purpose/rationale, and the ordered snippet relative paths that define the slice. Then update `scripts/measure_apdr_baseline.py` to accept a `--manifest-json` argument, load the cases in manifest order, emit the `slice_id` and manifest path in the generated report, and fail if the manifest references missing snippets. Keep the output shape aligned with the Phase 13 measurement contract instead of introducing a separate replay artifact schema.</action>
  <verify>python3 scripts/measure_apdr_baseline.py --fixtures-root tools/apdr/tests/fixtures --manifest-json .planning/phases/14-macos-execution-path-optimization/14-macos-replay-slice.json --output-json /tmp/phase14-macos-slice.json --output-md /tmp/phase14-macos-slice.md</verify>
  <acceptance_criteria>
    - `.planning/phases/14-macos-execution-path-optimization/14-macos-replay-slice.json` exists
    - `.planning/phases/14-macos-execution-path-optimization/14-macos-replay-slice.json` contains `"slice_id": "phase14-macos-replay"`
    - `.planning/phases/14-macos-execution-path-optimization/14-windows-guardrail-slice.json` exists
    - `.planning/phases/14-macos-execution-path-optimization/14-windows-guardrail-slice.json` contains `"slice_id": "phase14-windows-guardrail"`
    - `scripts/measure_apdr_baseline.py` contains `--manifest-json`
    - `scripts/measure_apdr_baseline.py` contains `slice_id`
    - `scripts/measure_apdr_baseline.py` contains `manifest_json`
    - `python3 scripts/measure_apdr_baseline.py --fixtures-root tools/apdr/tests/fixtures --manifest-json .planning/phases/14-macos-execution-path-optimization/14-macos-replay-slice.json --output-json /tmp/phase14-macos-slice.json --output-md /tmp/phase14-macos-slice.md` exits 0
  </acceptance_criteria>
  <done>The phase directory now owns the exact replay boundaries, and the baseline-reporting script can measure those boundaries directly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n 'replay_manifest|replay_slice_id|summary\\["replay_manifest"\\]' benchmark_ui/state.py benchmark_ui/service.py benchmark_ui/runner.py`
- [ ] `rg -n '"slice_id": "phase14-macos-replay"|"slice_id": "phase14-windows-guardrail"' .planning/phases/14-macos-execution-path-optimization/14-macos-replay-slice.json .planning/phases/14-macos-execution-path-optimization/14-windows-guardrail-slice.json`
- [ ] `rg -n -- '--manifest-json|slice_id|manifest_json' scripts/measure_apdr_baseline.py`
- [ ] `python3 -m unittest benchmark_ui.test_runner_events benchmark_ui.test_service_tier_stats`
- [ ] `python3 scripts/measure_apdr_baseline.py --fixtures-root tools/apdr/tests/fixtures --manifest-json .planning/phases/14-macos-execution-path-optimization/14-macos-replay-slice.json --output-json /tmp/phase14-macos-slice.json --output-md /tmp/phase14-macos-slice.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Phase 14 owns explicit macOS and Windows slice manifests rather than relying on `snippet_limit`
- Saved benchmark runs and generated reports preserve the replay-manifest identity needed for later comparisons
- `MAC-03` is grounded in a reproducible slice boundary, and `WIN-01` has a concrete representative comparison slice
</success_criteria>

<output>
After completion, create `.planning/phases/14-macos-execution-path-optimization/14-01-SUMMARY.md`
</output>
