---
phase: 14-macos-execution-path-optimization
plan: 02
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - scripts/run_phase14_replay.py
  - benchmark_ui/runner.py
  - benchmark_ui/state.py
  - benchmark_ui/service.py
  - tools/apdr/test_executor.py
  - benchmark_ui/test_runner_events.py
  - benchmark_ui/test_run_contract.py
autonomous: true
requirements:
  - MAC-03
  - MAC-04
user_setup: []
must_haves:
  truths:
    - Phase 14 provides one dedicated replay runner that can capture a cold baseline, prewarm the native caches, and record a warm candidate run on the locked macOS slice using the Phase 13 measurement contract.
    - The fast macOS replay lane uses native `env` validation, explicit build-profile selection, and a prebuilt APDR binary path instead of silently paying repeated `cargo run` or Docker startup overhead inside measured case execution.
    - macOS replay runs surface invalidating conditions such as Rosetta execution, Docker backend selection, mixed cache state, or a missing requested binary profile before the artifact is treated as comparable evidence.
  artifacts:
    - path: "scripts/run_phase14_replay.py"
      provides: "One orchestrated entrypoint for cold/warm Phase 14 replay captures"
      contains: "--prewarm"
    - path: "tools/apdr/test_executor.py"
      provides: "Profile-aware APDR binary selection for replay captures"
      contains: "build_profile"
    - path: "benchmark_ui/runner.py"
      provides: "macOS replay intent handling, worker policy, and replay-specific warnings"
      contains: "macos-replay"
  key_links:
    - from: "scripts/run_phase14_replay.py"
      to: ".planning/phases/13-measurement-and-run-contract-hardening/13-MEASUREMENT-CONTRACT.md"
      via: "Phase 14 replay captures must reuse the Phase 13 run-contract and timing schema instead of inventing a parallel format."
      pattern: "## Required Run Contract"
    - from: "benchmark_ui/runner.py"
      to: ".planning/ROADMAP.md"
      via: "Phase 14's core promise is a fast native macOS replay lane for repeated local iteration."
      pattern: "fast macOS native-env mode intended for repeated local iteration"
---

<objective>
Create the fast native macOS replay lane and its dedicated capture command so Phase 14 can generate before-and-after evidence without hidden benchmark overhead.

Purpose: once the slice boundary is locked, the next bottleneck is repeatability and runtime. The repo already has native env reuse in APDR and run-contract persistence in `benchmark_ui`, but it still lacks one replay-oriented entrypoint that can prewarm caches, enforce build-profile discipline, and make invalidating conditions obvious before a capture is used as proof.
Output: `scripts/run_phase14_replay.py`, replay-aware benchmark/run-config logic, and test-executor changes that honor the requested binary profile for measured macOS replay runs.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-macos-execution-path-optimization/14-RESEARCH.md
@.planning/phases/14-macos-execution-path-optimization/14-01-PLAN.md
@.planning/phases/13-measurement-and-run-contract-hardening/13-MEASUREMENT-CONTRACT.md
@benchmark_ui/state.py
@benchmark_ui/service.py
@benchmark_ui/runner.py
@scripts/measure_apdr_baseline.py
@tools/apdr/test_executor.py
@tools/apdr/src/docker/builder/env_backend.rs
@tools/apdr/README.md
@benchmark_ui/test_runner_events.py
@benchmark_ui/test_run_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the dedicated Phase 14 replay runner with cold/warm capture semantics</name>
  <files>scripts/run_phase14_replay.py</files>
  <read_first>scripts/measure_apdr_baseline.py, .planning/phases/14-macos-execution-path-optimization/14-RESEARCH.md, .planning/phases/14-macos-execution-path-optimization/14-01-PLAN.md, .planning/phases/13-measurement-and-run-contract-hardening/13-MEASUREMENT-CONTRACT.md</read_first>
  <action>Create `scripts/run_phase14_replay.py` as the Phase 14 orchestration entrypoint. It should accept a replay manifest, dataset or fixtures root, validation backend, build profile, worker override, and explicit output paths for baseline and candidate captures. Add a `--prewarm` mode that intentionally primes native caches before the measured candidate run, plus a `--probe-only` or equivalent fixture-safe mode that validates wiring without requiring a live dataset. The script should emit the same run-contract and timing schema used by Phase 13, while also labeling each capture as baseline or candidate for later regression checks.</action>
  <verify>python3 -m py_compile scripts/run_phase14_replay.py</verify>
  <acceptance_criteria>
    - `scripts/run_phase14_replay.py` exists
    - `scripts/run_phase14_replay.py` contains `--prewarm`
    - `scripts/run_phase14_replay.py` contains `--probe-only`
    - `scripts/run_phase14_replay.py` contains `--manifest-json`
    - `scripts/run_phase14_replay.py` contains `build_profile`
    - `scripts/run_phase14_replay.py` contains `baseline`
    - `scripts/run_phase14_replay.py` contains `candidate`
    - `python3 -m py_compile scripts/run_phase14_replay.py` exits 0
  </acceptance_criteria>
  <done>The repo now has one replay-specific command that can produce comparable baseline and candidate captures on the locked slice.</done>
</task>

<task type="auto">
  <name>Task 2: Tune benchmark execution and APDR binary selection for the native macOS replay lane</name>
  <files>benchmark_ui/state.py, benchmark_ui/service.py, benchmark_ui/runner.py, tools/apdr/test_executor.py, benchmark_ui/test_runner_events.py, benchmark_ui/test_run_contract.py</files>
  <read_first>benchmark_ui/state.py, benchmark_ui/service.py, benchmark_ui/runner.py, tools/apdr/test_executor.py, tools/apdr/src/docker/builder/env_backend.rs, tools/apdr/README.md</read_first>
  <action>Update the benchmark runner and test executor so `macos-replay` or equivalent run intents choose the native env path by default, honor an explicit build profile, and avoid burying cargo-build or Docker startup overhead inside measured runs. In `tools/apdr/test_executor.py`, make the binary-selection path able to require or strongly prefer the requested prebuilt profile instead of silently falling back to `cargo run` for replay evidence. In `benchmark_ui/state.py`, `benchmark_ui/service.py`, and `benchmark_ui/runner.py`, add replay-specific preflight warnings for Rosetta, wrong backend, mixed cache state, or a missing binary profile, and set a replay-oriented worker default or cap that avoids needless macOS disk thrash while keeping the path distinct from Windows or Docker-proof runs. Extend unit coverage so the replay intent and profile metadata stay persisted and visible.</action>
  <verify>python3 -m unittest benchmark_ui.test_runner_events benchmark_ui.test_run_contract</verify>
  <acceptance_criteria>
    - `benchmark_ui/state.py` contains `macos-replay`
    - `benchmark_ui/service.py` contains `macos-replay`
    - `benchmark_ui/runner.py` contains `macos-replay`
    - `benchmark_ui/runner.py` contains `build_profile`
    - `benchmark_ui/runner.py` contains `workers`
    - `tools/apdr/test_executor.py` contains `build_profile`
    - `tools/apdr/test_executor.py` contains `cargo run`
    - `tools/apdr/test_executor.py` contains `release`
    - `benchmark_ui/test_run_contract.py` contains `build_profile`
    - `python3 -m unittest benchmark_ui.test_runner_events benchmark_ui.test_run_contract` exits 0
  </acceptance_criteria>
  <done>The replay lane now has explicit native-env policy, profile-aware binary selection, and preflight warnings that keep the measured macOS path honest.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n -- '--prewarm|--probe-only|--manifest-json|build_profile|baseline|candidate' scripts/run_phase14_replay.py`
- [ ] `rg -n 'macos-replay|build_profile|workers' benchmark_ui/state.py benchmark_ui/service.py benchmark_ui/runner.py`
- [ ] `rg -n 'build_profile|cargo run|release' tools/apdr/test_executor.py`
- [ ] `python3 -m py_compile scripts/run_phase14_replay.py`
- [ ] `python3 -m unittest benchmark_ui.test_runner_events benchmark_ui.test_run_contract`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Phase 14 has one replay-oriented entrypoint that can produce cold and warm captures on the locked slice
- The native macOS replay lane avoids hidden cargo-build or Docker overhead inside measured runs
- `MAC-03` and the execution-path portion of `MAC-04` now have a concrete capture flow to measure
</success_criteria>

<output>
After completion, create `.planning/phases/14-macos-execution-path-optimization/14-02-SUMMARY.md`
</output>
