---
phase: 13-measurement-and-run-contract-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - benchmark_ui/runner.py
  - tools/apdr/src/lib.rs
  - tools/apdr/src/resolver/mod.rs
  - tools/apdr/src/resolver/tier3_llm/core.rs
  - tools/apdr/src/resolver/retry_loop.rs
  - tools/apdr/src/docker/builder/docker_backend.rs
  - tools/apdr/test_executor.py
  - tools/apdr/tests/test_cli.rs
autonomous: true
requirements:
  - MAC-01
  - MAC-02
  - EVD-05
user_setup: []
must_haves:
  truths:
    - APDR per-case artifacts expose distinct `llm_duration_ms` and `docker_startup_duration_ms` alongside the existing solve, validation, env-create, install, and smoke timings.
    - The Phase 13 run contract flows from `benchmark_ui` into `output_data_*.yml` and the APDR resolution report so later reporting code does not need to infer model choice, architecture, backend, context-window, or decoding policy from logs.
    - CLI and report tests lock the metadata schema so model, context-window, inference-policy, and build-profile comparisons stay attributable across later milestone phases.
  artifacts:
    - path: "tools/apdr/src/lib.rs"
      provides: "Expanded validation and report contract for Phase 13 stage timings and run metadata"
      contains: "docker_startup_duration_ms"
    - path: "tools/apdr/test_executor.py"
      provides: "Bridge from benchmark runner contract metadata into `output_data_*.yml`"
      contains: "run_intent:"
    - path: "tools/apdr/tests/test_cli.rs"
      provides: "CLI schema lock for the new timing and run-contract fields"
      contains: "LLM_DURATION_MS="
  key_links:
    - from: "tools/apdr/src/lib.rs"
      to: ".planning/REQUIREMENTS.md"
      via: "Phase 13 requires reportable stage timings for resolution, LLM work, env creation, package install, validation, and Docker startup"
      pattern: "stage-level timings for resolution, LLM work, env creation, package install, validation, and Docker startup"
    - from: "tools/apdr/test_executor.py"
      to: "benchmark_ui/run_contract.py"
      via: "the per-case APDR artifact keys must mirror the benchmark run contract created in Plan 13-01"
      pattern: "REQUIRED_RUN_CONTRACT_KEYS"
---

<objective>
Extend the APDR per-case metadata contract so the benchmark stack can attribute timing and model-policy deltas to the correct stage, backend, and run configuration.

Purpose: Phase 13 is not done when the top-level run summary is labeled. The benchmark runner also needs each case artifact to carry the same comparison metadata and the missing stage timings for LLM work and Docker startup, otherwise later performance claims still collapse important differences into opaque totals.
Output: Rust-side timing/report schema updates, `test_executor.py` propagation of run-contract fields into `output_data_*.yml`, runner handoff of the Phase 13 contract into APDR invocation through `--run-contract-json`, and CLI schema coverage in `tools/apdr/tests/test_cli.rs`.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/13-measurement-and-run-contract-hardening/13-01-PLAN.md
@benchmark_ui/run_contract.py
@benchmark_ui/runner.py
@tools/apdr/src/lib.rs
@tools/apdr/src/resolver/mod.rs
@tools/apdr/src/resolver/retry_loop.rs
@tools/apdr/src/resolver/tier3_llm/core.rs
@tools/apdr/src/docker/builder/docker_backend.rs
@tools/apdr/test_executor.py
@tools/apdr/tests/test_cli.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 13 timing fields and run-contract metadata to the APDR report schema</name>
  <files>tools/apdr/src/lib.rs, tools/apdr/src/resolver/mod.rs, tools/apdr/src/resolver/retry_loop.rs, tools/apdr/src/resolver/tier3_llm/core.rs, tools/apdr/src/docker/builder/docker_backend.rs</files>
  <read_first>tools/apdr/src/lib.rs, tools/apdr/src/resolver/mod.rs, tools/apdr/src/resolver/retry_loop.rs, tools/apdr/src/resolver/tier3_llm/core.rs, tools/apdr/src/docker/builder/docker_backend.rs, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>Extend the Rust-side validation/report contract to carry the missing Phase 13 measurement fields. Add `llm_duration_ms` and `docker_startup_duration_ms` to `ValidationSummary` and to every serialized output path that currently emits `solve_duration_ms`, `validation_duration_ms`, `env_create_duration_ms`, `install_duration_ms`, and `smoke_duration_ms`. Record `llm_duration_ms` around the tier3 LLM path in `tools/apdr/src/resolver/tier3_llm/core.rs`, and record `docker_startup_duration_ms` in `tools/apdr/src/docker/builder/docker_backend.rs` as a distinct launch-stage metric instead of folding it invisibly into the existing smoke timing. Also add flattened run-contract fields named `run_contract_version`, `model_name`, `base_url`, `run_intent`, `execution_mode`, `cache_state`, `host_architecture`, `apdr_binary_architecture`, `python_architecture`, `llm_context_window`, `inference_policy`, and `build_profile` to both the human-readable resolution report and the uppercase `summary_lines()` output so downstream scripts can attribute comparisons without parsing free-form logs.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml --test test_cli cli_resolves_from_stdin_without_validation -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/lib.rs` contains `pub llm_duration_ms: u128`
    - `tools/apdr/src/lib.rs` contains `pub docker_startup_duration_ms: u128`
    - `tools/apdr/src/lib.rs` contains `run_intent`
    - `tools/apdr/src/lib.rs` contains `model_name`
    - `tools/apdr/src/lib.rs` contains `execution_mode`
    - `tools/apdr/src/lib.rs` contains `llm_context_window`
    - `tools/apdr/src/lib.rs` contains `inference_policy`
    - `tools/apdr/src/resolver/tier3_llm/core.rs` contains `llm_duration_ms`
    - `tools/apdr/src/docker/builder/docker_backend.rs` contains `docker_startup_duration_ms`
    - `tools/apdr/src/lib.rs` contains `LLM_DURATION_MS=`
    - `tools/apdr/src/lib.rs` contains `DOCKER_STARTUP_DURATION_MS=`
    - `cargo test --manifest-path tools/apdr/Cargo.toml --test test_cli cli_resolves_from_stdin_without_validation -- --nocapture` exits 0
  </acceptance_criteria>
  <done>The APDR core now emits the Phase 13 timing surface and the per-case run-contract keys needed for trustworthy comparison reporting.</done>
</task>

<task type="auto">
  <name>Task 2: Pass the benchmark run contract into APDR execution and lock the output schema</name>
  <files>benchmark_ui/runner.py, tools/apdr/test_executor.py, tools/apdr/tests/test_cli.rs</files>
  <read_first>benchmark_ui/runner.py, benchmark_ui/run_contract.py, tools/apdr/test_executor.py, tools/apdr/tests/test_cli.rs, .planning/phases/13-measurement-and-run-contract-hardening/13-01-PLAN.md</read_first>
  <action>Update `benchmark_ui/runner.py` to pass the saved Phase 13 run contract into each APDR case invocation through a concrete `--run-contract-json <path>` argument. In `tools/apdr/test_executor.py`, add that CLI option, read the JSON file, validate the required keys from Plan 13-01, and copy the flattened fields into `output_data_*.yml` alongside the new `llm_duration_ms` and `docker_startup_duration_ms` keys. Update `tools/apdr/tests/test_cli.rs` so the CLI/report schema tests assert the presence of `LLM_DURATION_MS`, `DOCKER_STARTUP_DURATION_MS`, `MODEL_NAME`, `BASE_URL`, `RUN_INTENT`, `EXECUTION_MODE`, `CACHE_STATE`, `LLM_CONTEXT_WINDOW`, `INFERENCE_POLICY`, and `BUILD_PROFILE` in stdout or the persisted report.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml --test test_cli -- --nocapture</verify>
  <acceptance_criteria>
    - `benchmark_ui/runner.py` contains `--run-contract-json`
    - `tools/apdr/test_executor.py` contains `--run-contract-json`
    - `tools/apdr/test_executor.py` contains `llm_duration_ms:`
    - `tools/apdr/test_executor.py` contains `docker_startup_duration_ms:`
    - `tools/apdr/test_executor.py` contains `model_name:`
    - `tools/apdr/test_executor.py` contains `base_url:`
    - `tools/apdr/test_executor.py` contains `run_intent:`
    - `tools/apdr/test_executor.py` contains `execution_mode:`
    - `tools/apdr/test_executor.py` contains `cache_state:`
    - `tools/apdr/test_executor.py` contains `llm_context_window:`
    - `tools/apdr/test_executor.py` contains `inference_policy:`
    - `tools/apdr/tests/test_cli.rs` contains `LLM_DURATION_MS=`
    - `tools/apdr/tests/test_cli.rs` contains `DOCKER_STARTUP_DURATION_MS=`
    - `tools/apdr/tests/test_cli.rs` contains `MODEL_NAME=`
    - `tools/apdr/tests/test_cli.rs` contains `RUN_INTENT=`
    - `tools/apdr/tests/test_cli.rs` contains `EXECUTION_MODE=`
    - `tools/apdr/tests/test_cli.rs` contains `BUILD_PROFILE=`
    - `cargo test --manifest-path tools/apdr/Cargo.toml --test test_cli -- --nocapture` exits 0
  </acceptance_criteria>
  <done>Each APDR case artifact now carries the same Phase 13 run contract as the top-level benchmark run, with the missing timing fields locked by tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n 'llm_duration_ms|docker_startup_duration_ms|model_name|base_url|run_intent|execution_mode|cache_state|llm_context_window|inference_policy|build_profile|MODEL_NAME=|RUN_INTENT=|EXECUTION_MODE=|BUILD_PROFILE=' tools/apdr/src/lib.rs tools/apdr/src/resolver/tier3_llm/core.rs tools/apdr/src/docker/builder/docker_backend.rs tools/apdr/test_executor.py`
- [ ] `rg -n 'LLM_DURATION_MS=|DOCKER_STARTUP_DURATION_MS=|RUN_INTENT=|EXECUTION_MODE=|CACHE_STATE=|LLM_CONTEXT_WINDOW=|INFERENCE_POLICY=|BUILD_PROFILE=' tools/apdr/tests/test_cli.rs`
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml --test test_cli -- --nocapture`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- APDR case artifacts expose distinct LLM and Docker-startup timings in addition to the existing stage timings
- The Phase 13 run contract flows from `benchmark_ui` into `output_data_*.yml` and the APDR resolution report without lossy inference
- `MAC-02` and the metadata-attribution portion of `EVD-05` are now enforceable on a per-case basis
</success_criteria>

<output>
After completion, create `.planning/phases/13-measurement-and-run-contract-hardening/13-02-SUMMARY.md`
</output>
