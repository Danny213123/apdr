---
phase: 13-measurement-and-run-contract-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - "13-01"
  - "13-02"
files_modified:
  - scripts/measure_apdr_baseline.py
  - scripts/check_phase13_measurement_contract.py
  - .planning/phases/13-measurement-and-run-contract-hardening/13-env-fast-sample.json
  - .planning/phases/13-measurement-and-run-contract-hardening/13-docker-proof-sample.json
  - .planning/phases/13-measurement-and-run-contract-hardening/13-MEASUREMENT-CONTRACT.md
autonomous: true
requirements:
  - MAC-02
  - EVD-03
  - EVD-05
user_setup: []
must_haves:
  truths:
    - The Phase 13 evidence layer distinguishes `env-fast` versus `docker-proof` runs and warm versus cold cache state explicitly in reviewer-facing artifacts instead of leaving reviewers to infer them from command lines.
    - `scripts/measure_apdr_baseline.py` reports the full run contract and the full stage-timing set, including `model_name`, `llm_duration_ms`, and `docker_startup_duration_ms`, at both top-level and per-sample scope where available.
    - Phase 13 closes with one deterministic measurement-contract checker and one reviewer-facing contract note plus bounded example artifacts that document what a valid run record looks like.
  artifacts:
    - path: "scripts/measure_apdr_baseline.py"
      provides: "Phase 13 normalized evidence output that includes run-contract metadata and the full timing breakdown"
      contains: "docker_startup_duration_ms"
    - path: "scripts/check_phase13_measurement_contract.py"
      provides: "Deterministic checker for required run-contract keys, timing keys, and evidence labels"
      contains: "--sample-json"
    - path: ".planning/phases/13-measurement-and-run-contract-hardening/13-MEASUREMENT-CONTRACT.md"
      provides: "Reviewer-facing Phase 13 note that defines the measurement contract and sample artifact expectations"
      contains: "## Required Run Contract"
  key_links:
    - from: "scripts/measure_apdr_baseline.py"
      to: ".planning/ROADMAP.md"
      via: "Phase 13 evidence must expose the labels and timing columns the roadmap promised for comparable runs"
      pattern: "Saved benchmark evidence clearly distinguishes `env-fast` versus `docker-proof` runs and warm versus cold cache state"
    - from: "scripts/check_phase13_measurement_contract.py"
      to: ".planning/REQUIREMENTS.md"
      via: "the checker must validate exactly the run metadata and comparison keys required by MAC-02, EVD-03, and EVD-05"
      pattern: "records enough metadata to attribute gains to agent behavior, model choice, context configuration, decoding policy, runtime tuning, or backend differences"
---

<objective>
Normalize Phase 13 evidence output and add a deterministic checker so benchmark artifacts are reviewer-readable and comparable before any performance or accuracy tuning begins.

Purpose: once Plans 13-01 and 13-02 land, the repo still needs one evidence layer that turns the raw case metadata into comparable reports, plus one checker that rejects artifacts missing the required labels or timing fields. This is the gate that makes later macOS and LLM-improvement claims auditable instead of anecdotal.
Output: updated `scripts/measure_apdr_baseline.py`, a new `scripts/check_phase13_measurement_contract.py`, bounded sample JSON artifacts for `env-fast` and `docker-proof`, and `13-MEASUREMENT-CONTRACT.md`.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/13-measurement-and-run-contract-hardening/13-01-PLAN.md
@.planning/phases/13-measurement-and-run-contract-hardening/13-02-PLAN.md
@scripts/measure_apdr_baseline.py
@benchmark_ui/run_contract.py
@benchmark_ui/runner.py
@tools/apdr/test_executor.py
@tools/apdr/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the baseline-reporting script to emit Phase 13 comparison metadata and timing totals</name>
  <files>scripts/measure_apdr_baseline.py</files>
  <read_first>scripts/measure_apdr_baseline.py, benchmark_ui/run_contract.py, tools/apdr/test_executor.py, tools/apdr/src/lib.rs, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>Update `scripts/measure_apdr_baseline.py` so the generated JSON and Markdown outputs carry the normalized Phase 13 comparison metadata, not just generic backend and cache hints. Add top-level report fields for `run_contract`, `model_name`, `execution_mode`, `cache_state`, `build_profile`, `llm_context_window`, and `inference_policy`. Extend per-sample extraction to read `llm_duration_ms` and `docker_startup_duration_ms`, include those values in totals, and preserve the flattened run-contract keys for each sample. Update Markdown rendering so reviewers can see whether a capture was `env-fast` or `docker-proof`, whether it was warm or cold cache, and what model/context/build settings were used when comparing runs. Verify the integration path by generating a bounded fixture-backed report from `tools/apdr/tests/fixtures` and then validating that generated JSON with the Phase 13 checker instead of validating only hand-authored samples.</action>
  <verify>/bin/zsh -lc 'python3 scripts/measure_apdr_baseline.py --fixtures-root tools/apdr/tests/fixtures --limit 2 --validation-backend env --output-json /tmp/phase13-generated-report.json --output-md /tmp/phase13-generated-report.md && python3 scripts/check_phase13_measurement_contract.py --sample-json /tmp/phase13-generated-report.json'</verify>
  <acceptance_criteria>
    - `scripts/measure_apdr_baseline.py` contains `docker_startup_duration_ms`
    - `scripts/measure_apdr_baseline.py` contains `llm_duration_ms`
    - `scripts/measure_apdr_baseline.py` contains `model_name`
    - `scripts/measure_apdr_baseline.py` contains `run_contract`
    - `scripts/measure_apdr_baseline.py` contains `execution_mode`
    - `scripts/measure_apdr_baseline.py` contains `cache_state`
    - `scripts/measure_apdr_baseline.py` contains `build_profile`
    - `scripts/measure_apdr_baseline.py` contains `llm_context_window`
    - `scripts/measure_apdr_baseline.py` contains `inference_policy`
    - `scripts/measure_apdr_baseline.py` contains `env-fast`
    - `scripts/measure_apdr_baseline.py` contains `docker-proof`
    - `/bin/zsh -lc 'python3 scripts/measure_apdr_baseline.py --fixtures-root tools/apdr/tests/fixtures --limit 2 --validation-backend env --output-json /tmp/phase13-generated-report.json --output-md /tmp/phase13-generated-report.md && python3 scripts/check_phase13_measurement_contract.py --sample-json /tmp/phase13-generated-report.json'` exits 0
  </acceptance_criteria>
  <done>The baseline-reporting script now emits the full Phase 13 comparison contract instead of a partial backend-only summary.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Phase 13 contract checker, bounded sample artifacts, and the reviewer-facing measurement note</name>
  <files>scripts/check_phase13_measurement_contract.py, .planning/phases/13-measurement-and-run-contract-hardening/13-env-fast-sample.json, .planning/phases/13-measurement-and-run-contract-hardening/13-docker-proof-sample.json, .planning/phases/13-measurement-and-run-contract-hardening/13-MEASUREMENT-CONTRACT.md</files>
  <read_first>scripts/measure_apdr_baseline.py, benchmark_ui/run_contract.py, .planning/phases/13-measurement-and-run-contract-hardening/13-01-PLAN.md, .planning/phases/13-measurement-and-run-contract-hardening/13-02-PLAN.md</read_first>
  <action>Create `scripts/check_phase13_measurement_contract.py` as a deterministic validator that accepts one or more sample report JSON files and fails when any required run-contract key, stage-timing key, `env-fast`/`docker-proof` label, or warm/cold cache label is missing. The required metadata set must include `model_name`, `base_url`, `llm_context_window`, `inference_policy`, and `build_profile` so model and decoding comparisons remain attributable. Add two bounded example artifacts named `13-env-fast-sample.json` and `13-docker-proof-sample.json` that demonstrate the required top-level and per-sample shape for each execution mode. Write `13-MEASUREMENT-CONTRACT.md` with exact sections `## Required Run Contract`, `## Stage Timings`, `## Evidence Labels`, and `## Comparison Metadata`, and explain how reviewers should interpret `env-fast` versus `docker-proof`, warm versus cold, and the model/context/build metadata fields.</action>
  <verify>python3 scripts/check_phase13_measurement_contract.py --sample-json .planning/phases/13-measurement-and-run-contract-hardening/13-env-fast-sample.json --sample-json .planning/phases/13-measurement-and-run-contract-hardening/13-docker-proof-sample.json</verify>
  <acceptance_criteria>
    - `scripts/check_phase13_measurement_contract.py` exists
    - `scripts/check_phase13_measurement_contract.py` contains `--sample-json`
    - `scripts/check_phase13_measurement_contract.py` contains `env-fast`
    - `scripts/check_phase13_measurement_contract.py` contains `docker-proof`
    - `scripts/check_phase13_measurement_contract.py` contains `model_name`
    - `.planning/phases/13-measurement-and-run-contract-hardening/13-env-fast-sample.json` exists
    - `.planning/phases/13-measurement-and-run-contract-hardening/13-env-fast-sample.json` contains `"execution_mode": "env-fast"`
    - `.planning/phases/13-measurement-and-run-contract-hardening/13-docker-proof-sample.json` exists
    - `.planning/phases/13-measurement-and-run-contract-hardening/13-docker-proof-sample.json` contains `"execution_mode": "docker-proof"`
    - `.planning/phases/13-measurement-and-run-contract-hardening/13-MEASUREMENT-CONTRACT.md` contains `## Required Run Contract`
    - `.planning/phases/13-measurement-and-run-contract-hardening/13-MEASUREMENT-CONTRACT.md` contains `## Stage Timings`
    - `.planning/phases/13-measurement-and-run-contract-hardening/13-MEASUREMENT-CONTRACT.md` contains `## Evidence Labels`
    - `.planning/phases/13-measurement-and-run-contract-hardening/13-MEASUREMENT-CONTRACT.md` contains `## Comparison Metadata`
    - `python3 scripts/check_phase13_measurement_contract.py --sample-json .planning/phases/13-measurement-and-run-contract-hardening/13-env-fast-sample.json --sample-json .planning/phases/13-measurement-and-run-contract-hardening/13-docker-proof-sample.json` exits 0
  </acceptance_criteria>
  <done>Phase 13 closes with one deterministic measurement checker and one reviewer-facing contract package that documents exactly what a valid benchmark artifact must contain.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n 'docker_startup_duration_ms|llm_duration_ms|model_name|run_contract|execution_mode|cache_state|build_profile|llm_context_window|inference_policy|env-fast|docker-proof' scripts/measure_apdr_baseline.py`
- [ ] `/bin/zsh -lc 'python3 scripts/measure_apdr_baseline.py --fixtures-root tools/apdr/tests/fixtures --limit 2 --validation-backend env --output-json /tmp/phase13-generated-report.json --output-md /tmp/phase13-generated-report.md && python3 scripts/check_phase13_measurement_contract.py --sample-json /tmp/phase13-generated-report.json'`
- [ ] `python3 scripts/check_phase13_measurement_contract.py --sample-json .planning/phases/13-measurement-and-run-contract-hardening/13-env-fast-sample.json --sample-json .planning/phases/13-measurement-and-run-contract-hardening/13-docker-proof-sample.json`
- [ ] `rg -n '## Required Run Contract|## Stage Timings|## Evidence Labels|## Comparison Metadata' .planning/phases/13-measurement-and-run-contract-hardening/13-MEASUREMENT-CONTRACT.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Reviewer-facing benchmark evidence explicitly distinguishes `env-fast` versus `docker-proof` and warm versus cold cache state
- The full Phase 13 run contract and stage-timing set is present in reporting outputs and validated by a deterministic checker
- `MAC-02`, `EVD-03`, and `EVD-05` are now backed by one documented measurement contract instead of informal conventions
</success_criteria>

<output>
After completion, create `.planning/phases/13-measurement-and-run-contract-hardening/13-03-SUMMARY.md`
</output>
