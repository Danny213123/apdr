---
phase: 13-measurement-and-run-contract-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - benchmark_ui/run_contract.py
  - benchmark_ui/runner.py
  - benchmark_ui/service.py
  - benchmark_ui/state.py
  - benchmark_ui/test_run_contract.py
autonomous: true
requirements:
  - MAC-01
  - EVD-03
  - EVD-05
user_setup: []
must_haves:
  truths:
    - Every saved benchmark run gets one normalized `run_contract` captured before case execution starts, and that contract is persisted both inside `summary.json` and as a standalone `run_contract.json`.
    - The Phase 13 run contract records the exact comparison metadata the milestone needs: `model_name`, `base_url`, `host_architecture`, `apdr_binary_architecture`, `python_architecture`, `validation_backend`, `run_intent`, `execution_mode`, `cache_state`, `llm_context_window`, `inference_policy`, and `build_profile`.
    - Historical run hydration and the benchmark UI info panel read the stored run contract instead of recomputing incompatible labels later.
  artifacts:
    - path: "benchmark_ui/run_contract.py"
      provides: "Canonical run-contract builder, normalizers, and required-key list for saved benchmark runs"
      contains: "REQUIRED_RUN_CONTRACT_KEYS"
    - path: "benchmark_ui/runner.py"
      provides: "Run-start persistence of `run_contract` into `summary.json` and `run_contract.json`"
      contains: "run_contract.json"
    - path: "benchmark_ui/service.py"
      provides: "Saved-run hydration and info-field rendering for the normalized run-contract metadata"
      contains: "Run intent"
  key_links:
    - from: "benchmark_ui/run_contract.py"
      to: ".planning/ROADMAP.md"
      via: "the contract keys must exactly satisfy the Phase 13 comparison metadata promised in the roadmap success criteria"
      pattern: "llm context-window and inference-policy settings"
    - from: "benchmark_ui/state.py"
      to: ".planning/REQUIREMENTS.md"
      via: "default run config must expose the metadata inputs needed to capture MAC-01 and EVD-05 cleanly"
      pattern: "run intent, cache state, and the configured LLM context-window and inference-policy settings"
---

<objective>
Create the canonical Phase 13 benchmark run contract in `benchmark_ui` so every later report, replay, and saved-run comparison starts from one consistent metadata shape.

Purpose: Phase 13 is measurement-first. Before execution-path tuning or agent tuning can be trusted, the repo needs one normalized run contract that records architecture, backend, cache, intent, context-window, inference policy, and build profile at run start, then keeps those fields stable everywhere the UI or reporting stack reads the run.
Output: `benchmark_ui/run_contract.py`, run-config support in `benchmark_ui/state.py` and `benchmark_ui/service.py`, run-start persistence in `benchmark_ui/runner.py`, and focused unit coverage in `benchmark_ui/test_run_contract.py`.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@benchmark_ui/state.py
@benchmark_ui/runner.py
@benchmark_ui/service.py
@benchmark_ui/test_runner_events.py
@benchmark_ui/test_service_tier_stats.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the canonical Phase 13 run-contract builder and config inputs</name>
  <files>benchmark_ui/run_contract.py, benchmark_ui/state.py, benchmark_ui/service.py</files>
  <read_first>benchmark_ui/state.py, benchmark_ui/service.py, benchmark_ui/runner.py, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/research/SUMMARY.md</read_first>
  <action>Create `benchmark_ui/run_contract.py` with a small canonical API for Phase 13 metadata capture: define `RUN_CONTRACT_VERSION = 1`, define `REQUIRED_RUN_CONTRACT_KEYS` containing `run_contract_version`, `tool`, `model_name`, `base_url`, `validation_backend`, `run_intent`, `execution_mode`, `cache_state`, `host_architecture`, `apdr_binary_architecture`, `python_architecture`, `llm_context_window`, `inference_policy`, and `build_profile`, and implement helper functions that normalize those fields into one consistent dict. In `benchmark_ui/state.py`, extend `default_run_config()` so APDR runs can carry explicit `run_intent`, `build_profile`, `llm_context_window`, and `inference_policy` inputs instead of hiding them in ad hoc locals. In `benchmark_ui/service.py`, extend `_normalize_run_config()` and the saved-run hydration path so those same keys are preserved when a run is loaded back from `summary.json`, including the concrete model identifier used for the run.</action>
  <verify>python3 -m unittest benchmark_ui.test_run_contract</verify>
  <acceptance_criteria>
    - `benchmark_ui/run_contract.py` exists
    - `benchmark_ui/run_contract.py` contains `RUN_CONTRACT_VERSION = 1`
    - `benchmark_ui/run_contract.py` contains `REQUIRED_RUN_CONTRACT_KEYS`
    - `benchmark_ui/run_contract.py` contains `def build_run_contract(`
    - `benchmark_ui/state.py` contains `run_intent`
    - `benchmark_ui/state.py` contains `build_profile`
    - `benchmark_ui/state.py` contains `llm_context_window`
    - `benchmark_ui/state.py` contains `inference_policy`
    - `benchmark_ui/service.py` contains `run_intent`
    - `benchmark_ui/service.py` contains `build_profile`
    - `benchmark_ui/service.py` contains `llm_context_window`
    - `benchmark_ui/service.py` contains `inference_policy`
    - `benchmark_ui/service.py` contains `model_name`
  </acceptance_criteria>
  <done>The benchmark stack now has one canonical run-contract builder and one normalized set of run-config inputs for Phase 13 metadata capture.</done>
</task>

<task type="auto">
  <name>Task 2: Persist the run contract at run start and surface it in the benchmark UI</name>
  <files>benchmark_ui/runner.py, benchmark_ui/service.py, benchmark_ui/test_run_contract.py</files>
  <read_first>benchmark_ui/runner.py, benchmark_ui/service.py, benchmark_ui/run_contract.py, benchmark_ui/test_runner_events.py, benchmark_ui/test_service_tier_stats.py</read_first>
  <action>Update `benchmark_ui/runner.py` so run startup builds the normalized contract once, stores it under `summary["run_contract"]`, and writes a sibling `run_contract.json` file in the run directory before snippets execute. Make `benchmark_ui/service.py` prefer the stored contract when rendering historical runs and add explicit info fields for `Model`, `Run intent`, `Execution mode`, `Cache state`, `Ctx window`, `Inference`, and `Build profile`. Add `benchmark_ui/test_run_contract.py` with unit tests that cover required-key persistence, standalone file emission, and historical hydration from a saved summary containing a `run_contract` block.</action>
  <verify>python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_service_tier_stats benchmark_ui.test_runner_events</verify>
  <acceptance_criteria>
    - `benchmark_ui/runner.py` contains `run_contract`
    - `benchmark_ui/runner.py` contains `run_contract.json`
    - `benchmark_ui/service.py` contains `Model`
    - `benchmark_ui/service.py` contains `Run intent`
    - `benchmark_ui/service.py` contains `Execution mode`
    - `benchmark_ui/service.py` contains `Cache state`
    - `benchmark_ui/service.py` contains `Ctx window`
    - `benchmark_ui/service.py` contains `Build profile`
    - `benchmark_ui/test_run_contract.py` contains `class TestRunContract`
    - `benchmark_ui/test_run_contract.py` contains `test_runner_persists_run_contract`
    - `benchmark_ui/test_run_contract.py` contains `test_historical_run_uses_saved_run_contract`
    - `python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_service_tier_stats benchmark_ui.test_runner_events` exits 0
  </acceptance_criteria>
  <done>Saved runs now carry a stable Phase 13 run contract that the UI can display and reload without recomputing comparison metadata.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n 'RUN_CONTRACT_VERSION|REQUIRED_RUN_CONTRACT_KEYS|def build_run_contract' benchmark_ui/run_contract.py`
- [ ] `rg -n 'run_contract|run_contract.json' benchmark_ui/runner.py`
- [ ] `rg -n 'Model|Run intent|Execution mode|Cache state|Ctx window|Inference|Build profile' benchmark_ui/service.py`
- [ ] `python3 -m unittest benchmark_ui.test_run_contract benchmark_ui.test_service_tier_stats benchmark_ui.test_runner_events`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Every saved benchmark run carries one canonical run contract with the Phase 13 comparison keys
- Saved-run hydration and benchmark UI info fields use the stored contract rather than recomputing conflicting metadata
- `MAC-01`, `EVD-03`, and `EVD-05` now have one concrete metadata contract to build on in later waves
</success_criteria>

<output>
After completion, create `.planning/phases/13-measurement-and-run-contract-hardening/13-01-SUMMARY.md`
</output>
