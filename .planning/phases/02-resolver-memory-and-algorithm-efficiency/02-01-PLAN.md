---
phase: 02-resolver-memory-and-algorithm-efficiency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/apdr/src/resolver/pre_solve.rs
  - tools/apdr/src/resolver/pypi_client.rs
  - tools/apdr/tests/test_resolver.rs
autonomous: true
requirements:
  - EFF-01
  - EFF-02
  - EFF-03
  - EFF-05
user_setup: []
must_haves:
  truths:
    - The SMT pre-solve path no longer aggregates worker results through shared `Arc<Mutex<...>>` buckets or panic-prone `Arc::try_unwrap(...).unwrap()` teardown.
    - Metadata fetch and dependency-spec persistence reuse shared helper paths instead of rebuilding dependency-name vectors and knowledge-cache writes in every source branch.
    - Candidate-order preference and hard-unsat versus incomplete-metadata behavior remain deterministic after the refactor.
  artifacts:
    - path: "tools/apdr/src/resolver/pre_solve.rs"
      provides: "Owned worker-result aggregation for multi-version SMT pre-solve"
      contains: "struct PythonSolveAttempt"
    - path: "tools/apdr/src/resolver/pypi_client.rs"
      provides: "Shared helpers for persisting version and dependency metadata"
      contains: "fn persist_dependency_specs"
    - path: "tools/apdr/tests/test_resolver.rs"
      provides: "Regression coverage for candidate-order preservation and incomplete-metadata fallback"
      contains: "pre_solver_preserves_candidate_order_without_mutex_aggregation"
  key_links:
    - from: "tools/apdr/src/resolver/pre_solve.rs"
      to: "tools/apdr/src/resolver/pypi_client.rs"
      via: "pre-solve still relies on bulk-prefetched metadata, but the result ownership path is simplified"
      pattern: "bulk_prefetch_from_kgraph"
    - from: "tools/apdr/tests/test_resolver.rs"
      to: "tools/apdr/src/resolver/pre_solve.rs"
      via: "tests lock the new worker aggregation against ordering and fallback regressions"
      pattern: "solve_dependency_graph"
    - from: "tools/apdr/src/resolver/pypi_client.rs"
      to: "tools/apdr/src/resolver/pre_solve.rs"
      via: "shared metadata persistence keeps dependency-spec reuse cheap during solve setup"
      pattern: "dependency_specs"
---

<objective>
Remove avoidable shared-state contention and repeated metadata persistence from the first resolver hot path.

Purpose: Phase 2 starts by making multi-version SMT pre-solve cheaper and easier to reason about before touching the much larger retry loop in `resolver/mod.rs`.
Output: A lock-light pre-solve aggregation path, consolidated metadata persistence helpers in the PyPI client, and targeted resolver regression tests.
</objective>

<execution_context>
@C:\Users\danny\.codex\get-shit-done\workflows\execute-plan.md
@C:\Users\danny\.codex\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\apdr\.planning\PROJECT.md
@D:\apdr\.planning\ROADMAP.md
@D:\apdr\.planning\STATE.md
@D:\apdr\.planning\phases\02-resolver-memory-and-algorithm-efficiency\02-RESEARCH.md
@D:\apdr\.planning\phases\02-resolver-memory-and-algorithm-efficiency\02-VALIDATION.md
@D:\apdr\.planning\phases\01-baseline-and-guardrails\01-HOTSPOT-AUDIT.md
@D:\apdr\.planning\phases\01-baseline-and-guardrails\01-baseline.json
@D:\apdr\tools\apdr\src\resolver\pre_solve.rs
@D:\apdr\tools\apdr\src\resolver\pypi_client.rs
@D:\apdr\tools\apdr\tests\test_resolver.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace mutex-based pre-solve result aggregation with owned worker results</name>
  <files>tools/apdr/src/resolver/pre_solve.rs</files>
  <read_first>tools/apdr/src/resolver/pre_solve.rs, tools/apdr/src/resolver/pypi_client.rs, tools/apdr/tests/test_resolver.rs</read_first>
  <action>In `tools/apdr/src/resolver/pre_solve.rs`, keep the single-version fast path intact but refactor the multi-version branch of `solve_dependency_graph()` to use an owned result carrier named `PythonSolveAttempt { python_version, outcome }` instead of `Arc<Mutex<BTreeMap<...>>>` and `Arc<Mutex<Vec<_>>>` buckets. Each scoped thread should clone only the `SolveState` and `CacheStore` it needs, return one `PythonSolveAttempt` through its join handle, and let the parent thread build the success, hard-failure, and incomplete-failure sets after all joins complete. Preserve the current candidate-order preference when selecting the winning Python version, and preserve the current hard-unsat versus incomplete-metadata messages so the existing pre-solve failure semantics remain stable.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver pre_solver_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/resolver/pre_solve.rs` contains `struct PythonSolveAttempt`
    - `tools/apdr/src/resolver/pre_solve.rs` does not contain `Arc::try_unwrap(successes)`
    - `tools/apdr/src/resolver/pre_solve.rs` does not contain `Arc::try_unwrap(hard_failures)`
    - `tools/apdr/src/resolver/pre_solve.rs` does not contain `Arc::try_unwrap(incomplete_failures)`
    - `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver pre_solver_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>The SMT pre-solve worker aggregation path no longer depends on shared mutex result buckets or unwrap-based teardown.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate version and dependency metadata persistence helpers</name>
  <files>tools/apdr/src/resolver/pypi_client.rs</files>
  <read_first>tools/apdr/src/resolver/pypi_client.rs, tools/apdr/src/resolver/pre_solve.rs, tools/apdr/tests/test_resolver.rs</read_first>
  <action>Add shared helpers in `tools/apdr/src/resolver/pypi_client.rs` named `dependency_names_from_specs`, `persist_versions`, and `persist_dependency_specs`. `persist_versions` must save non-empty version lists into `CacheStore` and update the in-process knowledge cache in one helper call. `persist_dependency_specs` must save the raw dependency specs, derive dependency names exactly once through `requirement_name`, save the dependency-graph entry when names exist, and update the in-process knowledge cache from the same helper. Rewrite the cache, KGraph, smartPip, PyPI, and bulk-prefetch branches in `fetch_versions()`, `dependency_specs()`, and `bulk_prefetch_from_kgraph()` to call those helpers instead of repeating `save_*`, `collect::<Vec<_>>()`, and separate knowledge-cache lock blocks in every branch.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver pre_solver_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/resolver/pypi_client.rs` contains `fn dependency_names_from_specs(`
    - `tools/apdr/src/resolver/pypi_client.rs` contains `fn persist_versions(`
    - `tools/apdr/src/resolver/pypi_client.rs` contains `fn persist_dependency_specs(`
    - `tools/apdr/src/resolver/pypi_client.rs` contains `persist_dependency_specs(store, package_name, version`
    - `tools/apdr/src/resolver/pypi_client.rs` contains `persist_versions(store, package_name`
  </acceptance_criteria>
  <done>The PyPI client uses one shared persistence path for versions and dependency specs instead of several repeated update branches.</done>
</task>

<task type="auto">
  <name>Task 3: Add targeted tests for candidate order and incomplete-metadata fallback</name>
  <files>tools/apdr/tests/test_resolver.rs</files>
  <read_first>tools/apdr/tests/test_resolver.rs, tools/apdr/src/resolver/pre_solve.rs, tools/apdr/src/resolver/pypi_client.rs</read_first>
  <action>Extend `tools/apdr/tests/test_resolver.rs` with two explicit tests: `pre_solver_preserves_candidate_order_without_mutex_aggregation()` and `pre_solver_keeps_incomplete_metadata_separate_from_hard_failures()`. The first test must exercise the multi-version pre-solve path and prove that the first satisfiable Python version in candidate order still wins after the worker aggregation refactor. The second test must exercise the incomplete-metadata path and prove that it is still reported as fallback metadata loss rather than hard UNSAT after the ownership cleanup. Keep both tests validation-free by using `CacheStore::load()` in a temp target directory and preloading only the package/version data needed for the solver.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver pre_solver_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/tests/test_resolver.rs` contains `fn pre_solver_preserves_candidate_order_without_mutex_aggregation()`
    - `tools/apdr/tests/test_resolver.rs` contains `fn pre_solver_keeps_incomplete_metadata_separate_from_hard_failures()`
    - `tools/apdr/tests/test_resolver.rs` contains `solve_dependency_graph(`
    - `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver pre_solver_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>Pre-solve correctness remains covered while the ownership and metadata-persistence changes land.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver pre_solver_ -- --nocapture`
- [ ] `rg -n "PythonSolveAttempt|persist_versions|persist_dependency_specs" tools/apdr/src/resolver/pre_solve.rs tools/apdr/src/resolver/pypi_client.rs`
- [ ] `tools/apdr/tests/test_resolver.rs` contains the two new pre-solve regression tests
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Multi-version pre-solve no longer uses shared mutex result buckets
- Metadata persistence logic is shared instead of repeated across fetch branches
- Pre-solve still preserves candidate order and incomplete-metadata fallback behavior
</success_criteria>

<output>
After completion, create `.planning/phases/02-resolver-memory-and-algorithm-efficiency/02-01-SUMMARY.md`
</output>
