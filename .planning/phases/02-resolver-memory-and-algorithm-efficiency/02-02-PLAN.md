---
phase: 02-resolver-memory-and-algorithm-efficiency
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - tools/apdr/src/resolver/mod.rs
  - tools/apdr/tests/test_resolver.rs
autonomous: true
requirements:
  - EFF-01
  - EFF-03
  - EFF-04
  - EFF-05
user_setup: []
must_haves:
  truths:
    - `validate_with_retries()` re-renders requirements only when dependency state changes instead of rebuilding the same string every retry path.
    - Retry-loop note propagation and dependency lookup use shared helpers instead of repeated normalized scans and duplicated clone-heavy note updates.
    - Resolver recovery behavior remains deterministic and test-covered after the algorithm cleanup.
  artifacts:
    - path: "tools/apdr/src/resolver/mod.rs"
      provides: "Structured retry-loop state and cheaper dependency lookup helpers"
      contains: "struct RetryLoopState"
    - path: "tools/apdr/src/resolver/mod.rs"
      provides: "Shared helper for dirty-state requirement rendering"
      contains: "fn render_requirements_if_dirty"
    - path: "tools/apdr/tests/test_resolver.rs"
      provides: "Regression coverage for normalized dependency identity and retry-loop state reuse"
      contains: "resolver_dependency_updates_use_normalized_package_identity"
  key_links:
    - from: "tools/apdr/src/resolver/mod.rs"
      to: "tools/apdr/src/resolver/pre_solve.rs"
      via: "retry-loop work consumes the cheaper pre-solve outputs from Plan 02-01 without changing correctness guarantees"
      pattern: "solve_dependency_graph"
    - from: "tools/apdr/tests/test_resolver.rs"
      to: "tools/apdr/src/resolver/mod.rs"
      via: "tests lock the new retry-loop helpers against package-identity regressions"
      pattern: "validate_with_retries"
    - from: "tools/apdr/src/resolver/mod.rs"
      to: "scripts/check_apdr_regression.py"
      via: "Phase 2 still closes against the existing regression gate after the retry-loop cleanup"
      pattern: "render_requirements"
---

<objective>
Make the core resolver retry loop cheaper and easier to review without changing recovery semantics.

Purpose: `resolver/mod.rs` is the biggest hot-path file in the codebase, and its retry logic currently mixes mutation, bookkeeping, logging, and candidate selection in ways that add avoidable work and cognitive load.
Output: A structured retry-loop state helper, shared dependency lookup helpers, and resolver regression tests that protect the cheaper algorithm.
</objective>

<execution_context>
@C:\Users\danny\.codex\get-shit-done\workflows\execute-plan.md
@C:\Users\danny\.codex\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\apdr\.planning\PROJECT.md
@D:\apdr\.planning\ROADMAP.md
@D:\apdr\.planning\STATE.md
@D:\apdr\.planning\phases\02-resolver-memory-and-algorithm-efficiency\02-RESEARCH.md
@D:\apdr\.planning\phases\02-resolver-memory-and-algorithm-efficiency\02-VALIDATION.md
@D:\apdr\.planning\phases\02-resolver-memory-and-algorithm-efficiency\02-01-PLAN.md
@D:\apdr\tools\apdr\src\resolver\mod.rs
@D:\apdr\tools\apdr\tests\test_resolver.rs
@D:\apdr\scripts\check_apdr_regression.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce structured retry-loop state and dirty requirements rendering</name>
  <files>tools/apdr/src/resolver/mod.rs</files>
  <read_first>tools/apdr/src/resolver/mod.rs, tools/apdr/src/resolver/pre_solve.rs, tools/apdr/tests/test_resolver.rs</read_first>
  <action>In `tools/apdr/src/resolver/mod.rs`, introduce a retry-loop helper struct named `RetryLoopState` that owns the mutable retry bookkeeping now spread across `validate_with_retries()`: the seen-requirements set, attempted version history, removed-import tracking, and a dirty flag plus cached requirements string. Add a helper named `render_requirements_if_dirty` that only rebuilds `requirements_txt` when the dependency set actually changed, and update `validate_with_retries()` so lockfile keys, oscillation checks, and iteration snapshots use that helper instead of calling `render_requirements(resolved)` opportunistically in multiple branches. Keep current retry-budget handling, candidate-version selection, and iteration snapshot semantics intact.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/resolver/mod.rs` contains `struct RetryLoopState`
    - `tools/apdr/src/resolver/mod.rs` contains `fn render_requirements_if_dirty(`
    - `tools/apdr/src/resolver/mod.rs` contains `RetryLoopState` inside `validate_with_retries(`
    - `tools/apdr/src/resolver/mod.rs` contains `requirements_dirty`
    - `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>The retry loop keeps one authoritative requirements buffer and stops recomputing it when no dependency mutation happened.</done>
</task>

<task type="auto">
  <name>Task 2: Replace repeated dependency scans with shared normalized lookup helpers</name>
  <files>tools/apdr/src/resolver/mod.rs</files>
  <read_first>tools/apdr/src/resolver/mod.rs, tools/apdr/src/resolver/pypi_client.rs, tools/apdr/tests/test_resolver.rs</read_first>
  <action>Add shared lookup helpers in `tools/apdr/src/resolver/mod.rs` named `dependency_index_by_package` and `dependency_index_by_import`. Both helpers must use the existing normalized package-key rules so package identity stays consistent across `dedupe_dependencies`, `update_package_version`, `ensure_dependency`, `upsert_dependency`, seed LLM fallback, empty-log LLM fixes, and build-failure alternatives. Replace the repeated `.iter().find()` or `.iter().position()` scans in those code paths with the shared helpers, and switch `llm_removed_imports` from `Vec<String>` to `BTreeSet<String>` so removed-import tracking stays deduplicated throughout the retry loop.</action>
  <verify>cargo clippy --manifest-path tools/apdr/Cargo.toml --all-targets -- -D warnings</verify>
  <acceptance_criteria>
    - `tools/apdr/src/resolver/mod.rs` contains `fn dependency_index_by_package(`
    - `tools/apdr/src/resolver/mod.rs` contains `fn dependency_index_by_import(`
    - `tools/apdr/src/resolver/mod.rs` contains `BTreeSet<String>` for `llm_removed_imports`
    - `tools/apdr/src/resolver/mod.rs` contains `dependency_index_by_package(resolved`
    - `cargo clippy --manifest-path tools/apdr/Cargo.toml --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>The resolver uses one normalized dependency-identity path instead of repeating package scans and duplicate removed-import tracking.</done>
</task>

<task type="auto">
  <name>Task 3: Add resolver regression tests for normalized identity and retry-loop reuse</name>
  <files>tools/apdr/tests/test_resolver.rs</files>
  <read_first>tools/apdr/tests/test_resolver.rs, tools/apdr/src/resolver/mod.rs</read_first>
  <action>Extend `tools/apdr/tests/test_resolver.rs` with `resolver_dependency_updates_use_normalized_package_identity()` and `resolver_retry_loop_reuses_dirty_requirements_buffer()`. The first test must prove that package updates treat names like `Django`, `django`, and `django==...` as the same dependency identity after the helper refactor. The second test must exercise retry-loop mutation plus re-render behavior and prove that repeated no-op iterations do not keep generating new requirements strings or diverging retry metadata. Keep the tests targeted enough that `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture` remains the main verification command for this plan.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/tests/test_resolver.rs` contains `fn resolver_dependency_updates_use_normalized_package_identity()`
    - `tools/apdr/tests/test_resolver.rs` contains `fn resolver_retry_loop_reuses_dirty_requirements_buffer()`
    - `tools/apdr/tests/test_resolver.rs` contains `resolve_path(`
    - `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>The cheaper retry-loop algorithm remains covered by targeted resolver tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml --test test_resolver resolver_ -- --nocapture`
- [ ] `cargo clippy --manifest-path tools/apdr/Cargo.toml --all-targets -- -D warnings`
- [ ] `rg -n "RetryLoopState|render_requirements_if_dirty|dependency_index_by_package|dependency_index_by_import" tools/apdr/src/resolver/mod.rs`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Retry-loop state is explicit instead of spread across ad-hoc locals
- Dependency mutation paths share normalized lookup helpers
- Resolver behavior remains deterministic and test-covered
</success_criteria>

<output>
After completion, create `.planning/phases/02-resolver-memory-and-algorithm-efficiency/02-02-SUMMARY.md`
</output>
