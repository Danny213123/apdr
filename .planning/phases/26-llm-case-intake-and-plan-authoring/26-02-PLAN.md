---
phase: 26-llm-case-intake-and-plan-authoring
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - tools/apdr/src/lib.rs
  - tools/apdr/src/resolver/mod.rs
  - tools/apdr/test_executor.py
  - tools/apdr/tests/test_resolver.rs
  - benchmark_ui/test_run_contract.py
autonomous: true
requirements:
  - LLM-01
  - TRU-02
user_setup: []
must_haves:
  truths:
    - Successful intake must leave behind a machine-readable authored-plan artifact, not only `requirements.txt` and `resolution-report.txt`.
    - `llm-only` must fail truthfully when no usable authored plan exists instead of silently reconstructing intent from deterministic tiers.
    - Regular `llm` mode may continue downstream, but must mark which plan elements were LLM-authored versus deterministic fallback.
  artifacts:
    - path: "tools/apdr/src/lib.rs"
      provides: "Case artifact and summary export of authored-plan truth"
      contains: "case-plan.json"
    - path: "tools/apdr/test_executor.py"
      provides: "Benchmark output metadata for authored-plan and intake-failure fields"
      contains: "authored_plan_path"
    - path: "benchmark_ui/test_run_contract.py"
      provides: "Contract coverage for new authored-plan metadata keys"
      contains: "authored_plan_path"
  key_links:
    - from: "tools/apdr/src/resolver/mod.rs"
      to: ".planning/phases/26-llm-case-intake-and-plan-authoring/26-CONTEXT.md"
      via: "The context locks `llm-only` as strict: fail truthfully if intake cannot produce a usable plan."
      pattern: "llm-only remains stricter"
    - from: "tools/apdr/src/lib.rs"
      to: ".planning/ROADMAP.md"
      via: "Phase 26 success requires saved artifacts and debug folders to make plan authorship truth explicit."
      pattern: "Saved artifacts and debug folders make it clear"
---

<objective>
Persist authored-plan truth into the real case outputs and enforce the Phase 26 mode semantics around usable versus unusable intake plans.

Purpose: A richer IPC payload is not enough on its own. The case directory, report text, and benchmark summary stream need to preserve authored-plan truth, and `llm-only` must stop silently proceeding after unusable intake.
Output: case-plan and intake-failure artifacts, exported metadata keys, and strict `llm-only` semantics backed by regression tests.
</objective>

<execution_context>
@/Users/dannyguan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dannyguan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-llm-case-intake-and-plan-authoring/26-CONTEXT.md
@.planning/phases/26-llm-case-intake-and-plan-authoring/26-RESEARCH.md
@.planning/phases/26-llm-case-intake-and-plan-authoring/26-VALIDATION.md
@.planning/phases/26-llm-case-intake-and-plan-authoring/26-01-PLAN.md
@tools/apdr/src/lib.rs
@tools/apdr/src/resolver/mod.rs
@tools/apdr/test_executor.py
@tools/apdr/tests/test_resolver.rs
@benchmark_ui/test_run_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write authored-plan and intake-failure artifacts into case outputs</name>
  <files>tools/apdr/src/lib.rs, tools/apdr/test_executor.py, tools/apdr/tests/test_resolver.rs</files>
  <read_first>tools/apdr/src/lib.rs, tools/apdr/test_executor.py, tools/apdr/tests/test_resolver.rs, .planning/phases/26-llm-case-intake-and-plan-authoring/26-RESEARCH.md</read_first>
  <action>Extend the APDR result-writing path in `tools/apdr/src/lib.rs` so each case can write a machine-readable authored-plan artifact such as `case-plan.json` and, when needed, an `intake-failure.json` artifact. Add report text and summary-line fields for authored plan status, authored plan path, intake-failure class, intake-failure path, and whether a downstream plan section came from deterministic fallback. Update `tools/apdr/test_executor.py` to export those summary keys into benchmark `output_metadata`. Add Rust tests prefixed `phase26_truth_` that verify the new files and summary lines are written consistently.</action>
  <verify>cargo test --manifest-path tools/apdr/Cargo.toml phase26_truth_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/lib.rs` contains `case-plan.json`
    - `tools/apdr/src/lib.rs` contains `intake-failure.json`
    - `tools/apdr/src/lib.rs` contains `AUTHORED_PLAN_PATH`
    - `tools/apdr/test_executor.py` contains `authored_plan_path`
    - `tools/apdr/test_executor.py` contains `intake_failure_class`
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase26_truth_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>Successful and failed intake cases now leave explicit authored-plan or intake-failure artifacts that flow into benchmark metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce strict `llm-only` intake semantics and preserve deterministic-fallback truth for `llm`</name>
  <files>tools/apdr/src/resolver/mod.rs, tools/apdr/src/lib.rs, benchmark_ui/test_run_contract.py, tools/apdr/tests/test_resolver.rs</files>
  <read_first>tools/apdr/src/resolver/mod.rs, tools/apdr/src/lib.rs, benchmark_ui/test_run_contract.py, .planning/phases/26-llm-case-intake-and-plan-authoring/26-CONTEXT.md</read_first>
  <action>Update the main resolver flow in `tools/apdr/src/resolver/mod.rs` so `llm-only` stops immediately and truthfully when the authored intake plan is unusable, while regular `llm` mode may continue only if it marks downstream deterministic substitutions clearly in the exported authorship truth. Extend the output metadata and any affected tests so saved run contracts and result rows can preserve authored-plan status without needing a UI redesign. Add focused regression tests proving that `llm-only` no-output becomes an intake failure and that `llm` fallback paths remain explicitly labeled as non-authored sections rather than masquerading as fully LLM-authored intake.</action>
  <verify>python3 -m unittest benchmark_ui.test_run_contract && cargo test --manifest-path tools/apdr/Cargo.toml phase26_truth_ -- --nocapture</verify>
  <acceptance_criteria>
    - `tools/apdr/src/resolver/mod.rs` contains `llm_only_mode`
    - `tools/apdr/src/resolver/mod.rs` contains `authored_plan_status`
    - `benchmark_ui/test_run_contract.py` contains `authored_plan_path`
    - `benchmark_ui/test_run_contract.py` contains `intake_failure_class`
    - `python3 -m unittest benchmark_ui.test_run_contract` exits 0
    - `cargo test --manifest-path tools/apdr/Cargo.toml phase26_truth_ -- --nocapture` exits 0
  </acceptance_criteria>
  <done>`llm-only` now fails honestly on unusable intake while `llm` preserves explicit authorship truth for any deterministic downstream substitutions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path tools/apdr/Cargo.toml phase26_truth_ -- --nocapture`
- [ ] `python3 -m unittest benchmark_ui.test_run_contract`
- [ ] `rg -n 'case-plan.json|intake-failure.json|AUTHORED_PLAN_PATH|INTAKE_FAILURE_CLASS|authored_plan_path|intake_failure_class' tools/apdr/src/lib.rs tools/apdr/src/resolver/mod.rs tools/apdr/test_executor.py benchmark_ui/test_run_contract.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Case artifacts now show authored-plan truth directly
- `llm-only` no-output cases fail as intake failures instead of degrading into opaque downstream behavior

</success_criteria>

<output>
After completion, create `.planning/phases/26-llm-case-intake-and-plan-authoring/26-02-SUMMARY.md`
</output>
