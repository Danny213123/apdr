---
phase: 02-result-categorization-insights
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - web/index.html
  - web/src/styles.css
  - web/src/main.js
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-05, CAT-06]

must_haves:
  truths:
    - User sees "Deterministic Results" panel for tier1/tier2 cases
    - User filters deterministic results by status, tier, and Python version
    - User searches deterministic cases by ID or snippet content
    - Filter state persists during SSE updates (no reset when new cases arrive)
  artifacts:
    - path: web/index.html
      provides: Renamed panel with filter controls
      contains: "Deterministic Results"
    - path: web/src/styles.css
      provides: Filter toolbar and tier badge styles
      contains: ".filter-toolbar"
    - path: web/src/main.js
      provides: Filter logic and tier badge rendering
      contains: "filterDeterministicCases"
  key_links:
    - from: web/src/main.js
      to: case data from SSE
      via: filter functions check tier field
      pattern: 'tier.*=== "tier1"\\|tier2'
---

<objective>
Enhance Completed Cases panel for deterministic results with filtering, search, and tier badges.

Purpose: Separate tier1/tier2 (deterministic) from tier3 (LLM) cases with rich filtering capabilities (per D-01, D-02, D-03, D-06, D-08, D-09, D-10 from CONTEXT.md).

Output: Deterministic Results panel with per-section filters, search, and tier column.
</objective>

<execution_context>
@D:\apdr\.claude\get-shit-done\workflows\execute-plan.md
@D:\apdr\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\apdr\.planning\PROJECT.md
@D:\apdr\.planning\ROADMAP.md
@D:\apdr\.planning\phases\02-result-categorization-insights\02-CONTEXT.md
@D:\apdr\.planning\phases\02-result-categorization-insights\02-UI-SPEC.md
@D:\apdr\.planning\phases\01-non-blocking-ui-foundation\01-03-SUMMARY.md
</context>

<interfaces>
<!-- Key types from existing codebase -->

From web/index.html (existing structure):
```html
<details class="terminal-section cases-section collapsible-panel" id="cases-panel" open>
  <summary class="collapsible-summary">
    <span class="section-title">Completed Cases <span class="collapsible-count" id="cases-count"></span></span>
    <span class="collapsible-meta">expand / collapse</span>
  </summary>
  <div class="collapsible-body">
    <div class="collapsible-inner">
      <!-- Filter controls will be added here -->
      <div class="case-table-head">
        <span>STAT</span>
        <span>CASE ID</span>
        <!-- Existing columns -->
      </div>
      <div class="cases-scroll" id="cases-scroll"></div>
    </div>
  </div>
</details>
```

From web/src/styles.css (existing patterns):
```css
.custom-select { /* Existing dropdown component */ }
.toolbar-line { /* Existing toolbar layout */ }
.case-table-head { /* Existing table header */ }
```

From Plan 02-01 (tier metadata):
```javascript
case_complete event: {
  type: "case_complete",
  caseId: "test-001",
  status: "pass",
  tier: "tier1",  // "tier1" | "tier2" | "tier3"
  confidence: null,  // Only for tier3
  pythonVersion: "3.11",  // From existing data
  timestamp: "..."
}
```

From 02-UI-SPEC.md (Filter Contract):
Deterministic Section Filters:
- Status: All, Pass, Fail, Skip
- Tier: All, Tier1, Tier2
- Python: All, 2.7, 3.6, 3.7, 3.8, 3.9, 3.10, 3.11, 3.12
- Search: Case ID, snippet content, dependencies
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rename panel and add filter controls</name>
  <files>web/index.html</files>
  <action>
    Rename "Completed Cases" to "Deterministic Results" and add per-section filter toolbar.

    1. Update panel title (line ~183):
       ```html
       <span class="section-title">Deterministic Results <span class="collapsible-count" id="cases-count"></span></span>
       <span class="section-subtitle">(tier1 cache + tier2 heuristic)</span>
       ```

    2. Add filter toolbar inside collapsible-inner (before case-table-head, line ~188):
       ```html
       <div class="filter-toolbar">
         <label class="toolbar-label" for="det-status-filter">Status</label>
         <div class="custom-select custom-select-compact" id="det-status-filter" data-placeholder="All">
           <button class="custom-select-trigger" type="button" aria-haspopup="listbox" aria-expanded="false" aria-label="Filter by status">
             <span class="custom-select-label">All</span>
             <span class="custom-select-arrow" aria-hidden="true">▾</span>
           </button>
           <div class="custom-select-menu" role="listbox" aria-label="Status options">
             <div class="custom-select-option" role="option" data-value="all">All</div>
             <div class="custom-select-option" role="option" data-value="pass">Pass</div>
             <div class="custom-select-option" role="option" data-value="fail">Fail</div>
             <div class="custom-select-option" role="option" data-value="skip">Skip</div>
           </div>
         </div>

         <label class="toolbar-label" for="det-tier-filter">Tier</label>
         <div class="custom-select custom-select-compact" id="det-tier-filter" data-placeholder="All">
           <button class="custom-select-trigger" type="button" aria-haspopup="listbox" aria-expanded="false" aria-label="Filter by tier">
             <span class="custom-select-label">All</span>
             <span class="custom-select-arrow" aria-hidden="true">▾</span>
           </button>
           <div class="custom-select-menu" role="listbox" aria-label="Tier options">
             <div class="custom-select-option" role="option" data-value="all">All</div>
             <div class="custom-select-option" role="option" data-value="tier1">Tier1</div>
             <div class="custom-select-option" role="option" data-value="tier2">Tier2</div>
           </div>
         </div>

         <label class="toolbar-label" for="det-python-filter">Python</label>
         <div class="custom-select custom-select-compact" id="det-python-filter" data-placeholder="All">
           <button class="custom-select-trigger" type="button" aria-haspopup="listbox" aria-expanded="false" aria-label="Filter by Python version">
             <span class="custom-select-label">All</span>
             <span class="custom-select-arrow" aria-hidden="true">▾</span>
           </button>
           <div class="custom-select-menu" role="listbox" aria-label="Python version options">
             <div class="custom-select-option" role="option" data-value="all">All</div>
             <div class="custom-select-option" role="option" data-value="2.7">2.7</div>
             <div class="custom-select-option" role="option" data-value="3.6">3.6</div>
             <div class="custom-select-option" role="option" data-value="3.7">3.7</div>
             <div class="custom-select-option" role="option" data-value="3.8">3.8</div>
             <div class="custom-select-option" role="option" data-value="3.9">3.9</div>
             <div class="custom-select-option" role="option" data-value="3.10">3.10</div>
             <div class="custom-select-option" role="option" data-value="3.11">3.11</div>
             <div class="custom-select-option" role="option" data-value="3.12">3.12</div>
           </div>
         </div>

         <label class="toolbar-label" for="det-search-input">Search</label>
         <input class="filter-search-input" id="det-search-input" type="search" placeholder="search case id, result, dependency..." />
       </div>
       ```

    3. Add TIER column to table header (after STAT, before CASE ID):
       ```html
       <div class="case-table-head">
         <span>STAT</span>
         <span>TIER</span>  <!-- NEW -->
         <span>CASE ID</span>
         <!-- Existing columns -->
       </div>
       ```

    Per D-01: Repurpose existing two-panel structure — rename "Completed Cases" → "Deterministic Results".
    Per D-05, D-06: Per-section filter controls — each panel gets its own filter toolbar.
    Per D-08, D-09: Search input per section, use existing custom-select dropdown pattern.
  </action>
  <verify>
    <automated>grep -n "Deterministic Results\|det-status-filter\|det-tier-filter\|TIER" web/index.html</automated>
  </verify>
  <done>
    Panel renamed, filter toolbar added, TIER column in table header
  </done>
</task>

<task type="auto">
  <name>Task 2: Add filter toolbar and tier badge CSS</name>
  <files>web/src/styles.css</files>
  <action>
    Add CSS for filter toolbar and tier badges following terminal aesthetic.

    1. Add `.filter-toolbar` layout (after .case-controls, ~line 650):
       ```css
       .filter-toolbar {
         display: flex;
         flex-wrap: wrap;
         align-items: center;
         gap: 12px 12px;
         padding: 8px 0 16px 0;
         border-bottom: 1px solid rgba(122, 127, 138, 0.35);
         margin-bottom: 12px;
       }

       .filter-search-input {
         flex: 1;
         min-width: 200px;
         background: var(--bg);
         border: 1px solid rgba(122, 127, 138, 0.75);
         color: var(--text);
         padding: 6px 8px;
         font-family: inherit;
         font-size: 14px;
       }

       .filter-search-input:focus {
         outline: none;
         border-color: var(--yellow);
         box-shadow: 0 0 0 2px rgba(255, 217, 94, 0.2);
       }
       ```

    2. Add `.tier-badge` styles (after .case-row, ~line 580):
       ```css
       .tier-badge {
         display: inline-block;
         padding: 2px 6px;
         font-size: 12px;
         font-weight: 700;
         border-radius: 2px;
       }

       .tier-badge.tier1 {
         color: var(--green);
         background: rgba(65, 242, 156, 0.1);
       }

       .tier-badge.tier2 {
         color: var(--yellow);
         background: rgba(255, 217, 94, 0.1);
       }

       .tier-badge.tier3 {
         color: var(--blue);
         background: rgba(143, 209, 255, 0.1);
       }
       ```

    3. Add `.section-subtitle` for panel subtitle (after .section-title, ~line 450):
       ```css
       .section-subtitle {
         font-size: 12px;
         color: var(--muted);
         font-weight: 400;
         margin-left: 8px;
       }
       ```

    Per 02-UI-SPEC: 12px horizontal gap, 8px vertical gap, flex-wrap for responsive layout.
    Per 02-UI-SPEC: Tier badges — Tier1 (green), Tier2 (yellow), Tier3 (blue).
  </action>
  <verify>
    <automated>grep -n "filter-toolbar\|tier-badge\|section-subtitle" web/src/styles.css</automated>
  </verify>
  <done>
    Filter toolbar CSS added, tier badge styles match UI-SPEC colors
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement filter and search logic</name>
  <files>web/src/main.js</files>
  <action>
    Add filter state management and filtering logic for deterministic results.

    1. Extend state object with filter state (after line ~26):
       ```javascript
       deterministicFilters: {
         status: "all",
         tier: "all",
         python: "all",
         search: ""
       },
       ```

    2. Add filter state update handlers (after custom select initialization, ~line 800):
       ```javascript
       function setupDeterministicFilters() {
         // Status filter
         setupCustomSelect(ui.detStatusFilter, (value) => {
           state.deterministicFilters.status = value;
           applyDeterministicFilters();
         });

         // Tier filter
         setupCustomSelect(ui.detTierFilter, (value) => {
           state.deterministicFilters.tier = value;
           applyDeterministicFilters();
         });

         // Python filter
         setupCustomSelect(ui.detPythonFilter, (value) => {
           state.deterministicFilters.python = value;
           applyDeterministicFilters();
         });

         // Search input (debounced 150ms per UI-SPEC performance contract)
         let searchDebounce = null;
         ui.detSearchInput.addEventListener("input", (e) => {
           clearTimeout(searchDebounce);
           searchDebounce = setTimeout(() => {
             state.deterministicFilters.search = e.target.value.toLowerCase();
             applyDeterministicFilters();
           }, 150);
         });
       }
       ```

    3. Implement filtering logic:
       ```javascript
       function applyDeterministicFilters() {
         if (!state.currentRun || !state.currentRun.results) return;

         const filtered = state.currentRun.results.filter(caseData => {
           // Filter by tier (deterministic = tier1 or tier2)
           const tier = caseData.tier || "unknown";
           if (tier !== "tier1" && tier !== "tier2") return false;

           // Status filter
           if (state.deterministicFilters.status !== "all") {
             if (caseData.status !== state.deterministicFilters.status) return false;
           }

           // Tier filter
           if (state.deterministicFilters.tier !== "all") {
             if (tier !== state.deterministicFilters.tier) return false;
           }

           // Python filter
           if (state.deterministicFilters.python !== "all") {
             const pyVersion = caseData.pythonVersion || "";
             if (pyVersion !== state.deterministicFilters.python) return false;
           }

           // Search filter (case ID, snippet content, dependencies)
           if (state.deterministicFilters.search) {
             const searchTerm = state.deterministicFilters.search;
             const caseId = (caseData.caseId || "").toLowerCase();
             const result = (caseData.result || "").toLowerCase();
             const deps = (caseData.dependencies || "").toLowerCase();

             if (!caseId.includes(searchTerm) &&
                 !result.includes(searchTerm) &&
                 !deps.includes(searchTerm)) {
               return false;
             }
           }

           return true;
         });

         renderDeterministicCases(filtered);
       }
       ```

    4. Add tier badge rendering to case row creation:
       ```javascript
       function createCaseRow(caseData) {
         // Existing case row creation logic
         const row = document.createElement("div");
         row.className = "case-row";
         row.dataset.caseId = caseData.caseId;

         // Add tier badge cell (after status, before case ID)
         const tier = caseData.tier || "unknown";
         const tierBadge = `<span class="tier-badge ${tier}">${tier.toUpperCase()}</span>`;

         // Insert tierBadge into row HTML structure
         // ... existing row rendering
       }
       ```

    5. Call setupDeterministicFilters in initialize():
       ```javascript
       setupDeterministicFilters();
       ```

    Per D-10: Filter state preserved during SSE updates — filters apply on render, not cleared.
    Per 02-UI-SPEC: Search debounced to 150ms to prevent excessive re-renders.
  </action>
  <verify>
    <automated>grep -n "deterministicFilters\|applyDeterministicFilters\|tier-badge" web/src/main.js</automated>
  </verify>
  <done>
    Filter logic implemented, tier badges render in table, filters persist during SSE updates
  </done>
</task>

</tasks>

<verification>
1. Visual inspection: Open browser, navigate to Benchmark View tab
2. Verify panel title reads "Deterministic Results (tier1 cache + tier2 heuristic)"
3. Verify filter toolbar appears with 4 controls (Status, Tier, Python, Search)
4. Start benchmark, observe tier1/tier2 cases appear with tier badges
5. Test filters: Select "Pass" status, verify only pass cases shown
6. Test search: Type case ID, verify real-time filtering
7. Add new case via SSE, verify filters still applied (no reset)
</verification>

<success_criteria>
- Panel renamed to "Deterministic Results" with subtitle
- Filter toolbar with Status, Tier, Python, Search controls
- TIER column in table header with colored badges
- Filters work independently (status AND tier AND python AND search)
- Filter state persists during SSE updates
- Search debounced to prevent performance issues
</success_criteria>

<output>
After completion, create `.planning/phases/02-result-categorization-insights/02-03-SUMMARY.md`
</output>
