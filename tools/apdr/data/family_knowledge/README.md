# Curated Family Knowledge

Phase 8 touched-family scope only.

This directory seeds only the family-owned runtime surfaces moved in Phase 8.
It remains anchored to the 17-case Phase 7 snapshot corpus documented in
`.planning/phases/07-failure-baseline-parity-slice/07-family-snapshot-manifest.json`.

Included surfaces:

- Registry families: `setuptools`, `pil`, `sklearn`
- Bundle and recovery families: `legacy-pymc3`, `legacy-tensorflow`, `legacy-ggplot`
- Recovery rules: `pkg-resources`, `legacy-pillow`, `legacy-pymc3`, `legacy-tensorflow`, `keras-backend`, `legacy-ggplot`

Do not widen this directory beyond the protected Phase 7 touched boundary
without updating the Phase 7 family snapshot artifacts and the Phase 8
regression checker.
